import type { ObservationTrace } from "../../core/types.js";
export declare const repeatedDecisions: (traces: Partial<ObservationTrace>[]) => string[];
export declare const repeatedOutcomes: (traces: Partial<ObservationTrace>[]) => string[];
