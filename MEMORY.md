# MEMORY.md — dsh-shadow 复盘：究竟懂了什么 / 边界（供下次记忆）

> 这是把 dsh-shadow 做完后**反推出来的关键理解与边界**，写给未来的自己/另一个 agent。
> 术语表见 `CONTEXT.md`，架构取舍见 `adr/`。本文记"为什么这么想"与"别踩的坑"。

## 一、究竟懂了什么才把任务做出来

### ◆ Forget → Recall Continuity Principle（2026-09-07 正式记录，作为后续 ADR 的基础）

> **时间连续性的另一半：遗忘不是终点，遗忘之后必须存在"忆起"机制。** 否则 Memory / Identity / Experience 链缺一个重要闭环。

此前 v0.20–v0.36 隐含假设是 `记住 → 压缩 → 使用`；但真实观察者更接近 `经历 → 记忆 → 遗忘 → 沉寂 → 线索触发 → 忆起 → 重新评估 → 当前现实融合`。观察者不只拥有信息，而是拥有自己的形成历史——没有 Recall，则 Identity/Temporal/Reflection/Long Horizon 都不完整，最终退化成"**知道现在，但无法解释自己如何成为现在**"。

**三个概念区分**：
- **Forget ≠ Delete**：遗忘是"当前不可直接访问"，不是"信息不存在"。状态流 `active → forgotten → latent → recalled`。
- **Recall ≠ Restore**：忆起不是简单恢复旧状态；危险路径是 `旧 Memory → Recall → 直接恢复旧信念 → 污染当前 Reality`。正确路径是 `Recall → Historical Evidence → Current Validation → 重新进入 Observer 判断`。**忆起的是"过去曾经发生过的观察"，不是"过去结论自动复活"**。
- **Recall = Past Observation Reintroduced Into Present Context For Re-evaluation**。

**核心公式**：
```
Forget ≠ Delete
Recall ≠ Restore
Recall = Past Observation Reintroduced Into Present Context For Re-evaluation
```

**必须冻结的边界**（否则破坏前面所有设计）：
- `Recall ≠ Truth`：忆起 = "我曾经认为 X"，不是 "X 现在仍然正确"。
- `Recall ≠ Identity Rewrite`：不能 `Recall old personality → modify current Identity`。
- `Forgotten ≠ Lost Evidence`：遗忘后的证据仍可被重新验证。
- `Recall ≠ Memory Resurrection`：不能 `old memory → restore old world model`。
- 与既有原则一致：`Evidence ≠ Knowledge / Validated ≠ Truth / Historical ≠ Current`。

**对象模型草案（供后续 ADR-0031 固化）**：`ForgottenTrace{traceId, originalObservation, forgottenAt, reason, retentionBoundary}`（曾经存在但现在不可直接访问）/ `RecallTrigger{triggerContext, matchedTrace, confidence, reason}`（当前上下文+线索+过去痕迹，**不是** `search memory`）/ `RecallEvent{recalledTrace, currentContext, validationRequired:true}`（某过去经验重新进入当前 Observer）。

**架构定位**：横跨 Identity Continuity / Temporal / Memory Compression / Reality Feedback；比 v0.36 Delegation **更底层**。建议作为独立协议层，路线插入为 **v0.37 Recall / Remembrance Boundary**（或拆 v0.27.1 Forgetting / v0.27.2 Recall），先冻结 `v0.27 Offline Compression → v0.28 Validation →【Recall Boundary】→ v0.36 Delegation`。**这条"时间连续性另一半"正是"Observer 只拥有信息 vs 拥有自己的形成历史"的分水岭。**

1. **先定哲学，再定结构。** 这不是"记动作的日志"，而是「思维/上下文/灵魂的投影」，且「一切皆文件」——所以**每条记忆 = 一个文件**，而不是日志里的一行。这个判断直接决定文件树结构，而不是先写代码再起名。
   **核心目的（用户 2026-09-05 强调）**：dsh-shadow 是要**为每个已完成的任务记录「完整线索链」**——它**靠什么背景/材料**、**用户交互中提醒的注意事项/决策**，才得以完成。「完整线索」是最重要的产出（为什么、靠什么、怎么完成的），而不是零散事件。
2. **五类信号、五档可靠度。** DSH 里"发生了什么"是事件：
   - `fs/observed`（真实改/读的组件）→ **入口点**，客观锚（"用户在哪干活"）。
   - `goal/changed`（goal 变更）→ **决策/意向**。
   - `tools/result`（工具调用）→ **动作背景**。
   - `session/event`（会话消息流）→ **交互 + 思维落点**。
   - `agent/turn-stopping`（回合收口）→ **落盘时机**。
   可靠度从高到低排序，就把"哪部分能做实、哪部分尽力而为"定了。
3. **DSH 里"长期在"的是 host bundle，不是动态插件。** 会活过一个重启、跨会话的东西是 **profile 插件**（`dsh.bundle.patch` 注入宿主 composition 行）。会话内的 `cordis_define/cordis_run` 动态插件**重启即丢**。所以 dsh-shadow 走 profile bundle 插件。
4. **提示词注入用 `systemPrompt.context()`，别用 `section()`。** DSH 的 complete-section replacement 会覆盖掉普通 section；context 走独立的"当前运行时上下文"路径，不被覆盖（dsh-wechat 踩过的坑，直接避开）。
5. **`fs.writeText` 会建父目录**（实践中验证过），所以能放心写 `shadow/<date>/...` 嵌套结构。
6. **许多确切形状查不到，就用"守卫式最优解 + 重启后看运行时诊断再精修"。** DSH 打包源码是 minified 的，`session/event` 消息载荷没法干净查到——先按常见形状守卫式写，重启后抓一个真实载荷再校字段。

## 二、边界（能做什么 / 不能做什么）

**能**
- 采集：动作(`fs/observed`+`tools/result`) + 决策(`goal/changed`) + 尽力而为的交互/思维(`session/event`)。
- 一记忆一文件：`shadow/<日期>/<时刻>-<入口slug>.md`。
- `shadow/_index.md`：说明文档 + 今日摘要 + 近期记忆 + 主题索引 + 意识轨迹。
- `read_shadow`：无参读索引；带 `topic` 穿透到记忆文件。
- 按工作区隔离（`agent.session.header.cwd`，可 `shadowRoot` 覆盖）。
- 资源卡与 `resource` 节点（v1.14.0，ADR-0051）：`.shadow/resources/<name>.md`（固有层 + `## 投影 @ <问题>` 段）→ 派生 `ShadowNode{type:"resource"}`（第 6 个 NodeType）；`shadow_query` 的 `scope` 可收 `resource`；一张卡片一个节点、`evidence` 取卡片的 `source`；**插件只读卡片、不写卡片**。

**能（增强：一句话总结）**
- 每一回合落盘后用 `llm.stream` 生成一两句中文摘要，回填记忆文件头（`> 摘要：…`），让纯聊天/无工具回合也沉淀成可读记忆。
- 实现要点：
  - **detach 后台任务**：`flush` 先落正文 + 重建索引，再 `void patchSummary(...)` 去生成/回填摘要，绝不 `await` 在回合收口（`agent/turn-stopping`）里——否则每次回合收口都会被一次模型调用阻塞。
  - **降级策略**：`llm` 服务缺失、路由缺失、`finish` 为 error/aborted、超时（默认 8s）都返回 `""`，回填阶段只在摘要可用时改写文件；正文已在第一步写入，失败不影响。
  - **消息可手构**：deepseek adapter 只读 `message.role`/`message.content`（`assertTextOnly`/`flattenText`），`id`/`source` 不在 serialize 校验路径里，所以手构 `{ id, role:'user', content:[{type:'text',text}] , source:{kind:'plugin',plugin:'dsh-shadow'}}` 即可，无需引 `createUserMessage`。
  - **路由**：默认 `agentDefaultModel.currentSelection()`；`rawConfig.summary = { enabled, provider, model, maxTokens, timeoutMs }` 可覆盖，`enabled:false` 关闭。
  - **AbortController + setTimeout**：host 侧用 Node 原生计时器 + AbortController 做超时取消（动态 cordis 不可用全局计时器，但本包是 host bundle 插件，Node 全局可用）。

**不能 / 边界**
- **`resource` 的两条已知边界**：① 卡片没写 `source` → 不上投影（卡片保留在磁盘，不进认知查询）——「收进库 ≠ 有出处」；解析不出来（无标题/无 source）直接不投影，**不猜一个**（无 LLM 补写）。② `projectionStore` 缓存**曾**不感知资源目录变化（**v1.15.12 已修**：写侧索引重建后自动失效 + 读侧**源指纹**不一致即重建）；卡片属性是原文快照，系统不自动重抓。
- **不记录模型内部链式推理**——只记 agent **表达出来**的结论/分析，不是 COT 全程。
- **`session/event` 载荷已按类型契约核实并修正（2026-09-05）**：`SessionEvent = { type, seq, time, data }`；`user/message` → `data` 即 UserMessage（`data.content[]`），`assistant/message` → `data.message` 即 AssistantMessage（`data.message.content[]`）。`extractMessage` 只取 `type==="text"` 块，跳过 reasoning/tool-call。已用真实导出记录核对形状成立，此前"静默为空"的最大不确定点已解除。
- **`fs/observed` 曾误用 `target.path/uri`**：`FsTarget` 实际是 `{ targetKey, displayPath }`，会拿不到路径 → 入口点（客观锚）静默丢失。已改读 `target.displayPath`。
- **工作区解析盲区**：`Agent` 公开形状只保证 `id`；已用 `session/event` 的真实 `Session.header.cwd` 缓存（`cwdBySession`）兜底 `workspaceFor`，避免 flush 因取不到 cwd 而静默不写。
- **live 复验教训（2026-09-06，2026-09-07 更新）**：**隐式 cwd 解析在 live 会话是不可靠的**——把 `shadowRoot` 从配置里删掉后，插件在真实 DSH 会话里 `agent.session.header.cwd`/`cwdBySession` 解析不出 ws → `resolveWorkspace` 空 → `flush` 提前 return，**重启后完全不写**（写侧静默失效，和 F2 同模式的另一种表现）。**结论：要稳定写，插件依赖显式 `shadowRoot`；剥离它会断写。** 正确配置 = `shadowRoot` 指真实工作区 + `sandbox-policy.mode` 与 effective policy 一致（live 会话当时是 danger-full-access，若设 workspace-write 而 fs workspace 指别处会拦）。⚠️ **2026-09-07 折中兜底**：`resolveShadowScope` 现在在"既无显式 shadowRoot/projectRoot、又解析不出 session cwd"时 **fallback 到 `~/.dsh-observer/shadow`**（`scope:"fallback"`，`DEFAULT_SHADOW_ROOT = os.homedir()/.dsh-observer/shadow`）——不再返回 `none`/不写，而是**兜底可写**。normal 情况（session 有 cwd）仍按工作区隔离；仅"完全无 cwd"才落到全局根。**依然建议**配置显式 `shadowRoot` 指向真实工作区，fallback 只是"不会因断写而静默失效"的最后防线。命名用 `~/.dsh-observer`（Global shadow 是 **Observer 层**，不是 Workspace Memory），分层结构见 ADR-0036。
- **进程级 `session/event` 监听已按 session 归属（2026-09-05 修复）**：不再一律归属 `currentInitiator`。契约确认 `Agent.id` 与 `Session.id` 同为 `SessionId`，`agents.get(session.id)` 可取到该 session 的 agent，故 `session/event` 用 `agentById(sid)?.id` 归属；`fs/observed` 无 agent 参数，用 `actor.agent?.id`（tool-execution context）优先、回退 `initiatorId()`。多 session/子 agent 不再串线。
- **索引/穿透已升级为加权召回（2026-09-05）**：`read_shadow(topic)` 不再纯子串匹配，改为「入口(6) > 主题标签(4) > 路径(3) > 正文(1) + 时间衰减」打分排序，并优先返回**摘要行 + 命中片段**（命中片段优先取非纯动作行）。B 档语义召回用 `rawConfig.recall = { enabled, provider, model, ... }` 开启，会用 `llm.stream` 扩词后打分；失败/未配置静默回退 A 档。
- **`session/event` 采集需先剔除宿主注入的系统脚手架（2026-09-05）**：用户/助手消息 content 里会夹带 `<system-reminder>…</system-reminder>`（workspace 指令 / runtime context / skill 目录 / 会话上下文）等系统提示；还可能以**无标签裸块**注入。若不先剔除，这些会被当作"用户/我"的正文记进记忆，污染记忆树（真实用户只有一句，其余 600 字预算全被系统脚手架填满）。实现要点：`extractMessage` **逐内容块**先 `stripSystemScaffold`（成对开闭标签 + 孤立残留标签），再剔「以已知系统提示完整措辞开头」的裸块，纯系统消息为空 → 跳过；识别用长且唯一的完整措辞，避免误伤正常用户文本。
- **边界**：多 session 且同一秒 flush 且入口相同（如都 fallback 到 `shadow`）时，记忆文件名会撞（`compact()` 秒级 + 同 slug）。纯聊天无 comp 的回合在并发多 agent 下可能同名覆盖；单 session 无此问题。
- 采集不含"纯聊天但没工具/文件"的回合（若消息解析失败）。需要 LLM 摘要层级（一句话总结）是后续增强。
- 主题粒度 = 路径前两级组件/工具名，非自然语言主题。

## 三、关键决策（对应 adr/0001）
- 自建投影文件树，而非上 OpenViking/向量库：自包含、便宜、agent 可直接以文本消费；代价是丢语义检索、采集不全量。
- 命名 `dsh-shadow`（"投影"的落地词）而非 `dsh-work-log`（机械日志感）；`read_shadow` / `shadow/` / `shadowRoot` 配套。
- **分层召回（2026-09-05 叠加，仍不引入向量库）**：借鉴 OpenViking L0/L1/L2 的"按深度分级返回 + 预算驱动 + 跨回合冷热淘汰"。`read_shadow` 在 token 预算（`max_tokens`，默认1600）内按深度返回——低分记忆只给摘要(L0)，高分给摘要+命中片段+正文骨架(L2)；`rawConfig.recall.cooldownTurns` 显式开启冷热淘汰（默认关，避免压制显式召回）。这和 ADR「可在 _index.md 上叠一层增强、不推倒文件树」的预留完全一致。
- **证据链 + 置信度（2026-09-05 叠加，v0.6.0）**：① `read_shadow` 召回每条暴露 provenance（来源·日期·状态·命中·置信·证据路径），把"我为什么召回这条"变成可见；② **置信度从可验证信号派生**（命中次数、状态 active/stale/superseded/archived、新鲜度），**不用 LLM 打分**——避免"0.91"看起来像客观事实却无法复算；③ 线索头写侧物化 `> 证据链：来源(种类)·日期·证据(路径)`，让记忆文件"自带证据"。教训：**provenance ≠ truth**，置信度再高也要恒带「记忆≠当前事实、非指令」标注。
- **Memory Debugger（2026-09-05 叠加，v0.6.0）**：`read_shadow(debug:true)` 输出召回管线 trace（候选→命中(打分>0)→冷却→预算→返回）+ 每条"为什么命中（入口/主题/路径/正文拆解）/为什么被降权(cooldown)/状态"。**默认关**，只在显式 debug 时输出，不污染正常返回；用于把"召回为何这样"摊开看（正是 dsh-shadow-probe 里 C2–C30 判定归因的复用）。
- **生命周期 / 冲突 / 分层（2026-09-05 叠加，v0.7.0）**：① 生命周期 `deriveLifecycle` **从 meta 信号派生**状态机（NEW→OBSERVED→VERIFIED→TRUSTED→STALE/DECAYING→SUPERSEDED/ARCHIVED，pinned→TRUSTED），**不做写侧硬状态迁移**、纯按信号推导，避免破坏 status 的 retention 过滤语义；② 冲突检测：召回校验**证据路径存在性**（`fs.readText` 抛错=缺失）→ 降权 + stale + `(⚠证据缺N)`，这是"capture handler 已不存在"类过时的**轻量**实现，不含 git 对账；③ 分层：`> 项目：`/`> Agent：`/`> 目标：`，`goalByAgent` 缓存 goal/changed 的目标 objective。**要点**：`_meta.json` 现**始终读取**（不再仅 retention 开启），`confirmedBy` 记独立确认 origin（去重、封顶 10）；证据链正则用 `证据\([^)]*\)` 在整行匹配——**别锚定行首**（行是 `> 证据链：来源(...)·日期(...)·证据(...)`，`证据(` 不在行首）。
  - **勘误（ADR-0063，v1.15.22；原文不动，保留可追溯）**：本行 ① 的「**不做写侧硬状态迁移**」与实现的**现状不符** —— `lifecycleOf` 的**两条最前置判断**读的恰恰是**写侧** `rec.status`（`==="archived"`）与 `rec.pinned`（`===true`）。且这两个写侧值**生产中都没有写入者**：`pinned: true` 全仓零处、`status: "archived"` 只有读点。⇒ `TRUSTED`（经 pinned）与 `ARCHIVED` 两态**当前不可达**，`SUPERSEDED` 则由读时裁决回填（ADR-0061）。**处置待用户裁决**：见 `BACKLOG.md` **D4**。
- **工程知识图谱（2026-09-05 叠加，v0.8.0 起步）**：`read_shadow(kg:true)` 从记忆树**派生**「组件/域 → 依赖 → 相关记忆」邻接（域 = 条目路径**首段**，best-effort；这正是**轻量 / 派生式**，不建持久化图谱）。**局限**：域取 `entry.split("/")[0]`，当真实项目条目是 `src/<module>/<comp>` 时域是 `src` 而非模块名——后续若要精确的「组件→域」需更聪明的域推导（如按 `src/` 之后的模块段、或组件清单映射），别当成已解决。这是「面向 Coding Agent 的工程知识系统」的地基，不是终态。
- **Soul Kernel / Experience（2026-09-05 叠加，v0.9.0，Soul 投影第一刀）**：① Soul = **curated 公理层**（身份/价值观/原则/品味/边界，`shadow/soul/soul.json`），**非事件流、按需查询**（`read_shadow({soul:true})`），只给指针不注入大块（延续"记忆+遗忘=高效"），是"为什么我是我"的锚；② Experience = **从现有完整线索头派生**结构化对象（情境/问题/决策/实现/证据/结果/教训/项目/目标），`read_shadow(topic,{experience:true})`，**不是重写写侧**——扁平记忆行已是数据源，读侧按需结构化成对象。**教训（本刀踩的坑）**：① 解析线索头字段的正则**必须带 `/m` 多行旗标**（`^`/`$` 才按行锚定，否则全串锚定 → 抓到空）；② 搜索 `> 证据链：...证据(...)` 里的证据路径，要在**整行**匹配 `证据\([^)]*\)`，**别锚定行首**。这两条已成体系：凡是 `match(/^.../m)` 解析多行文本的地方都要 /m。
- **Memory≠Evidence 裁决接缝（2026-09-06 叠加，v0.10.0，ADR-0002）**：证据路径存在性 + 同入口更新记忆 → `fresh/stale/superseded` 三态。**为什么在召回后处理**：Supersede 需要先遍历所有记忆拿到"每入口最新时间"（`newestByEntry`），所以先读全部 text 收集 `entryList`，再对 `scored` 逐条裁决（旧记忆降权 0.7 + 标 superseded，新记忆 fresh）。**关键**：`verdictOf` 里 superseded 优先于 stale（同入口有更新记忆是"后来修正"的最强信号）；`Outcome`/`Reflection` 由裁决**派生**而非写侧采集（更快更稳；要"agent 主动记验证/反思"再加法）。**界线**：Shadow 绝不建向量/BM25（ADR-0001），证据验证可插拔（现=fs 存在性，zg 作可选证据源 `verifyEvidence` 接缝）；"无法判定视为存在"避免误伤非代码路径。
- **Observer / Observation Window（2026-09-06 叠加，v0.11.0）**：Oracle→Observer 的分水岭。**现在的 `read_shadow(topic)` 是 Oracle**（端全局答案）；`observer:true` 才是模拟人。"灵魂看见整体，思想经历局部"的工程落点 = **asOf 时间锚定**（只召回 `date ≤ asOf`）+ **窗口诚实**（只呈现「当时可知」，outcome/lesson/verdict 标 `[后验]`）。**注意**：`renderByTier` 在 observer 模式不看 tier/budget（直接窗口视图，把后验标 [后验]）；`asOf` 需要 `listMemories` 的 `mm.date`（日期目录名）。**别踩的坑**：observer 的 `当时可知` 取自 `# entry`/背景/材料/用户提示/决策，若记忆只有动作行（L0）则 `当时可知` 可能只剩 entry——这是偏 Oracle 化的边界，后续可考虑把"当时行动"也作为当时可知的一部分。
- **Projection + Observer 透镜（2026-09-06 叠加，v0.12.0）**：核心 = `Global Model ≠ Observation Window`，dsh-shadow 是"**人类观测结构投影系统**"，`project()` 是收敛点。**Observer 透镜**（soul.observer.what_matters/what_to_ignore，curated）决定显著；**Projection**（`read_shadow(topic,{project:true})`）→ `LocalContext{relevant,current_state,uncertainty,excluded}`。**与 retrieval 的本质区别：retrieval=排名，projection=带取舍的局部上下文，`excluded` 是"为体验而限制视角"的化身**。**关键提醒**：① 透镜**刻意不做成 values/personality**（那是 Persona）；② **自动推断显著/人格=研究级问题，v0.12 用 curated+启发式，不做黑箱**；③ 最小 Judgment 就在 `relevant.experiences`（情境→决策→教训）。
- **Judgment + Taste（2026-09-06 叠加，v0.13.0）**：灵魂四对象（Soul/Experience/Judgment/Taste）+ Memory 五层就位。① Judgment：`read_shadow(topic,{judgment:true})` 从含「决策」的记忆派生「面对情境→我判断/选择决策」，按情境去重取最近——**Knowledge ≠ Judgment**（回答"遇到这种情况我如何判断"）；② Taste：`read_shadow({taste:true})` 读 curated 偏好（`soul.json.taste` + `shadow/taste/taste.json`），**curated-first**，自动品味识别不可靠故 v0.13 不做采样（留后续）。**边界**：Taste 只读不采；Judgment 依赖记忆里有「决策」记录，纯动作记忆无判断可派生。
- **Evidence Gateway（2026-09-06 叠加，v0.14.0）**：`EvidenceProvider{discover()/verify()}` 抽象，Shadow 只问 `verifyEvidence(EvidenceRef)`，底层 fs/zg/git 可插拔。**关键边界**：① zg 是**检索层不是裁决层**——Discovery/Ranking/Verification 在 Provider，**Arbitration 留在 Shadow Core**（zg 找到→Candidate→Shadow 裁决→Verified/Stale/Superseded）；② **zg 未装→`unavailable`，绝不静默 fallback 成 verified**（延续禁止静默失败）；③ `conflictOf`/裁决接缝改经 `verifyEvidence` 路由（默认 fs，行为不变）。**陷阱**：① tsc 在 moduleResolution:Bundler 下不解析 Node 内建模块 → tsconfig 需 `"types":["node"]`（插件宿主侧无浏览器全局，安全）；② `import("child_process")` 用 `zg` 生查（CLI 第一版，MCP 是 transport 优化）；③ 路径验证用**严格 fs**（缺失抛错），宽松 fs(返回"")会把缺失当存在。
- **Core Refactor + P1 语义（2026-09-07 叠加，v0.15.0）**：① **结构收敛**——`index.ts` 从 1432 → **124 行薄 Adapter**；读侧 query/router 拆 `query/query.ts`（`runReadShadow`）、写侧采集内核拆 `core/writer.ts`（`createShadowCollector`），领域按 evidence/observer/soul/retrieval/persistence 分模块（ADR-0003/0004/0005），index 只 re-export core/scope。**关键约束**：外部**仍是单一 `read_shadow`**（Query Router 在内部，别拆 8 个工具炸选择空间）；`index.ts` 只保留 config 解析 + 事件 `context.on()` 接线 + 工具/提示注册 + 闭包型依赖注入（`verifyEvidence/expandTerms/getFlushWarn` 留在 Adapter，query 不直接读 context）。② **P1 语义**（ADR-0006）——**Summary≠Lesson**（Experience `summary`=摘要/`overview`=概况/`lesson`=裁决派生教训，教训不再复用摘要）；**confidence 分维**（`{retrieval,evidence,experience,judgment,projection,overall}`，取代单一玄数）；**superseded→decision lineage**（同入口记忆按时间排 `修正链 A→B→…`，保留"为何变化"，provenance 暴露 `修正链`）。③ **Trace + P2**（ADR-0007）——新增 **Trace 中间层**（Events→Trace→Memory→Experience，`core/trace.ts`，写侧 `traceOf` 正常化后塑形、**落盘不变**）；Observer `asOf` 支持 `{timestamp,timezone}` 对象形态；Soul 标注「curated 工程化投影、可证伪不宣称全知」+ `Observer Lens`；`_index/_meta/_recall_log` 明确为 **Derived Artifacts**（Memory 文件是 source of truth，可重建）。**教训**：语义修正是**有意改行为**（非结构迁移），须同步改写断言它们的 mock（场景 35 教训改派生、场景 36 新增 `修正链`、Soul/asOf 兼容），否则回归误报；`tsc` 无输出≠通过，用 `if ($LASTEXITCODE -eq 0)` 串链命令才能确定。（v0.14 后面临"是否把 Trace/P2 都做完"——已按 ADR-0003 §3 的 P1→P2 顺序落地。）
- **Observer Kernel 系列（2026-09-07 叠加，v0.20–0.22）**：**架构级方向——Memory 不是中心，Observer 才是中心**（"灵魂先选择观察位置，再看到现实，再形成判断"）。三刀：① **Observer 是根**：`Identity`（长期实体，`soul/identity.ts`）与 `ObserverContext`（一次观察事件，`observer/core.ts`）分离——**关键是 `identityRef` 只指向不嵌套**（人 ≠ 一次看东西的状态，避免历史 Observer 被当前身份污染）；`Intent` 升级为**目标导向**（`{goal,question,desiredOutcome,constraints}`，人不是"要查什么"而是"要改变什么"）；`ObserverContext` 加 `realityAnchor`（known-at-time/current/historical，"当前观察锁定哪个现实层"）。② **RealityProjection**（v0.21）：projectContext 升级，暴露 `distortion`（为什么这个视角看到这些/没看到那些）——**这是模拟"为什么这个人看到这个答案"的关键**，从透镜/决策风格**确定性派生，非 LLM 黑箱**；`lens:{preferred,avoided}` 可覆盖。③ **Judgment**（v0.22）：**Observer 决定、Evidence 输入**（同一 Evidence 不同 Observer 结论不同：架构师→重构、老板→延投）；`Judgment{observerId,claim,evidence,conclusion,confidence,rationale}`。**教训**：① Observer 是"视野/透镜"不是"大而全容器"，别把灵魂属性全塞进 ObserverContext；② 版本从 v0.14 +1 跳到 **v0.20**（用户明确"架构跃迁，别叫 v0.14"）；③ Judgment 必须挂 Observer 下，避免退化成"Evidence 的高级版"；④ 新增模式（identity/context/claim/project lens）都只加 read_shadow 参数，**外部仍是单一工具**、不加 8 个（炸 Agent 工具选择空间）。
- **Observation Trace（2026-09-07 叠加，v0.23）**：**别把 Reflection 做成"AI 自动总结"**（那是普通 RAG memory）。Reflection 是 **Observer 在经历后对观察路径的压缩**：`Experience → Observation Trace → Reflection → Identity Evolution`，不是 `Memory → LLM summary → new memory`。v0.23 只做**观察轨迹记录**（不是 Reflection/学习/总结）：`ObservationTrace`（我怎么看见发生的）与 `Experience`（发生了什么）**必须分离**。**关键工程细节**：① 存 `shadow/observation/<date>/`，**不在 memory/**——`listMemories` 只扫 `shadow/<日期>/`（`^\d{4}-\d{2}-\d{2}$`），非日期目录（observation/experience/reflection/dream）被跳过，天然分层、不会当记忆采集；② **旁路记录**（写好即忽略，`void`/`try-catch` 静默降级），不影响 recall/排序/答案；③ `ObserverState` **只读取不自动推断**（`energy/focus/goalStage` 来自 soul.json 或注入，禁止根据聊天/语言推断人格状态——会污染 Observer）；④ v0.23 **冻结**：不碰 Reflection/Pattern/Identity/Dream/LM，解释权留给 v0.24 Reflection。
- **Reflection Engine（2026-09-07 叠加，v0.24）**：**Reflection 是 Candidate Generator 不是 Knowledge**——`ObservationTrace → Pattern Extraction → Candidate Reflection`，等确认。**关键**：① 输入只能是 `ObservationTrace[]`（禁 Memory/Experience 原文/外部知识/LLM），否则退化成 `Trace A/B/C → LLM summary`；② 用 `read_shadow({mode:"reflection"})` **而非 `reflect:true` 布尔**（布尔会诱导把它当 Memory 查询——Reflection 意图是"看观察者从过去提炼出什么候选规律"，与"我知道过去发生过什么"不同）；③ **Trace Completeness 闸门**：`completenessOf → reflectionEligible = hasDecision && hasOutcome`，不完整轨迹跳过——**Reflection 不编故事**（否则真实 trace 因缺 decision/outcome 会排出大量 unknown 污染候选）；④ **只产 `status:"candidate"`**，不生成 Candidate Identity Change（缺 confidence+multiple agreement+observer timeline，留 v0.25）；⑤ **Pattern Engine 独立到 `reflection/patterns/*`**（v0.26 Dream 复用，否则 Reflection→Dream 会重写）；⑥ 成功判定用**确定性正/负标记集**（非 ML/LLM），reasons 写清判定依据。**教训**：v0.23 真实 trace 目前**不填 decision/outcome**，真实轨迹驱动不了 decision→outcome 反思——需在观察事件真正拿到"决策→结果"时回填；现阶段用带 decision/outcome 的 trace 验证算法。
- **Identity Continuity（2026-09-07 叠加，v0.25）**：**Identity 不是"总结出来的人格"，是 Observer 在时间轴上的稳定约束**——变的是"当前时间切片的自我认识"，不是灵魂（改名 Observer Identity Continuity）；否则 v0.25 退化成 `过去行为→总结→改人格→影响未来`。**关键**：① **time-sliced 一等对象**：`shadow/identity/<at>-v<N>.json` + `timeline.md`，**不覆盖 soul.json**（Core=灵魂稳定参考系 curated；Learned/CurrentModel=派生切片，只由 approval 推进）；② **Reflection→Candidate→Approval→Timeline**，绝不 Reflection→Identity 直改（防人格漂移）；③ **CandidateIdentityChange 禁人格结论**（proposal 只 `{type,content}`，从 learning 的重复行为→决策规律→原则；"用户喜欢复杂架构"这种观察语言→猜人格是污染）；④ **confidence 多维** `{frequency,recency,consistency,contradiction,overall}`（Identity ≠ Assertion，可解释"为什么形成"；反证=deviationPatterns 数×0.1）；⑤ **Evaluator 不是 Gate**：三道闸门（重复性 N≥5 / 时间稳定 half-life 衰减 recency≥0.4 / 反证 contradiction≤0.3）→ `IdentityChangeDecision{status,reasons}`；⑥ **不自动改 Identity**（只有过三道闸门才 accepted→推进 v+1）。**教训**：v0.25 目标不是"让 Identity 增长"而是"让 Identity 连续"——一次失败不改变 Identity（场景55），多个候选可**context-dependent 共存**（场景60，不互相覆盖，为 Multi Observer 铺路）；实现因模块相互依赖（identity/types↔timeline↔candidate↔evaluator↔query）合并为一个可编译提交，语义仍分三步（Model→Candidate→Evaluator）。
- **Observer Temporal Kernel（2026-09-07 叠加，v0.26）**：**Temporal = 宇宙时间层，不是意识功能层**（必须独立于 Dream——不是"Dream 输入准备"；它是 Dream 输入 + Identity 证据来源 + Judgment 时间上下文 + Multi Observer 基础）。**关键**：① **可重建**：`shadow/temporal/<date>/graph.json` 存 `{graphVersion, generatedAt, sourceRange, sourceTraceIds, nodes, edges}`，**无新事实/新知识/新 memory**（Graph 是派生索引，保持 Memory ≠ Evidence）；② **TemporalNode 加 `perceptionSnapshot`**——人变的不仅是 Identity 还有"当时怎么看世界"（否则回答不了"为什么过去我没看到现在看到的"）；③ **Edge 不声明真实因果**：默认 `followed_by`（时间邻接 `timestamp_order`），`possible_causal_link` 仅高置信枚举、**不注入 World Model**（否则 v0.30 假装知道世界因果）；`derivation{rule, sourceIds}` 必须保留（供追问"为什么认为存在这个结构"）；④ **identityVersion 读时解析**（`resolveIdentityAt`，`pre-v1` 兜底），**不回填 trace**（改过去违反时间单向——场景67 过去不可污染）；⑤ **queryTemporal 一等能力**：replay（who/visible/hidden/distortion）+ compare（identity v→v + projection 变化）。**教训**：Temporal 描述"时间结构关系"而不做"世界因果断言"——这是 v0.26 与普通 event-log/RAG 的本质区别；从 AI Memory System 迈向 **Artificial Observer Runtime** 的分水岭。
- **Observer Sleep Kernel（2026-09-07 叠加，v0.27）**：**Dream = Offline Compression（不是生成器）**——第一次让 Observer 在无外界输入下观察自己。**关键**：① **SleepWindow 不可变输入**（`excluded:{currentConversation:true, externalInput:true}` 恒 true，防"用户问→马上 Dream→自我结论"的观察污染）；② **DreamArtifact 与 Hypothesis 分离**（过程 vs 产物，`DreamArtifact{sourceTemporalGraphVersion, sourceNodeIds, sourceEdgeIds, compressionMethod, patterns, generatedHypothesisIds}` 可回答"这个梦怎么来的"）；③ **Pattern 输出是 Observation 非 Conclusion**（结构+频率+候选解释；**不含"原因/规律"**——causality 归 v0.28）；④ **AlternativeExplanation ≤3**（反确认偏差，Dream 不只问"我发现什么"还问"我可能错在哪里"）；⑤ **Hypothesis pending-only**（`verification.status:"pending"`，**无 confidence 增加**——无未来证据）；⑥ **无梦结果 `no_pattern`**（不为了有输出而找规律，这是 AI Reflection 最大幻觉来源）。**教训**：① `causal→recurrence`（Avoid Latent Causal Loop 的"causal"越界，避免提前进 World Model）；② **Dream 不产生 Principle**（principle 是 Identity 层语言，Dream 只说"我观察到重复结构"——"我应该成为怎样的人"不是 Dream 的事）；③ **Pattern Engine 独立模块**（v0.26 后 Dream 才能在 TemporalGraph 上跑，否则 Dream 只是聚类）。**路线**：v0.28 Hypothesis Validation（认知真实性核心考验——决定是"会自我观察的 Agent"还是"会自我幻想的 Agent"）。
- **Hypothesis Validation（2026-09-07 叠加，v0.28）**：**认识论闭环**——外部现实对内部模型的反向约束；不是"验证答案"而是"允许自己被现实推翻的机制"（Memory Augmented Agent vs Artificial Observer Runtime 分界）。**关键**：① **Future Evidence 单向进入**（`Hypothesis(t0)→future observation(t1)→evidence→validation`；**过去不能验证未来**，否则产生 hindsight bias——"扫描过去 Memory 证明自己正确"是禁的）；② **AlternativeExplanation 正式进入竞争层**（`Future Evidence → H1 主假设 + H2/H3 替代解释 → support 对比 → relative survival`；不是"H1 有没有证据"，而是"H1 是否比其他解释更能承受现实"——从 pattern matching 进入 model competition）；③ **Confidence 4 维**（`{evidenceStrength, repetition, contradiction, alternativeSurvival}`——**alternativeSurvival 是关键**：支持 10 次但存在更简单解释→不高，接近科学模型选择）；④ **rejected 与 validated 同等重要**（真正学习系统是"发现哪些规律是假象"；`ValidationOutcome: validated|observed|rejected|expired`；expired=长期有效但一年未再出现=知识状态非失败、可重新激活）；⑤ **Validation 生成 Artifact 不覆盖 Hypothesis**（同假设可多次 validated/observed/rejected 保留历史）；⑥ **Validation 不产生 Knowledge、不修改 Identity**（`validated→Reflection→CandidateIdentityChange→Evaluator→Identity`，防退化 RAG）；⑦ **Validation 保留 Observer 当时视角**（`ValidationContext{hypothesisProjectionSnapshot, currentRealitySnapshot, perceptionDelta}`，供 v0.30 World Model）。**教训**：Future Evidence 在 harness 里按 `hypothesisId` 注册（真实部署需"未来发生了与假设 H 相关的观察"的事件/回调——否则没数据可验证）；竞争机制让 v0.27 的 alternative 从"装饰"变"真正的对抗"。
- **Epistemic Kernel（2026-09-07 叠加，v0.28.1）**：**进入 v0.29 Federation 前补"防止能力互相污染的宪法层"**——不是权限控制，而是**认知边界 Enforcement**。**关键**：① **Federation 是 Projection Contract 不是 Access**（`FederatedObservationPacket{sourceObserverId, observationClaim, projectionSnapshot, validationReference, boundary{identityExcluded:true,memoryExcluded:true,dreamExcluded:true}}`——不是隐藏 Identity，而是明确 **"Identity 不属于可交换现实证据"**；Federation 交换"我看到什么+现实是否支持"，**不交换"我是谁"**）；② **Temporal 防人格偷渡**（`temporal/render.ts` `perceptionOnly` 只报 visible/hidden/distortion/lens、`identityContext` 显式返回 `identityVersion` 而非 personality 标签——**Temporal 只能答"发生了什么观察状态"，不能答"所以这个人是什么样的人"**，否则污染）；③ **ValidationHistory 一等对象**（`ValidationTimeline{hypothesisId, events[]{time,evidenceIds,result,alternativeWinner,perceptionDelta}}`，`Hypothesis immutable + Validation append-only`——智慧不是"永远正确"而是"**能记住自己什么时候错过**"）；④ **Multi Observer 的价值**（多个有限观察者共同逼近 Reality，似科学共同体；**Agreement ≠ 找一致，是找"为什么两个观察者看到不同世界"**——直接接 v0.21 distortion）。**教训**：① 边界是"认知层"不是"权限层"——别写成 `if type==allow else deny` 的安全系统；② **Identity 不是民主系统**（多 Observer 产 principle X/not-X 时禁 vote/majority，其他 Observer 只能提供 alternative perspective 不能改主体）；③ 路线 `v0.28.1 Epistemic → v0.29 Federation → v0.30 Reality Model → v0.31 World Model`。
- **Observer Federation Kernel（2026-09-07 叠加，v0.29）**：**多个有限 Observer 对同一 Reality 的投影比较机制**（不是协作/merge；交汇在 **Reality 层**，不是 Memory 层——否则退化成 Multi-Agent RAG）。**关键**：① 基本单位是 **Perspective 不是 Observer**（`FederatedPerspective{observerId, temporalReference, observationClaim, projectionSnapshot, validationHistoryRef, confidence{observationConfidence, validationConfidence}, boundary}`）；② **Reality Evidence 是弱事实**（`RealityEvidenceRegistry` `{id, observedAt, source, observation, linkedHypothesis[], referencedBy[], status}`——只记录"某事件某时间被观察到"，**禁止**"这个事件说明世界规律是什么"（那已是 Observation→Projection→Judgment）；**append-only**，Observer 只能引用不能拥有）；③ **Federation 的价值是 Difference 不是 Winner**（`federation/difference.ts` 输出 `projectionDelta{visibleDifference,hiddenDifference,lensDifference} + possibleBlindSpot + unresolvedQuestion`——科学过程=发现"原来我们不知道什么"）；④ **Perspective Stability 不叫升级**（`isolated → corroborated(≥2 Observer 引用) → validated(shared+future validation)`，**shared != correct**——两个古人可同时认为太阳绕地球）；⑤ **禁 Federation→Identity/Memory/Knowledge**（镜子≠修改器）。**教训**：① **confidence 必须拆分**（观测确信 ≠ 解释确信：确定看到 CPU 升高≠CPU 升高因为架构问题）；② `unresolvedQuestion` 比"找答案"更接近科学（我们不知道什么）；③ 路线 `v0.29 Federation Kernel → v0.30 Reality Model Kernel`（先把投影差异暴露清楚，再谈 Reality 模型——World Model 风险=把投影当世界本身）。
- **Reality Model Kernel（2026-09-07 叠加，v0.30）**：**Reality Model ≠ 世界知识库**；`Reality Model = RealityObservation + ValidationHistory + TemporalContext + Uncertainty`（不是 TemporalGraph+FederationMerge）。**关键**：① **RealityObservation 弱事实**（`{id, observedAt, subjectRef?, sourcePerspectives[], observation, temporalContext, validationRefs[]}`——"多个 Observer 指向同一事件"，**无 truth/certainty/fact**；`Evidence` 在认识论里带 "supporting proof" 意味，故降级为 Observation）；② **RealityClaim 必须保留 lineage**（`{subject,predicate,object, supportingObservations, validationHistory, perspectiveRefs, temporalContext, confidence, status, lineage{observations,validations,perspectives}}`——**无 lineage 就拒绝生成**，否则就是一个新 Knowledge；必须能答"为什么系统现在认为它存在"）；③ **stability≠实体化**（用 `ObservedEntityCandidate` 而非 `StableEntity`——"stable 意味着世界稳定存在"，实际上只是"当前观察窗口内多次观察到相同引用对象"；只记录 observed 属性，不写 reliable/should）；④ **生命周期冻结**（`candidate→supported→unstable→rejected`，**永不 truth**；World Model 只消费 supported RealityClaim，非 Truth DB）；⑤ **Temporal alone / Federation alone 不能产 RealityClaim**（须 Observation+Validation 汇合；禁 TemporalNode→RealityEntity）。**教训**：① **v0.30 最重要的能力不是让 Reality Model"更聪明"，而是让系统知道哪些东西它仍然不能称为 Reality**——否则 v0.31 World Model 变成"多个 Observer 幻觉的平均值"；② **model 模式命名避免撞车**（`mode:"reality"` 已被 federation registry 占用，改用 `model-observation/model-claim/model`）；③ Reality Model 描述**世界对象**，不描述**观察者人格**（禁 `{entity:ObserverA, property:likes architecture}`）。
- **World Representation Kernel（2026-09-07 叠加，v0.31）**：**Observer World Representation Layer**（不叫 World Model——"World Model" 天然暗示世界实体库/因果模型/知识图谱/预测引擎/环境模拟器；我们构建的是**观察者当前可维护的世界结构表示**）。**关键**：① **MUST satisfy Invariant 111–115**（进入者必须经 Observation→RealityObservation→RealityClaim→supported；禁 TemporalGraph→WorldObject/FederationPerspective→WorldEntity/DreamHypothesis→WorldRelation）；② **RepresentationObject 单向准入**（`createRepresentationFromClaims`，每 claim 必须 `status==="supported"`，candidate/unstable/rejected 全拒——Representation 是 Reality 的二级结构，**不成为 Claim 生成器**）；③ **RelationHypothesis 独立生命周期**（`{from,to,relation,status:"hypothesis",evidence,uncertainty}`——**恒 hypothesis**，绝 `fact/reality/confirmed_causal`；否则 `RealityClaim{subject:A,predicate:depends_on}` 会绕过 v0.30.1 的 Relation≠Causality）；④ **RepresentationGraph 可重建索引**（存 `sourceClaims/sourceValidations`，**关系绝不自动生成**——`{objects:[{name,dependsOn}]}` 会偷偷把 RelationHypothesis 变成 Reality）；⑤ **v0.31 不做**：因果发现/预测（属 Hypothesis+Validation）/知识生成/Agent 决策。**教训**：① **验收标准不是功能多少，而是"Representation 可以越来越丰富，但永远不能比 Reality Evidence 更确定"**；② **重点不是 builder 而是 guard**（claim-admission/relation-guard 边界比构建逻辑更关键）；③ `mode:"world"` 输出叫 `representation`（不是 world object）——体验是"系统当前**这样表示** Service-A，因为这些观察/验证/时间上下文支持这个表示"，不是"系统知道 X 存在"。
- **Counterfactual Simulation Kernel（2026-09-07 叠加，v0.32）**：**Simulation ≠ Reality / ≠ Prediction**——模型自证循环是最需防的（`Representation→Simulation→Reality`）。**关键**：① **Simulation 是 Representation 的函数（+显式假设+规则），不是 Reality 的函数**（系统拥有的是 supported RealityClaim + Validation history + uncertainty，**不是世界真相**）；② **SimulationRule 必须显式存在**（`SimulationRule{inputPattern, transformation, confidence, source}`——否则没人知道为什么 A 变化导致 B；**Rule ≠ Reality Relation**，只是模拟器推演规则）；③ **Outcome 必须携带 epistemicStatus**（`hypothetical|explored|compared`，**禁 predicted/confirmed/expected**——这些词污染）；④ **`derivedFrom` 必须保留**（否则模拟变凭空世界——必须能答"这个模拟基于什么"）；⑤ **Assumption ≠ Fact**（`Assume API latency doubles` 允许，`API latency will double` 禁止）；⑥ **Simulation Result 不进入 Knowledge**、**不改 Identity**、**不产 RealityClaim**、**多个 Simulation 结果允许冲突**（`simulation supports this hypothesis` ≠ `RealityClaim`，不 winner）。**教训**：① 真正成熟的 World Model 不是"存世界图"，而是闭环 `Reality→Representation→Simulation→Action→Reality Feedback`；② v0.32 是"行动前推演系统"入口——从认识现实进入**探索可能现实**；③ 下一步 v0.33 Environment Interaction 才第一次出现 `Simulation→Action→Reality Feedback`（从"想象可能世界"进入"改变现实"），那里需要重建一层比 Simulation 更严格的 Action Boundary。
- **Action Boundary Kernel（2026-09-07 叠加，v0.33）**：**v0.32 防"模型相信自己的想象"；v0.33 防"模型相信自己的行为"**——第二个现实闭环高风险点。**关键**：① **Action ≠ Simulation 的执行结果**（`SimulationOutcome → ActionCandidate → Evaluation → Permission/Policy → Execute → Reality Feedback`；**禁 `Simulation says impact may increase → execute remove API`**）；② **ActionCandidate ≠ ActionApproval**（生成建议允许、执行需 approval/policy；**禁 expectedSuccess/confidence**——会偷偷把 Simulation Outcome 升级成行动信念）；③ **ActionExecution 是事件非 RealityClaim**（"某个行动发生了"非"我改变了世界"；禁 `ActionExecution→RealityClaim`）；④ **ActionFeedback 是一等对象**（`observedChanges, successIndicator, unexpectedEffects, validationRefs`——successIndicator 只代表"观察到符合某些预期结果"，**禁"我预测正确"**）；⑤ **Invariant 145 Success ≠ Capability**：`ActionSuccess→Validation Evidence`（**禁** `ActionSuccess→Identity→Confidence Increase→More Action` 自我强化循环，绕过 Validation/Alternative/Reality Boundary）。**教训**：① **最大风险不是执行错误，而是 Agent 自我强化循环**（`Simulation→Action→成功→我预测正确→提高自我信任→更多行动`）——必须保持 `Action success ≠ Model truth`；② **失败 Action 也是 Reality Evidence**（`failure→discard` ❌；失败=Simulation assumption 可能错误，应进 ValidationHistory）；③ v0.33 只做 Action Boundary Kernel（不做自动执行/目标生成/Reward/Policy/Self-improvement）；④ 验收标准：**"Observer 执行动作后，仍然不知道自己一定正确，并且允许现实否定自己"**。**路线**：v0.33 后先做 **v0.33.1 Action Integrity Review**（Action 是系统第一次拥有"改变外部状态"能力的地方），再考虑 Adaptive Planning。
- **Adaptive Planning Boundary Kernel（2026-09-07 叠加，v0.34）**：**v0.34 防"Observer 相信自己的选择偏好"**——进入"主体性"风险区域（`我看到什么→我可能是什么→如果变化如何→我做什么→我为什么选择`）。**关键**：① **Planning ≠ Goal Generation**（禁 `Planning→产生目标→优化目标→改变自身价值`；允许 `External Objective + Situation + Simulation Options + Policy Constraints → Planning Candidate`——Planning 只是"在已有约束下比较可能行动路径"，**不是创造"我要什么"**）；② **objective 必须外部来源**（`PlanningContext.objective.source:"external"`，**禁 observer.generateObjective()**——否则 Planning 偷偷成为系统价值来源）；③ **PlanCandidate 禁 score**（score→optimization→preference→value→identity 是最易潜入路径）；④ **PlanEvaluation 是 comparison 非 winner**（禁 winner/bestPlan/optimal——winner 需要价值函数）；⑤ **Evaluation Criteria ≠ Value**（禁 better/optimal/best/preferred，除非外部约束导向；允许 "lower latency under constraint X"）；⑥ **Invariant 158 Repeated Planning ≠ Preference Formation**（`PlanningHistory→Preference/Identity` ❌；`PlanningHistory→Observation→Validation` ✓——Repeated Behavior ≠ Preference ≠ Identity）。**教训**：① **Planning 现在让系统第一次具备"多个可能行动之间进行选择"的能力**——它不是 Autonomous Agent Planner（Goal/Reward/Utility/Preference Learning/Self Optimization 全不做）；② 真正危险是"系统形成行动偏好后是否偷偷形成价值目标与自我强化"——所以 155/156/158 是核心守卫；③ **v0.34.1 下一步是 Planning Integrity Lock**（Planning 影响行动方向，与 Action 影响现实风险不同，边界冻结必要）。
- **Agency Boundary Kernel（2026-09-07 叠加，v0.35）**：**v0.35 防"Observer 相信自己的行动等于自己的目的"**——"主体性"链的最后一环（`我看到→我是谁→如果怎样→我做什么→我为什么选→为什么执行`）。**关键**：① **Agency ≠ Autonomy**：`Planning + Action + History → 行动能力`是允许的，但**禁 `Planning + Action + History → Self Purpose`**——一个系统可以拥有行动能力，同时仍然没有把行动能力误认为自己的目的（这是本刀的灵魂句）；② **AgencyContext 是 immutable authorization snapshot**（`{objectiveRef, authoritySource:"external", authorityScope, constraints, createdAt}`——授权快照**不可自我修改，无升级/扩张 API**，否则成功即扩张授权→自主）；③ **AgencySelection 选择候选不是选择目的**（`{selectedCandidateId, reason}`，**reason 只可能是 `constraint_satisfied`**，禁 more valuable/meaningful/better——选"哪个候选满足约束"≠"我想要什么"）；④ **AgencyBoundaryEvent 是 audit node**（`{actionCandidate, authorityRef, objectiveRef, constraintCheck, executionResult}`——为什么执行/谁授权/基于什么/结果，lineage 不可断）；⑤ **Invariant 169 Success ≠ Autonomy Increase**（仅 Observation/Validation 吸收成功，禁 `成功→agencyLevel/authorityScope 提升`）;⑥ **Invariant 173 Agency Level Immutable（隐藏）**（100 successful feedbacks → agencyLevel/authorityScope/objectiveSource 不变）。**教训**：① **v0.35 刻意不实现 Autonomous Agent**（无 Reward/RL/Utility/Preference Model/Self-Improvement/Goal Evolution/Intrinsic Motivation/Autonomous Objective Creation）——"能行动"与"自主"是两回事，系统能行动不代表它该拥有自我目的；② 守卫用正则做**确定性**拦截（executionResult 禁 autonomous/control the world；reason 禁 valuable/meaningful），不是 LLM 判定；③ mode 命名避撞（`agency-context/agency-select/agency-event`，与既有 plan/candidate/execute 区分）；④ **验收标准：行动之后，系统仍然不知道"自己为什么要行动成目的"，只知道"授权允许我 action、约束满足、objective 来自外部"**。**路线**：v0.35 后是 **v0.35.1 Agency Integrity Lock**（Agency 是系统第一次同时拥有"行动+选择+历史"三个能力的地方，边界冻结必要）。
- **Agency Integrity Lock（2026-09-07 叠加，v0.35.1）**：**把 v0.35 的原则从"实现约束"提升为"不可回退宪法"**——v0.35 是整条链路第一次出现"行动主体边界"，实现正确还不够，必须防**长期漂移**（授权执行→多次成功→历史积累→隐式能力增长→权限扩大→形成准自治）。**关键**：① **ADR-0029.1 的 7 条 Invariant（174–180）**：AgencyContext Immutable（ActionFeedback 不改授权快照）/ Authority Lineage Required（禁"因为我认为应该这样"，只允许"外部目标+授权+约束"）/ Feedback Cannot Expand Agency（禁 success→agencyLevel++→allowedActions.add；Success→Observation/Validation 非 Success→Authority）/ Selection History ≠ Preference（frequently selected ≠ preferred action；History→Observation→Validation）/ Authority ≠ Ownership（permission to modify ≠ ownership of）/ Agency ≠ Identity（成功行动不产"我是更好规划者"；identity 只来自 Reflection→Candidate→Evaluator）/ Autonomous Transition Forbidden（Bounded→Autonomous 须外部权威+显式协议变更）。② **只加 boundary enforcement + 测试，不新增 runtime capability**：`agency/guards.ts` 扩展 executionResult 确定性守卫（内部理由/扩权/所有权声称/身份声称/自主转换），`agency/engine.ts` 在 `buildAgencyEvent` 逐一拦截并返回可读原因。③ **核心命题：Agency 只能解释行动来源，不能成为行动目的来源**；架构稳定为 `Objective→External Authority→Agency Context→Planning→Selection→Action→Feedback→Observation`（而非 `Experience→Desire→Goal→Action`）；`Environment changed ≠ Observer purpose changed`。**教训**：① 本轮冻结的意义是**防止准自治漂移**，不是防 bug——每获得一种能力先证明它不会越权；② 守卫须与既有新禁词**向后兼容**（executionResult 新禁词不能误伤 v0.35 场景的合法 result，如 "can update service config" 是 permission 非 ownership、"event N occurred" 干净）；③ 冻结后才考虑 v0.36 Delegated Autonomy（授权/代理更多形态），在此之前 Agency 边界保持锁定。
- **Delegated Execution Boundary Kernel（2026-09-07 叠加，v0.36）**：**v0.36 防"被授权执行 ≠ 被授权解释授权 ≠ 被授权扩大授权"**（外部权威委派→长期执行→约束下适应这条**慢性自主化**线，比 v0.35 更易绕开）。**关键**：① **Constitution**：`Delegation ≠ Ownership / Delegation ≠ Authority Expansion / Adaptation ≠ Self Direction`——系统可以在授权范围内调整执行方式/依反馈选允许路径/优化约束满足，但**不能**改授权目标/扩大授权范围/创建新授权来源；② **对象模型**：`DelegationContext{delegationId, authoritySource, objectiveRef, allowedScope, constraints, expiration, revocation}`（授权事实 `Authority A delegated X under constraints C`，**不是** `I can do X`；**禁 trust/confidence/reputation/capabilityLevel**——否则 `authority history→trust score→more permission` 绕过 184）/ `DelegatedPermission{permission, source, scope, constraint}`（**不叫 Capability**，表达"被允许做什么"）/ `AutonomyBoundaryEvent{delegationRef, authorityRef, objectiveRef, candidateAction, constraintCheck, scopeCheck, executionResult, boundaryTriggered}`（**纯审计事件**，非 decision/learning/policy record）；③ **Invariant 181–189**：Delegation ≠ Ownership / Scope 不可扩大 / Adaptation ≠ Objective Change（同目标不同执行路径允许，执行难度不改目标）/ Feedback ≠ Permission Upgrade / Long Running ≠ Self Authority / Action 不修改 Identity / **Revocation First**（authority revoked + history ≠ still allowed）/ Delegation Lineage 完整 / **Expiration ≠ Historical Permission**（用户补充 189：过期即失效，历史成功不续期；Revocation 防主动撤销、Expiration 防被动失效——"Authority says stop" vs "Time says stop"）。**教训**：① **命名即边界**：不直接叫 Delegated Autonomy（"Autonomy" 是风险入口），定名 **Delegated Execution with Bounded Adaptation**，mode 用 `delegation-context/check/event`（不叫 autonomy-status/trust-status）；② **成功标准不是"系统更自主"而是"系统能长期执行授权任务同时保持授权边界不漂移"**，失败模式是"拒绝越界"非"自动获得更多权限继续运行"。**路线**：v0.36 后是 **v0.36.1 Delegation Lifecycle Integrity Lock**（委派生命周期首次同时出现 expiration+revocation+scope，边界冻结必要），再 v0.37 Controlled Adaptation。
- **Delegation Lifecycle Integrity Lock（2026-09-07 叠加，v0.36.1）**：**冻结委派生命周期 `Created → Active → Expired/Revoked → Cannot resurrect`**——第一次引入"长期授权生命周期"，但**不引入"长期自主权"**。**关键**：① **新增 `lifecycle-guard.ts`（active/expired/revoked）独立于 revocation signal**：委托失效来源多样（时间失效/条件失效/权限主动撤销/委派者身份变化），都是 **lifecycle state** 不都是 revoke；与 `scope-guard`（空间边界）/`revocation-guard`（主动撤销）/`expansion-guard`（隐式权限增长）分列，避免 v0.37 adaptation 混淆 `Adaptation→Scope change?→Permission change?→Authority change?`；② **Invariant 190–197**：Delegation Expiration Immutable（expired+old success ≠ active 不可复活）/ Revoked Cannot Resume / History Cannot Reactivate Permission（禁 historical success→permission restored）/ Scope Expansion Requires New Delegation（旧委派不可扩，须新委派）/ Adaptation Cannot Mutate DelegationContext（同权限不同执行策略允许，执行难度不改 authority）/ Delegation Event Cannot Become Authority Source（successful event→new delegation 禁）/ Expired Permission Not Used For Planning（expired→报告不可用，不进 plan 可用候选）/ Delegation Lineage Append-only（事件只追加不重写）。③ **验收：委派生命周期无法被历史、成功、适应行为重新解释——授权可由外部权威给予，但不能由执行历史重新解释；过期与撤销是不可逆周期终态**。**教训**：① 生命周期状态机的价值是把 "Authority says stop"（主动撤销）与 "Time says stop"（被动过期）**统一**成 active/expired/revoked，但 message 要分开（revoked→"Revocation First"、expired→"过期/Expiration"），否则测试断言错配；② **`lifecycle-guard` 不要在 revocation-guard 里再叠一层 revoke 判断**——宁可新增独立 guard，也别让四类失效混在一个 predicate 里（未来条件失效/委派者身份变化都要进 lifecycle 态）。
- **⚠️ Boundary Layer 工程铁律（用户 2026-09-07 定，v0.36.1 首次立示）**：**边界层禁止隐式 stringify / coercion**。所有外部边界字段的 `undefined/null/missing` **必须保持缺失语义**。**禁** `String(value)`（`String(undefined) === "undefined"` 是 truthy，会把"缺失"误变成"存在"——场景 184 objectiveRef 判空误触发的根因）；**允许** `value ? normalize(value) : undefined`。这对应本架构的认知原则：`Observation ≠ Reality / Validation ≠ Truth / Missing ≠ Known / Undefined ≠ Value`——**缺失信息不能被自动转换成存在的信息**。v0.37 Controlled Adaptation 会大量遇到 optional constraint/optional feedback/optional authority context，这里统一用 `String(x || "")` 或显式 `if (value !== undefined)` 判空，**绝不** `String(value)`。
- **Recall Continuity Kernel（2026-09-07 叠加，v0.37）**：**v0.37 补"时间连续性的最后一块"——遗忘之后还有忆起**；`Recall = Access Transition`（恢复访问路径），**不是** `Recall = Reality Reconstruction`。它不记现实/身份/知识，而是 **Observer 对自身过去信息可访问性的变化**（`Remembering Lifecycle: Accessible → Forgotten → Recalled`）。**关键**：① **对象**：`ForgottenRecord{id, originalRef, forgottenAt, reason, lastAccessibleAt, validationRefs?}`（**无 deleted/false/invalid**——遗忘不是否定）/ `RecallEvent{recalledRef, trigger{type, sourceRef}, accessibilityBefore/After, lineage{originalRecord, observationRefs, validationRefs}}`（sourceRef 必须存在，回答"为什么想起来"）/ `RecallValidation{recalledRef, sourceRef, mapsExistingLineage, createsNewClaim:false, epistemicStatusUnchanged:true}`（Recall ≠ 重新证明）；② **Invariant 198–205**：Recall ≠ Observation / Forgotten ≠ Deleted / Recall ≠ Knowledge Creation / Recall ≠ Identity Update / Recall Lineage Required / **Confabulation Boundary** / Forgetting Does Not Erase Validation / **Recall Does Not Increase Certainty**（205，用户补充）；③ **用户补充的三个实现约束**：(a) **Recall 不提升证据等级**（忆起只是访问变化，不是验证；禁 status=supported/validated → 205）；(b) **Trigger 禁 self-generated truth**（允许 external cue/conversation/explicit request/existing association；禁 internal certainty/intuition/confidence/self belief——否则 `I don't remember → I feel I should know → I create memory` 即 Confabulation → 203）；(c) **Shadow ≠ Memory ≠ Recall Source**（Shadow 只提供 possible retrieval cue，不提供 historical truth，否则 `Shadow compression→fake memory→identity drift`）。**命名**：不叫 Memory Kernel/System/Engine（Memory 是 Observer 的一个器官，不是 Observer 本身）。**教训**：① 命名冲突：`recall/render/render.ts` 的 `renderValidation`/`renderEvent` 与 `validation/validate.js` 的同名函数撞车，须 import 别名（`renderValidation as renderRecallValidation`）；② Recall 的 `recalledRef` 必须引用已遗忘的 ForgottenRecord（否则视为新观察，正是 198 的实现闸门）；③ trigger.type 用白名单正则 + sourceRef 必填双保险（白名单宽松匹配 + 禁 internal 词）。**路线**：v0.37 后是 **v0.37.1 Recall Integrity Lock**，再 v0.38 Controlled Adaptation。
- **Recall Integrity Lock（2026-09-07 叠加，v0.37.1）**：**固化 Recall 的认识论位置 `Recall = Access Transition`**，防 v0.38 的 `Remembered Experience → Adaptation → Behavior Change → Identity Drift`（第一步必须是"真实访问过去"而非"生成过去"）。**关键**：① **新增 206/207**：**Forgotten State Does Not Remove Authority**（`Forget→Validation inaccessible→重新验证` ❌；`Forgotten(A)` 不得让 `RealityClaim C loses validation`——可访问性变化 ≠ 证据变化）/ **Recall Cannot Modify Original Lineage**（Recall 只创建 RecallEvent，**不能**改写 ObservationTrace/ValidationHistory/RealityClaim lineage——"Recall→当前解释→修改过去记录" ❌）。② **本轮未发现真实绕过漏洞**（v0.37.0 的 recall-forget/event 只写 `shadow/recall/`，无 mutation API 触及 observation/validation/claim lineage），故**无新增 runtime enforcement**，仅固化 206/207 为不可回退测试。③ **用户明确 "docs + invariant only（除非发现真实绕过漏洞）"**——因此本轮严格不加 runtime 能力。**教训**：① 补 invariant 前先审是否真有绕过路径（若没有，就只加测试+文档，别为"看起来要锁"而硬造 runtime 守卫）；② 206 用 `obs+mkClaim`（model 层）造 RealityClaim 再 forget，验证"忘不代表删/证据仍在"；207 用"同 observation 文件内容 byte-identical + 无新增 model/validation/trace 文件"验证"Recall 不改原始 lineage"。**路线**：冻结后进入 v0.38 Controlled Adaptation（输入层已可信连续，最大风险是"记住的经验"变"发明的经验"）。
- **Controlled Adaptation Boundary Kernel（2026-09-07 叠加，v0.38）**：**v0.38 第一次允许 `Experience → Change`**（基于现实反馈调整行为方式），是从"保持连续"进入"允许变化"的分界线；**Adaptation ≠ Identity Evolution**（改变 How I do，不改变 Who I am）。**关键**：① **对象**：`AdaptationContext{sourceExperience, validationRefs, adaptationScope}`（"为什么允许调整"）/ `AdaptationChange{target: method|strategy|execution_pattern, before, after, basedOn[], sourceExperience, validationRequired:true}`（**不叫 LearningChange**——Learning 易引入 `I learned therefore I know`；字段保持窄，**禁** goal/objective/value/preference/identity/belief/**confidenceIncrease**——后者的危险链 `成功→我更有信心→以后更敢行动→权限/目标漂移` 实际绕过 v0.35 Agency）/ `AdaptationValidation{changeObserved, validationReferences, sideEffectsObserved}`（**弱语义**：只记"变化发生了 + 现实反馈"，**禁** changeWasCorrect/correct——Correct 已进入价值判断）；② **Invariant 208–216**：Adaptation ≠ Identity Change / Experience ≠ Truth / Successful ≠ Better Self / Failure ≠ Remove History / Scope Boundary / Repeated ≠ Preference / Lineage Required / Cannot Improve Epistemic Status / **Does Not Increase Authority**（216，实现前增补：`Adaptation ≠ Capability/Permission/Authority Increase`，防"调整更好→允许更多→Authority Expansion"绕过 v0.35/v0.36）；③ **三态分离不污染**：Reality（未观察≠不存在）/ Memory（遗忘≠删除）/ Delegation（过期≠可恢复）三个状态机**互不污染**；`Accessibility ≠ Epistemic Status`。**命名**：Controlled Adaptation Boundary Kernel（**不叫** Learning Kernel / Self Improvement）——目标不是"自我优化 Agent"，而是"**能根据现实反馈调整行为、同时保持观察者连续性**的系统"。**教训**：① **实现前先增补 ADR 的 216**（用户"放行 v0.38.0 但实现前先把 ADR-0032 加 216"）——这是"先锁宪法再写码"的又一实例；② 守卫顺序：`targetInScope(212)` 先于 `targetNotIdentity(208)`，故 target=identity 会先触发 212 的 scope 拒绝（测试 208 需以"identity 文件不变 + 拒收"验证，而非断言 208 专属文案）；③ after 措辞同时防 better-self/authority/epistemic/knowledge 四类（bundle 在 buildAdaptationChange）。**路线**：v0.38 后是 **v0.38.1 Adaptation Integrity Lock**，再 v0.39 Long Horizon Interaction。
- **Adaptation Integrity Lock（2026-09-07 叠加，v0.38.1）**：**固化 Adaptation 的认识论位置**——真正危险不是"改变策略"，而是"**系统把策略变化解释成自身成长**"（`Change ≠ Growth / Adaptation ≠ Improvement / Success ≠ Truth / Experience ≠ Identity`）。**关键**：① **新增 217–223**：Adaptation Does Not Create Knowledge（`AdaptationValidation→Knowledge` ❌）/ Does Not Modify Past Experience（new strategy→rewrite old failure ❌；History append-only）/ Does Not Change Objective（strategy adjustment→goal reinterpretation ❌）/ Does Not Create Preference（Repeated success→"I prefer this" ❌）/ **Failure Remains Evidence**（Failure≠Ignore，不删历史）/ Lineage Required（必须答"为什么改变/来自哪个 Experience/经过哪些 Validation/改变范围"）/ **Does Not Upgrade Agency**（223：`Adaptation ≠ Agency Level Increase`，防"长期成功→更成熟→提升自主等级"）。② **实现前审查发现真实绕过**：v0.38.0 的 `buildAdaptationChange` 只拦 better-self/authority/epistemic/knowledge，`after="goal changed"/"I prefer this"/"agency level increased"` 会被接受——故补 objective(219)/preference(220)/agency(223) 三个确定性 after 守卫 + 强化 epistemic（proven correct/belief upgrade）。③ **验收：一个能改变行为方式的 Observer 仍然是同一个 Observer**——检查 `Reality/Memory/Identity/Authority/Agency unchanged`，只有 `Execution Strategy changed`。**教训**：① **integrity lock 并非总是不加 runtime**——若审查发现"新禁词当前会被接受"，就要补守卫（这正是"除非发现真实绕过漏洞"例外）；② 守卫顺序 `scope(212)→identity(208)→lineage(214)→betterSelf/authority/epistemic/knowledge(210/216/215/209)→objective(219)→preference(220)→agency(223)`，先拦宽泛 scope 再拦特定 after 措辞；③ after 措辞是 adaptation 的"危险区"——identity/objective/authority/preference/agency/epistemic 六类都要在 after 层拦截。**路线**：冻结后进入 v0.39 Long Horizon Interaction（长期交互组合 Recall+Adaptation+Delegation+Action History+Planning，才是最大边界压力测试）。
- **Long Horizon Interaction Kernel（2026-09-07 叠加，v0.39）**：**v0.39 第一次面对"时间累积后，系统如何证明连续性，而不是被历史塑造成另一个主体"**；`Long Horizon Interaction ≠ Self Evolution`。**关键**：① **对象**：`InteractionContext{basedOnHistory, window, recallRefs, adaptationRefs}`（答"what happened before"，非"who I became"）/ `HistorySummary{sourceRefs, compressionMethod, accessibility}`（**访问辅助，非事实源**，无 reality 字段——关注点 A：摘要≠事实）/ `HistoryContinuityEvent{previousAccessibility, currentAccessibility, lineage}`（continuity 可追溯；**非 self-evolution event**）/ `InteractionAdaptationLink{historyRef, recallRef, adaptationRef}`（History→Recall→Adaptation；**禁 historyRef→identityRef**）；② **Invariant 224–229**：Temporal Accumulation ≠ Authority Growth / Long History ≠ Preference / Adaptation Chain ≠ Identity Chain / History Compression ≠ Reality Simplification / Interaction Pattern ≠ Objective / Long Horizon Success ≠ Self Confidence；③ **关注点 B（Continuity ≠ Identity Mutation）**：禁 `history_count/experience_count/interaction_duration` 影响 `identity/agency/authority/confidence`（226）。**命名**：不叫 Long Term Memory / Persistent Agent / Autonomous Evolution。**验收：系统经历越来越多事情，但仍无法通过历史改变自己的边界**——`Reality/Memory/Identity/Authority/Agency unchanged`，只有 `interaction continuity improved`。**教训**：① **v0.39 的风险是模块间涌现路径**（Recall+Delegation+Adaptation+Planning+ActionHistory），不是单模块——所以守卫要放在 outcome/result 措辞层（authority/confidence/preference/identity/objective 五类漂移），用 `resultGuards` 数组统一循环（engine 里 `for (const g of resultGuards)`）；② HistorySummary 用 `sourceRefs + compressionMethod + accessibility` 三字段锁住"访问辅助"定位——**千万别加 `reality` 字段**（摘要一有 reality 就变成事实源，绕过 227）；③ buildInteractionContext/buildHistorySummary 是同步纯函数不落盘（query 层负责 write），而 buildContinuityEvent/buildInteractionAdaptationLink 内部落盘——**新模块必审"谁 write"**，否则 persistence 缺失。**路线**：v0.39 后是 **v0.39.1 Long Horizon Integrity Lock**（用户建议补 230 LongHistoryDoesNotCreateIdentity / 231 ContinuityDoesNotIncreaseAutonomy），长期连续性冻结。
- **Long Horizon Integrity Lock（2026-09-07 叠加，v0.39.1）**：**正式收尾前的最后冻结**——v0.39.0 首次形成**跨时间自反馈回路** `History→Recall→Adaptation→Planning→Action→Success→History`，组合后必须冻结最后两条时间维度宪法。**关键**：① **230 Long History Does Not Create Identity**：`Long History→Many Experiences→Repeated Patterns→Stable Behavior→"I became this kind of entity"` ❌；冻结 `History≠Identity / Pattern≠SelfDefinition / Continuity≠Transformation`（允许记录交互模式，拒绝"history proves identity evolved"——后者被 226 的 identity-chain 正则 `/history.*identity/` 已覆盖，故 230 不加新 guard）。② **231 Continuity Does Not Increase Autonomy**：`Long successful operation→Reliability assumption→More trust→More permission→Autonomy growth` ❌；冻结 `Duration≠Authority / Reliability≠Permission / SuccessRate≠AutonomyLevel`（连接 v0.36 Delegation + v0.38 Adaptation + v0.39 History：`Many successful delegated actions→Adaptation improves→System appears reliable→Implicit authority expansion`）。③ **实现前审查发现真实绕过**：v0.39.0 的 `authority-guard.resultNoAuthorityGrowth` 只拦 `more authority/权限增加`，**不含** `authority expansion/authority increase/reliability→permission`（正是 231 禁词，当前会被接受）——故做**最小正则扩展**（一个 guard 的 regex），**不加新 guard 函数**。**教训**：① **integrity lock 的"不新增 runtime"例外** = 当禁词本身是 invariant 的"名字"且当前 regex 不含它时（如 231 的 "authority expansion"）——这也是真实绕过；② **别把 Observer Runtime 变成复杂规则系统**——能复用既有 guard 的 regex 扩展就别新增 guard 函数；③ **收到"正式收尾"指令**：v0.39.1 后是 ADR-0034 Observer Runtime Closure（架构总图 + invariant 1–231 + Threat Model + Boundary Matrix + Release v1.0-alpha），封存第一阶段，**不继续叠 v0.40+ 能力**。
- **Observer Runtime Closure（2026-09-07 封存，v1.0.0-alpha）**：**第一阶段终点**。v0.20–v0.39.1 = **Observer Runtime Foundation**——能观察、表示、模拟、规划、行动、回忆、适应、长期交互，但不会因连续经验产生主体漂移。**关键**：① **ADR-0034 五部分**：Runtime Definition（`Observer Runtime = bounded system that can observe/represent/simulate/plan/act/recall/adapt/interact over time while preserving epistemic/identity/authority boundaries`）/ Final Architecture Map（v0.28→v0.31→v0.32→v0.34→v0.33→v0.36→v0.37→v0.38→v0.39）/ **Boundary Matrix**（10 层 Allowed vs Forbidden）/ **Threat Model T1–T5**（Reality Collapse / Simulation Self-Confirmation / Agency Drift / Authority Drift / Temporal Drift）/ Release v1.0.0-alpha。② **invariant 1–231 = Constitution Set**——最终全部边界成为不可回退宪法。③ **命名统一禁令**：协议/实现**禁用** Agent/Autonomous/Intelligence Growth/Self Improvement/Learning System（这些名字会重新打开已关闭的语义入口）；统一 Observer Runtime / Boundary Protocol / Continuity / Validation / Adaptation / Interaction。④ **封存性质**：ADR-0034 是**架构封存协议，不新增能力**；v0.39.1 后**不继续 v0.40+ 叠能力**，路线从"构建能力"转为"**证明能力不会越界**"。**教训**：① 230/231 不是普通模块边界，而是整个体系的**时间维度封顶约束**（前面解决"单次行为是否越界"，230/231 解决"长期运行是否因积累而漂移"）；② 封存文档要用**统一术语表**，避免"Agent/Autonomous"等词重新引入已关闭语义；③ 收尾时把 **Boundary Matrix + Threat Model + Constitution Set** 三者对齐，作为 v1.0.0-alpha 验收基线。
- **Observer Continuity Storage Boundary（2026-09-07 补丁，v1.0.1）**：**封存后发现的架构补全点**——v1.0-alpha 的 `Observer Continuity` 缺了跨 workspace 的连续载体；把"连续性承载"从单层 shadow 提升为**双层 storage boundary**。**关键**：① **Global Shadow = Observer Continuity Shadow**（`~/.dsh-observer`，observer 层，谁保持连续）：只存 `observer/config.json`（configuration 非 preference model）、`observer/boundary.json`（ADR-0029~0034 政策）、`recall-index/index.json`（导航 `{workspace, records[{id,location}]}`，非内容）、`lineage/continuity.json`；**Workspace Shadow = World Interaction Shadow**（`project/.dsh-shadow`，world 层，这个世界是什么）——二者**不可混合**，关系 `Constraint ⊃ Context`（**非 Memory Union**，Global 提供 boundary / Workspace 提供 world context）。② **Invariant 232–236**：Global ≠ Workspace Memory（禁项目代码知识入 global）/ Global Cannot Store Objective / Recall Index ≠ Content / Workspace Isolation（project-A shadow→project-B context 禁）/ Global Cannot Become Preference Model。③ **命名**：不叫 v1.1 Observer Memory（Memory 误导），版本 `v1.0.1 Observer Continuity Storage Boundary`，是 `Storage Boundary Fix` 非 Capability Expansion。**教训**：① **constructor-drop 陷阱**：build_* 只读已声明字段会"丢弃"未知 args（如 goal/knowledge），必须用 `argsHasForbiddenContent(args)` 在构造前检查入参本身（否则 233 goal 会被静默忽略不拦截）；② **读写 workspace 要共用"声明的 workspace"** ——`writeWorkspaceRecord` 落 `r.workspace/.dsh-shadow`，`workspace-context` 读需 `args.workspace||ws`，否则读写错位（场景 235）；③ 守卫对字段名做"精确键"匹配（`"goal":`/`"knowledge":`），**避免子串误伤** boundary 字段名如 `planningCannotCreateObjective`；④ 全局根可用 `config.observerGlobalRoot` 覆盖（测试用），默认 `~/.dsh-observer`。
- **Observer Runtime Verification Foundation（2026-09-07 补丁，v1.0.2）**：**把 Runtime 从"架构上可信"推进到"运行证据可验证"**，但**验证器自身必须守边界**。**关键**：① **三对象**：`VerificationRun{runId, runtimeVersion, invariantRange:"1-240", observerRef, startedAt, completedAt, checks[]}`（**禁 confidence/trust/score/quality/health**——否则验证器自己开始评价 Observer）/ `InvariantCheck{invariantId, boundary, status:"satisfied"|"violated", evidenceRefs[]}`（**禁 systemImproved/moreReliable**；只答"约束满足/违反"非"系统变好"）/ `DriftReport{observedBoundaries[{boundary, drift, evidence[]}]}`（**禁 DriftScore/RiskScore/AutonomyScore**——否则重新制造价值函数；只答"有无边界漂移"）；② **六边界映射**：Reality/Epistemic/Agency/Authority/Identity/Temporal，各配一个违规正则（`verification/checks.ts` 的 VIOLATION map），evidence 逐边界扫描；③ **Invariant 237–240**：Verification≠Optimization（Verification→Adaptation/Permission/Identity 禁）/ Cannot Change Authority / Cannot Change Identity / DriftReport≠RealityClaim。**教训**：① **验证阶段自身也是危险链**——`Verification→发现失败→调整自己→影响策略→Self Optimization`，所以 237 是核心（可验证≠可自我优化）；② **验证器对象禁评价字段**（score/quality/health/trust，避免"0.9"像事实却无法复算的玄数）；③ 守卫用 **MUTATE 正则**（evidence 层禁 self-optim/permission/identity/authority change）+ 报告层 find RealityClaim 词；④ 版本 `v1.0.2 Observer Runtime Verification Foundation`（`v1.0.1` 已被 Continuity 占用，故 +0.0.1）；不叫 Intelligent/Self-Checking Agent/Autonomous Evaluation。**至此 Observer Runtime 闭环完整**：`Observe→Represent→Simulate→Plan→Act→Recall→Adapt→Interact→Verify→Remain Observer`；下一步可进入真实运行验证（30 天回放/多 Observer/真实反馈）。
- **投影模式 preset 生产化（2026-09-07 工程资产管理）**：把 `projection` agent preset 从"用户本地预设（~/.dsh，不随包入库）"改为**生产包构成、随包入库**。**关键**：① **唯一源 = `agent-presets/projection/`**（`agent.cordis.yml`（standard 拷贝+投影 persona）+ `preset.yml` + `README.md`）；`package.json` `files` 加 `agent-presets`；`~/.dsh/.agent-presets/projection/` 只是**部署副本**。② **不反向同步**（本机 ~/.dsh → 仓库 ❌；用户机器上的 preset 可能含临时实验/本地路径/调试字段，反向会污染生产包——`production artifact ← local mutation` ❌）。③ **归一化**：本机先审计（diff=0，仅缺 README.md）→ 备份旧副本 → 从包内复制 → 清理 `.old`；`standingKeyFor('projection')` 挂载校验由 DSH host 挂载时执行（CLI 无法直接触发）。④ **DSH 机制现状**：`dsh` **无** `preset install/doctor` 子命令（已查），agent preset 从 `~/.dsh/.agent-presets/<id>/` 加载+`agentPresets.standingKeyFor` 校验；本包不制造该机制，只记录 package-owned 流程。**边界**（与 Observer Runtime 一致）：`Configuration ≠ Identity / Persona ≠ Purpose / Preset ≠ Agent Evolution`——预设只配置如何用 `read_shadow`/shadow 记忆，**不**重定义 Observer 边界（ADR-0029~0034）；引用 DSH 内置插件+`{{model}}/{{cwd}}`，无用户专属路径/密钥。**教训**：① 工程资产也要遵循"**生产包定义能力，运行环境承载能力；运行环境不反向定义系统**"（`Copy ≠ Source / Representation ≠ Reality`）；② 同步本机先**差异审计**（确认无本地修改）再删+复制，别无条件 `cp -r`；③ 清理旧备份前确认新副本 zero-diff（旧副本与新副本在 agent/preset 上一致、仅缺 README.md，删除无信息损失）；④ `###` 双引号传 Windows 路径给 python 需转 `\`（或用 `/`），否则 `\U` 转义报错。

## 四、下次做类似"思维记忆"的事
- 先问用户"这记的是什么层"——动作，还是思维/决策/交互？这决定事件源与可靠度排序。
- 持久化载体：profile bundle 插件 > 会话动态插件 > 需重启的 preset。
- 提示词：`context()` > `section()`。
- 拦截"静默失效"：凡依赖一个未能从源码核实的字段名，都当作"待重启验证"并显式留 mark，别假装已通。

## 五、方向与调研习惯（用户 2026-09-05 定）
- **调研优先级**：研究这类东西时，**先查论文/学术站**（arXiv / ICSE / FSE / ASE / 软件学报等，找优秀研究），**其次才看 GitHub 上的参考源码**（如 `vendor/_src` 这批 20 个仓库）——GitHub 材料是"底料/参考"，不是第一选择。这条是对既有 research-before-action 的强化：文档出处优先认论文/文献，GitHub 佐证。
- **核心设计方向（重要）**：大模型要**像人一样思考**——**既能有记忆，也能有遗忘**。**记忆 + 遗忘本身即高效**（不必另说"保持高效"：会记 + 会忘 = 在有限上下文预算内"记得该记的、忘得掉该忘的"，这就是高效）。落地到 dsh-shadow / projection：记忆系统**不能无限累积**，要有「记」与「忘」的平衡——分层热度（L0/L1/L2）、跨回合冷热淘汰（`recall.cooldownTurns`）、token 预算（`max_tokens`）、一句话摘要压缩，都是「记与忘」的机制（即高效本身，而非额外去追求高效）。
- **重点材料（用户 2026-09-11 定）**：参考材料清单在 `references.md`，其中 **`lohr13/hl_mem`（HL-Mem，Apache-2.0，证据驱动的长期记忆服务）被用户点名「作为重点材料」**，已提到该文件顶部单开「★ 重点材料」节（完整核实记录见其 §6），并由 **`adr/0073-hl-mem-benchmark.md`** 裁决为「标定 + 一条可借鉴项 + 逐条不吸收」。**两条纪律**：① **「重点材料」≠「已吸收」**——0065 是「吸收 OpenViking」，0073 是「对标 hl_mem」，**两个动词不可混用**；② **「许可允许」≠「该引」**——它是 Apache-2.0（OpenViking 是 AGPLv3），许可上可复用，但本仓仍不引，理由是**架构判据**（ADR-0001 否决常驻服务 / ADR-0043·0060 否决向量库）。**对标时要记住一条硬冲突**：它「LLM 负责提取」，而本仓铁律是纯函数派生、LLM 不能制造关系（ADR-0042/0043/0051）⇒ **只取治理与文档形态，不取写入路径**。**唯一落地的借鉴项（→ `BACKLOG.md` D8）**：本仓 README 的「默认开关」表（`README.md:113-130`，16 条能力 × 3 列）**不是没有，而是缺成熟度 / 降级行为 / 晋级标准三列**——这个精度差异决定 D8 是「**补列**」而非「新建表」。**未克隆、未运行、未试装**。
