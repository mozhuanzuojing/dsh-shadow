# dsh-shadow 项目约定（在本仓库内工作时适用）

本文件面向在 `dsh-shadow` 仓库内工作（改代码/改文档/改配置）的 agent 与协作者。

## 提交后必须推送

- 任何修改落成 `git commit` 后，**必须推送到 `origin/main`**，禁止只提交、不推送。
- 若本次新建了 tag，一并推送：`git push origin <当前分支> <tag>`（精确指定，避免 `--tags` 误推无关标签）。
- 例外（需说明原因）：用户明确说「先别推 / 只提交」；无 remote / detached HEAD；推送会带来多余副作用。

## 本仓库常用的构建与验证

- 构建：`npm run build`（= `tsc`）。`dist/` 由构建生成，**改源码后必须重编译并保持 `dist/` 与源码同步提交**。
- **验证只用一条命令：`npm run verify`** —— 它串行跑 `typecheck:tools` → `typecheck:tests` →
  `audit:layers` → `audit:docs` → `eval:retrieval:check` → `audit:ratchet` → `tsc --noEmit` → `test:all`
  （**检查条数不写在这里**：由 `run-tests` 自己打印，末行应为 `[run-tests] ALL PASS ✅`）。
  ⚠️ **本文件曾写「仓库没有配置 test runner」—— 那是过期信息**（运行器是 `tools/run-tests.ts`）。
- **跑测试前必须 `npm run build`**：`test/*.test.ts` 与 `tools/*.selftest.ts` **import 的是 `dist/`**。
  只跑 `npx tsc --noEmit`（**不产出**）就去跑测试，读到的是**旧代码**，
  会得到「明明改了却没生效」的假象（v1.15.65 实测踩过一次）。
- 源码入口：`index.ts`（Cordis adapter）→ tsc → `dist/index.js`（DSH 加载编译后 JS）。

## 脚本一律 TypeScript

- 临时探针、一次性脚本、取证脚本**一律写 `.ts`**，用 `node x.ts` 直跑（原生 type-stripping）；
  **不要** `.js` / `.mjs` / `.cjs`。仓库内不保留手写 `.js`（`dist/` 编译产物除外）。
- 取证/探针脚本放 `../.docs/fix/<年-月-日>/`，**并在文档里写明怎么跑**（复审者要能重放）。
- **证据的入口在这里**：`../.docs/fix/2026-09-12/` —— 先看 `meta-review-four-rounds.md`
  （四条可重放命令 + 分类对照 + 未做清单），再按里面的命令名找对应探针。
  **任何一次「审查/修复」类工作都应在该阶段结束时更新或新增这份入口** ——
  否则证据散在 `CHANGELOG`/ADR 里，复审者找不到它（v1.15.69 实测：前四轮就是这样，**没人能看到怎么重放**）。

## 引用纪律（v1.15.65 立）

本仓的核心纪律之一是「每条断言带 `文件:行号`」。代价是**行号是最易腐烂的引用形态**：
v1.15.65 只改了 6 个文件，就产生 **≥5 处**过期行号（`core/writer-core.ts:69`、
`core/writer-materialize.ts:161` 等），散落在 `README` / `BACKLOG` / `adr/*` 里。

### 历史文档 vs 当前态文档（**先分清楚，再谈修**）

| 层 | 行号的语义 | 过期了怎么办 |
|---|---|---|
| `CHANGELOG.md` | **该版本当时**的行号 | **不改**。它是归档，改写 = **伪造历史** |
| **已冻结**的 ADR（正文） | 冻结时的行号（正文常自带「冻结时」字样） | **不改正文**，写**补记**（如 `adr/0049` 的补记） |
| `README.md` / `BACKLOG.md` / `CONTEXT.md` / `MATERIALS.md` / `AGENTS.md` / **未冻结** ADR | **当前**代码的行号 | **必须修**（或改成符号引用） |

⇒ 上一轮我把 `CHANGELOG` 里的一处计数（「169」）改掉是对的（那是**计数错误**，不是行号）；
但**不要**去「批量刷新全仓行号」—— 那会把归档层一起改坏。**只修当前态文档。**

### 三条操作要求

- **优先引符号名**（函数名 / 常量名 / 配置键），行号只在必要时加，且写成
  「`core/writer-core.ts` 的 `episodeShow`」这种**不依赖行号也能定位**的形式。
- **改完代码，回头核对引用它的文档**：对每个改过的文件跑一次
  `Select-String -Path *.md,adr\*.md -Pattern '<文件名>:\d+'`，只处理**当前态**文档里那些。
- **数字要带范围与时刻**：「`core/` 下当时 25 处」而不是「本仓 25 处」——
  否则它会在下一次改动后变成一句无人能复核的断言。

### 每份文档「owned by」什么（免得同一件事写五遍）

| 文档 | 它**拥有**的层 | 它**不该**承担 |
|---|---|---|
| `README.md` | **当前状态** + 面向使用者的表（如「默认开关」表） | 决策理由 |
| `adr/*.md` | **决策与理由** + 判据 + 缺陷表（权威） | 状态流水账 |
| `BACKLOG.md` | **状态与指向** | 复制 ADR 的表（应写「见 `adr/00xx` §n」） |
| `CHANGELOG.md` | **每个版本的历史归档**（含当时的数字） | 当前状态 |
| `../.docs/fix/<日期>/` | **证据**（链路矩阵 / 正反对照 / 怎么重放） | 决策与状态 |

同一条事实出现在多层时，**每层只回答它自己那个问题**，而不是互相复制同一段文字。

## 判据收一处 / 修一类而不是修一条

- 同一个判据（比较点扫描、路径分类、默认值回落、降级是否可见）**只能有一份实现**。
  本仓已有两次代价明确的教训：`tools/comparison-points.lib.ts`（两个审计工具各写一份正则）、
  `core/util.ts:numOr`（默认值回落散在三处）。
- **修一类缺陷时要扫同类 `catch`**，不是只修清单上那几条 —— v1.15.65 就因为在
  `writeAbstracts` 旁边漏看了一个同族 `catch` 而返工。

## 改长文档的三条规矩（v1.15.67 立，**因为差点丢掉 34 个版本条目**）

事故经过（锚点命中同名标题 ⇒ 删掉 34 个版本条目、`Set-Content` 零报错、靠数不变量才发现）
见 **`adr/0085` §8.7**；此处只留规矩。

| # | 规矩 | 为什么 |
|---|---|---|
| 1 | **先算行区间，只在区间内定位锚点**。**绝不**用 `indexOf` / `Replace` 在全篇找「看起来一样」的标题 | 长文档里同名小节极多（`### 6. 验证` 在本仓出现过几十次），全篇查找等于**赌** |
| 2 | **`Replace` 必须打印匹配数，0 就拒绝写盘** | PowerShell 的 `.Replace()` **没有匹配也不报错**，静默返回原文。本轮已有 **≥3 次**「以为改了、其实没改」（一次导致条目里整段内容没写进去） |
| 3 | **改完先核对一个可数的结构不变量**（标题数 / 表格行数 / 章节数），再提交 | 「命令成功」不等于「改动按意图发生」。本仓每道门都在打印数字，**文档改写也该如此** |

**优先用 `edit` 工具做精确替换**（它要求 `old_string` **恰好出现一次**，天然满足规矩 1）；
只有批量改写才用脚本，且必须满足规矩 2、3。

## 「能推出来的字段不要手写」+ `audit:docs` 门

**判据**：任何能由其他数据推出来的字段 —— **版本号 / 计数 / 链路清单** ——
**要么别手写，要么配一道门**。事故经过（实测数据、三个字段各自烂了多久）见 **`adr/0085` §8.7/§8.8**，此处只留规矩。

**`npm run audit:docs`**（已进 `verify`）查三条：

| # | 检查 | 退出码 |
|---|---|---|
| ① | **三方版本一致**：`package.json.version` / `README` **行首粗体**的「当前版本」行 / `CHANGELOG` 首个 `## [vX.Y.Z]` | 0 / 1 / 2 |
| ② | **`verify` 与两块文档**：每一步都被 `README` 的「改代码后先过闸门」代码块与 `AGENTS.md` 链路行点名（正向），**且**那两块点名的每个 `npm run` 都在 `verify` 里（**反向**） | 0 / 1 / 2 |
| ③ | **声明计数 = 实际计数**：`README` 默认开关表声明的「现 N 行」必须等于实际数据行数 | 0 / 1 / 2 |

- **② 的反向一半是必须的**：只有正向时，「门被从 `verify` 里摘掉」反而让检查更宽松、照样全绿，
  而文档里那一行变成**死指针**。
- **③ 的由来**：v1.15.67 只给**链路清单**配了门，**计数**只立了规则没落地 ⇒ 一天内就发现两处在腐烂。
  它只覆盖**这一处**计数（结构稳定、可机械化）；其余计数（如 `BACKLOG` 的表头）用「**判据 + 命令**」代替写死。
- ⚠ **三条都只是必要条件**：答「有没有 / 一致不一致」，答不了「顺序与描述对不对」⇒ **不得据它宣称文档正确**。
- **发版时三处一起改**：`package.json` 的 `version` · `README` 的「当前版本」行 · `CHANGELOG` 新条目。
- **末条（v1.15.69 的血教训）：在「修计数腐烂」的同一段里，我又手写了一个错的计数。**
  `BACKLOG` 表头那处修计数时写了「✅ 3 个 ⇒ 现存 10 条」，而真值是 **✅ 4 个（漏了刚结案的 `T8`）⇒ 现存 9 条**，
  13−3=10 的算术跟着错，**而它自己列的名字只有 9 个**（数字与列表互相矛盾）。**`audit:docs` 抓不到它**——
  检查③只管 `README` 的表格行数。⇒ **两条**：
  **① 「判据 + 命令」比「写死的数」好，但最好的是「只给规则与命令、不写数」**（写数就得负责它下次还对）；
  **② 写了数就当场用命令核一遍并写下口径**（本例：按**标记**算 9 条，按**实质**算可能 8 条——`T3` 未带 ✅ 但正文称已分诊）。
- 本仓工作副本是 **CRLF**：写解析代码前**先归一化行尾**（`\r` 会让 `$` 锚定的正则失效 ——
  v1.15.68 的检查③就因此报过「声明 18 / 实际 19」的**假警报**，差一点把对的文档改错）。
