# dsh-shadow 项目约定（在本仓库内工作时适用）

本文件面向在 `dsh-shadow` 仓库内工作（改代码/改文档/改配置）的 agent 与协作者。

## 提交后必须推送

- 任何修改落成 `git commit` 后，**必须推送到 `origin/main`**，禁止只提交、不推送。
- 若本次新建了 tag，一并推送：`git push origin <当前分支> <tag>`（精确指定，避免 `--tags` 误推无关标签）。
- 例外（需说明原因）：用户明确说「先别推 / 只提交」；无 remote / detached HEAD；推送会带来多余副作用。

### 发版 tag 约定（v1.15.78 立 —— 因为**第二条仪式又悄悄停了**）

- **每发一版打一个 tag**：名字 = `v<package.json 的 version>`，**轻量 tag**（`git tag <名>`，不加 `-m`）。
  **口径**（别写死个数）：`git for-each-ref refs/tags --format='%(objecttype)'` 应全是 `commit` ——
  ⚠ **该口径的适用范围是 `v1.15.77` 起**：v1.15.81 实测同一命令得到 **`commit` 32 个 / 注解对象 33 个**，
  那 33 个注解 tag **全部在 `v1.15.77` 之前**（`v0.1.0` … `v1.14.1`、`v1.7.2` 等，早期用 `git tag -a` 打的）。
  按「历史缺口不补」**同一个决定**，**不追溯改建历史 tag** ⇒ 读这条时**只对 `v1.15.77` 起的 tag 生效**。
- **这道仪式现在有门**：`audit:docs` 的检查 ⑤（见下方「能推出来的字段不要手写」一节）。
  **它的能力边界（当前版本被豁免 ⇒ 漏打迟一版才发现）也写在那一节里** —— 用这道门之前先读那两段。
- **命令**：`git tag v1.15.78` → `git push origin main v1.15.78`（**精确指定**，不要 `--tags`）。
  核对：`git rev-parse --short v1.15.78` 应等于该版本的**发布提交**。
- ⚠ **历史缺口（用户 2026-09-13 定：不补）**：远端最后一个 tag 曾是 **v1.15.3**，
  而当时已到 **v1.15.77** ⇒ **v1.15.4 … v1.15.76 共 73 个版本没有 tag**。
  **决定：不追溯补打**，**从 `v1.15.77` 起往后每版都打**。⇒ 读 `git tag` 时不要以为「跳号 = 丢提交」。
- **为什么写在这里**：这是本仓**第二条**「靠习惯维持的仪式悄悄停掉」——
  第一条是 `README` 的「当前版本」行（**25 个版本**没人更新，v1.15.67 发现，检查①的由来）。
  **两条都没报错、也没腐烂，它们只是停了** ⇒ 凡「每次发版都要做一次」的动作，
  **要么写进这份清单，要么配一道门**；靠记性 = 迟早停。

### 发版一律走 `npm run release`（v1.21.26 立 —— 因为**出口没有闸门**）

- **一条命令**：`npm run release -- [--dry-run] [--message "<提交信息>"]`（= `tools/release.ts`）。
  顺序：三方版本一致预检 → **`npm run verify`（闸门）** → `git add/commit` → 删旧 tag（本地 + 远端）→
  打新 tag → 推分支 → 推 tag → 打印 `HEAD` 与 `tag → sha` 供核对。
- **闸门是退出码**：`verify` 非 0 ⇒ **立刻退出，一个 git 写操作都不发生**。
  判据在 `tools/release.lib.ts` 的 `gateDecision`（**唯一一份**），正反对照在 `tools/release.selftest.ts`
  （含 `exit 1` / `exit 2` 两条不许放行）；`exit 2` 会单独提示「结构/语料根缺失 ⇒ 那次绿是假绿」。
- **没有跳过开关**：刻意不提供 `--skip-verify` —— 那正是下面这起事故的成因。
- **由来（v1.21.26 事故，本会话第 4 次同类自身出错）**：`v1.21.26` 被 commit / 打 tag / **推到远端**，
  而它的 `verify` 是 **exit 1**（`writer-write` 与 `t8-silent-degradation` 两条测试红）。
  直接原因不是「测试红了」（那总能修），而是**出口没有闸门**：人手工走 commit/tag/push，
  `npm run verify` 的退出码**从未被当成继续/停止的判据**（此前只有「内容改写」上装了守卫）。
  ⇒ 上面那句「**要么写进清单，要么配一道门**」在这里第二次生效：清单（上一条 tag 约定）早就有，
  **门没有**，于是照旧停不住。
- **它不替代人工核对**：工具末尾会打印 `HEAD` 与 `tag → sha`（两者必须相等 —— tag 指向发布提交），
  远端 tag 集另用 `git ls-remote --tags origin` 核（口径：应只剩当前版本那一个）。
- **它刻意不做**：不 `--force`、不 `--tags`（精确指定 ref）、不改写历史；`--dry-run` 只打计划、什么都不跑。

## 本仓库常用的构建与验证

- 构建：`npm run build`（= **`clean` + `tsc`**）。`dist/` 是**本地 / 发包**产物，**不进 git**（v1.20.6 起）。
  `prepare` / `prepublishOnly` 会跑 build；Junction / 新克隆后若还没有 `dist/`，先 `npm run build`（或再跑一次 `pnpm install` 触发 `prepare`）。
  npm 包仍带 `dist`（`package.json` 的 `files`），宿主加载的永远是编译后 JS。
- **凡会写出 `dist/` 的 `tsc`，之前必须先 `clean`**（v1.20.11 立）：`tsc` **不删**已搬家/已删源文件留下的旧 `.js`/`.d.ts`
  （例：`identity/` → `selfhood/identity/` 后，`dist/identity/` 会一直躺着）⇒ 测试 / 宿主可能仍 import 到**幽灵产物**。
  **口径**：`npm run clean` 删整个 `dist/`；`npm run build` / `build:watch` **已内含** clean。
  手写 `tsc` / `tsc -w`（会 emit）之前也要先 `npm run clean`。
  `tsc --noEmit` / `typecheck:tools` / `typecheck:tests`（`noEmit: true`）**不写盘**，不必 clean。
- **验证只用一条命令：`npm run verify`** —— 它串行跑 `build`（clean+tsc，工具面/测试面类型门都 import `dist/`）→ `typecheck:tools` → `typecheck:tests` →
  `audit:layers` → `audit:scripts` → `audit:docs` → `audit:complexity` → `audit:granularity` → `eval:retrieval:check` → `audit:ratchet` → `tsc --noEmit` → `test:all`
  （**检查条数不写在这里**：由 `run-tests` 自己打印，末行应为 `[run-tests] ALL PASS ✅`）。
  ⚠️ **本文件曾写「仓库没有配置 test runner」—— 那是过期信息**（运行器是 `tools/run-tests.ts`）。
- **跑测试前必须 `npm run build`**：`test/*.test.ts` 与 `tools/*.selftest.ts` **import 的是 `dist/`**。
  `verify` 开头已 build 一次；`test:all` 再 build 一次（含 clean，防中途脏）。
  只跑 `npx tsc --noEmit`（**不产出**）就去跑单测，读到的是**旧代码**，
  会得到「明明改了却没生效」的假象（v1.15.65 实测踩过一次）。缺 `dist/index.js` 时 `run-tests` **exit 2**。
- **语料根（v1.15.83）**：`eval:retrieval:check` 要一个带 `.shadow` 的**工作区根** —— 默认由 `tools/retrieval-eval.ts` 的位置**往上找**（最多三级，取第一个存在的）。
  一个都没有 ⇒ **exit 2，且后面 3 步（分诊棘轮 / 插件面类型门 / 全部测试）不会跑** ⇒ 那一次「全绿」是**假绿**。
  兜底：`SHADOW_EVAL_ROOT=<工作区> npm run verify`。**取「有 `.shadow` 且语料够大」的那个根，别照抄写死的路径** —— 本机实测（2026-09-25）：`D:\project\dsh1` 的 `.shadow/atoms` 只剩 **87** 条 < 协议常量 `min_corpus_files=100` ⇒ 该门**拒出读数**（不是"通过"，也不是"失败"）；可用的是 `D:\project\net1`（**161** 条）。
  **T11 ①（v1.21.28）：`verify` 跑的是「调参切片」** —— 协议常量 `holdout_from`（`>= 切点` 的记忆）是**留出集**，默认路径按设计**排除**它 ⇒ **留出集在调参期间不会被读**（结构上排除，不靠自觉）。
  要出**报告口径**的读数用 `node tools/retrieval-eval.ts --holdout-only --json`（它**不在 `verify` 里**，就是为此）；
  录基线必须**显式**声明 `--corpus-role <live-workspace|frozen-snapshot>`（基线是「报告口径」的锚点，不声明就无法判断它是不是拿调参语料录的）。
- **`dist/` 出现在 `git status` 里 ⇒ 说明 ignore 坏了**（v1.20.6 起它应被忽略）。若误 `git add`，
  `.gitattributes` 仍钉 `dist/** text eol=lf`，避免 Windows 下 CRLF 假脏。
- 源码入口：`index.ts`（Cordis adapter）→ tsc → `dist/index.js`（DSH 加载编译后 JS）。
- **改默认值时跑一次 `npm run sweep:timebomb`**（T12 的可复现探针，`tools/timebomb-sweep.ts`）——
  凡改 `forget` / `retention` / `compact` 这类**默认值**（不是改代码）的变更，都要重跑「假日期 × 全部测试」这道判定：
  v1.15.43 的判定前提被 v1.15.85「默认全开」悄悄翻掉、没人重跑，直到 v1.15.98 才发现 6 个测试到期必炸。
  分钟级，**刻意不进 `verify`**（`verify` 要的是快速缝合线）。
- **目录级 sidecar 的规模与耗时**：`npm run sidecar:cost -- <工作区根>`（= `tools/sidecar-cost.ts`，`T9` ② 的读数来源）。
  它**驱动产品面**（`createShadowCollector(...).ensureIndex(ws)` ⇒ 真实 `writeAbstracts`），**读真实语料、写全部重定向到临时沙箱**
  （跑完自检「写越界 0 次 / 语料文件数未变」，两个口径各用独立沙箱）。同样**刻意不进 `verify`**（要真语料）——
  改了 `abstracts` / `showInIndex` / Episode 收口（`compact`）之后重跑它。
- **派生索引（SQLite）的三个基准数字 + 二进程并发**：`npm run bench:derived-index -- [--atoms 8800]`
  （= `tools/derived-index-bench.ts`，`T17-C` 矩阵第 8/9 项的读数来源）。它**自己造合成语料**（不进仓库、不碰真实记忆），
  在真临时目录上驱动**真 provider**，报 cold rebuild / startup / incremental 三个数字，并起两个子进程做同机并发探针。
  **刻意不进 `verify`**（要造 8800 个文件）；改了 `core/candidate/sqlite.ts` 或索引新鲜度逻辑之后重跑它。
  ⚠ 并发读数**时序相关**（同一份代码两次运行可能一次撞锁一次不撞）⇒ **别拿单次运行当结论**（见 `adr/0095` 补记）。
- **棘轮基线的录制与「已确认的收缩」**（`v1.21.33`）：`node tools/audit-wiring.ts . --update-ratchet`（`drift` 侧同理）
  会先过 **V7 语料健康门**：`files < 基线×0.9` / `dirs` 骤降 / **线索跌 > 20%** 任一条 ⇒ `PARTIAL` ⇒
  **拒绝录基线**（防「工具坏了 → 线索骤降 → 被判成修好了 → 写进基线」这条自我固化路径）。
  若那份骤降**确实是人删出来的**（例：`T26` 删掉零消费者的 `decision/` 整层，A 段线索 33 → 25），
  用 **`--confirm-shrink "<一句人话理由>"`** 显式确认：判据在 `tools/corpus-health.lib.ts` 的 `shrinkConfirmVerdict`
  （**只**放开「文件面健康 + 哨兵齐 + 只有线索面骤降」这一种），理由与前后数字写进基线的 `confirmed_shrinks`。
  ⚠ **它不是万能开关**：`EMPTY` / 哨兵缺失 / **文件面**骤降一律拒绝确认；**编不出理由就说明还没查清**。

## 脚本一律 TypeScript

- 临时探针、一次性脚本、取证脚本**一律写 `.ts`**，用 `node x.ts` 直跑（原生 type-stripping）。
  **由来**：`adr/0064`（v1.15.22，用户指令「所有的 js 脚本 mjs 脚本必须全部切换到 ts」）—— 那一次把
  `tools/*.mjs` 与 `test/replay-*.mjs` 迁成 `.ts`，并**顺手**把 `_research/` 下 6 个**在跑的**探针一并改了扩展名
  （同族但不在清单上的也一起改，是这条能站住的唯一方式）。**门**：`npm run audit:scripts`（v1.15.87，在 `verify` 里）
  —— 它只答「有没有」，答不了「`.ts` 里写的是不是 TypeScript」。
- **一次性脚本用完就删，别囤（v1.15.87 立的判据）**：会**改写**文档 / 源码的脚本（`upd_*` / `gen_*` / `fix_block`
  那一类）在它那一版发完之后**只剩余危险** —— 再跑一次就等于**拿当时的脚本改写现在的文档**。
  只读的探针按「**能复现的放 `tools/`**；**当时取证的放 `../.docs/fix/<日期>/`**」分流（分流记在该目录的入口文件里）。
- **禁止 `.js` / `.mjs` / `.cjs`**（`dist/` 是编译产物，除外）：仓库内不保留手写 `.js` ——
  **已经存在的一律迁移成 `.ts`**。迁移口径（v1.15.85 立）：**直接改名即可** —— 本包 `package.json` 是
  `"type": "module"`，Node 原生剥类型能直跑 `.ts`（ESM 写法、`node:*` 导入、顶层 await **全部原样可用**）；
  只需留意**互相 import 时把 `.mjs` 说明符一并改成 `.ts`**（本仓那次迁 19 个文件，**0 处**需要改说明符）。
  **门**：`npm run audit:scripts`（已进 `verify`）。
- **两类「js」别混**（v1.20.9 后补钉，避免再被改写脚本误伤）：
  | 形态 | 是不是「手写 js」 | 怎么办 |
  |---|---|---|
  | 仓库里的 `.js` / `.mjs` / `.cjs` **文件**（`dist/` 除外） | **是** ⇒ 禁 | 改成 `.ts`；门抓 |
  | 源码里的 `from "./foo.js"` **说明符**（文件实际是 `foo.ts`） | **不是** | 产品面（`core/`/`query/`/… → `tsc` → `dist/`）**必须**写 `.js`（TS ESM 发射约定；宿主加载的是 `dist/*.js`）。`tools/` / 直跑的 `node x.ts` 互引写 **`.ts`** |
- **Node 运行时：永远 ≥ 26**（本机用 mise：[`mise.toml`](./mise.toml) 钉 `node = "26"`；`package.json` `engines.node: ">=26"`）。
  ⚠ Cursor 自带 helper 可能仍是 Node 22 —— **在本仓跑命令前先确认** `node -v` 以 `v26.` 开头
  （`mise` shim 在 PATH 里应优先于 Cursor helper）。
- **允许** `.py` / `.ps1` 等**其它脚本语言**（用户 2026-09-15 立的边界）：**不要求**移植成 `.ts`。
  但**下一条的「怎么跑」照旧适用** —— 判据是「复审者能不能重放」，不是扩展名。
- 取证/探针脚本放 `../.docs/fix/<年-月-日>/`，**并在文档里写明怎么跑**（复审者要能重放）。
- **证据的入口**：`../.docs/fix/<年-月-日>/` 下的**入口文件**（例：`2026-09-12/meta-review-four-rounds.md`）——
  先看它：**若干条可重放命令** + 分类对照 + 未做清单（⚠ **不写条数**：它在每轮增长，v1.15.75 这里还写着「六条」
  而实际已九条以上；**文件名保留 `four-rounds` 但内容已更新到十轮**，因为改名会让引用腐烂），再按里面的命令名找对应探针。
  ⚠ **这个目录在仓库之外、不受版本控制** ⇒ **换机 / 新克隆就没有它**（v1.15.83 实测：本机只有 `2026-09-14`、`2026-09-15`，
  而本节原先写死指向的 `2026-09-12` **不在**；`CHANGELOG` / `adr/*` 里若干「对账探针」路径同理）。
  ⇒ **别猜路径**，找本机最近一份入口：
  `Get-ChildItem ../.docs/fix -Directory | Sort-Object Name -Descending | Select-Object -First 1`
  ⇒ 由此出一条判据：**能复现的东西放 `tools/`**（进版本控制、进 `verify`；如 `tools/contract-surface.selftest.ts`），
  `.docs` 只放**当时**的取证脚本 —— 否则复审者拿到的是一串**只有我这台机器**才存在的路径。
  **任何一次「审查/修复」类工作都应在该阶段结束时更新或新增这份入口** ——
  否则证据散在 `CHANGELOG`/ADR 里，复审者找不到它（v1.15.69 实测：前四轮就是这样，**没人能看到怎么重放**）。

## 材料（`vendor/_src`）的 git：**访问前先查最新**（用户 2026-09-20 立）

**规则**：**每次访问材料之前，先跑一次新鲜度检查**（只读，一次 `git ls-remote`/仓）：

```powershell
node tools/materials-freshness.ts          # 谁落后、落后到哪个 sha、以及**爆炸半径**
```

**别为 24 个仓付网费**（v1.18.3 补：因为**全量实查太慢**，规则会被绕过 —— 而规则被绕过比没有规则更坏）：

```powershell
node tools/materials-freshness.ts --only <目录名>   # 典型场景：你接下来只访问**某一个**材料（~1s）
node tools/materials-freshness.ts                  # 全部（**命中缓存 ~0.7s**；实查 ~25s）
node tools/materials-freshness.ts --max-age 0      # 强制实查（不信缓存就用它）
```

- **实查 24 个仓 ≈ 25s**；**调并发没用**（实测 12 并发与串行差不多，单次往返才 819ms ⇒ 对端会**限流并发小请求**）。
- ⇒ 有**带 TTL 的缓存**（默认 `600s`，落在**系统临时目录**，**不污染工作区**）：同一会话里第二次起 **~0.7s**。
  缓存**不改变判定语义**：过期重查、`--max-age 0` 强制实查；读不到/坏件 ⇒ 当作**没有缓存**（重查），
  **绝不当作最新**（ADR-0049）。
- ⇒ **要访问单个材料就用 `--only`**，别为了一个仓去问 24 个。

**为什么**：材料是**外部世界**的快照。不查就会拿**旧版**下结论 —— 而本仓有多份 ADR / `references.md`
建立在**具体行号**上（「行号是最易腐烂的引用形态」，见下一节）。

**⚠ 「更新到最新」有两个意思，别混**：

| 做法 | 效果 | 代价 |
|---|---|---|
| `node tools/materials-freshness.ts --update` | **只把 HEAD 移到最新**（`git reset --mixed`） | **工作树不动** ⇒ **读文件读到的仍是旧内容**；台账只能说「上游现在是哪个 sha」 |
| `... --update --hard` | **连工作树一起更新**（`git reset --hard`） | **覆盖 vendored 拷贝** —— 本机的裁剪/改动会丢；**未跟踪文件不会被清**（工具**不做** `git clean`） |

⇒ 默认走**前者**（非破坏）；**要真读到最新内容才用 `--hard`**，且先看清它要覆盖多少处（工具会先把差异数打出来）。

**三条硬边界**：

1. **冻结快照不动**：目录名带 `--snapshot-<版本>` 的（如 `langextract--snapshot-v1.6.0`）**按设计**停在那版；
   更新它＝**它不再是那个快照** ⇒ 必须 `--include-frozen` **显式**要求。
2. **先看爆炸半径，再动**：工具会列出「哪些文档里有该材料的 `文件:行号` 引用」；
   **更新后必须回头核对那些引用**。⇒ 更新与「改代码后回头核对文档」是**同一条纪律**（见下一节）。
   ⚠ **门抓不住这一半**：`audit:docs` 检查⑥ 只判「**越界**」（引用区间超出该文件现在的行数）。
   实测（v1.18.4）：`openclaw` 更新到 `052d26ee` 后 `memory-builtin.md` 整体**下移 3 行**，
   `adr/0095` 引的 `:249` / `:259` **一个都没越界、却全部失配** ⇒ 这类只能人工回头核。
   因此**引用外部材料的行号，必须同时写下当时 pin 的 sha**（见「引用纪律」末条）。
3. **它不进 `verify`**：因为**要联网**。本仓的门**刻意不联网**（同族：`audit:docs` 检查⑤ 的 tag 核对照样
   用读 ref 文件实现）。把要联网的东西塞进 `verify` ⇒ 离线的「全绿」会变成**假红/假绿**。

**更新之后**：重跑 `node tools/materials-ledger.ts`（`MATERIALS.md` §1.1 的「工作树 vs HEAD」列会变），
并把**旧→新 sha** 记进当次 `CHANGELOG`。

## 引用纪律（v1.15.65 立）

本仓的核心纪律之一是「每条断言带 `文件:行号`」。代价是**行号是最易腐烂的引用形态**：
v1.15.65 只改了 6 个文件，就产生 **≥5 处**过期行号（`core/writer/core.ts:69`、
`core/writer/materialize.ts:161` 等），散落在 `README` / `BACKLOG` / `adr/*` 里。

### 历史文档 vs 当前态文档（**先分清楚，再谈修**）

| 层 | 行号的语义 | 过期了怎么办 |
|---|---|---|
| `CHANGELOG.md` | **该版本当时**的行号 | **不改**。它是归档，改写 = **伪造历史** |
| **已冻结**的 ADR（正文） | 冻结时的行号（正文常自带「冻结时」字样） | **不改正文**，写**补记**（如 `adr/0049` 的补记） |
| `README.md` / `docs/maintainers.md` / `BACKLOG.md` / `CONTEXT.md` / `MATERIALS.md` / `AGENTS.md` / **未冻结** ADR | **当前**代码的行号 | **必须修**（或改成符号引用） |

⇒ 上一轮我把 `CHANGELOG` 里的一处计数（「169」）改掉是对的（那是**计数错误**，不是行号）；
但**不要**去「批量刷新全仓行号」—— 那会把归档层一起改坏。**只修当前态文档。**

### 四条操作要求（v1.18.4 补第 4 条）

- **优先引符号名**（函数名 / 常量名 / 配置键），行号只在必要时加，且写成
  「`core/writer/core.ts` 的 `episodeShow`」这种**不依赖行号也能定位**的形式。
- **改完代码，回头核对引用它的文档**：对每个改过的文件跑一次
  `Select-String -Path *.md,adr\*.md -Pattern '<文件名>:\d+'`，只处理**当前态**文档里那些。
  跑一次 `node tools/citation-audit.ts .` 更快：它把「**越界**」这一类直接判掉
  （检查⑥；`--verbose` 还会列出所有**未判定**的引用，那才是要人工看的部分）。
- **数字要带范围与时刻**：「`core/` 下当时 25 处」而不是「本仓 25 处」——
  否则它会在下一次改动后变成一句无人能复核的断言。
- **引外部材料的行号，必须同时写下当时 pin 的 `sha`**（v1.18.4 立）。
  由**实测**而来：把 `openclaw` 从 `c1c870a4` 更新到 `052d26ee` 后，`adr/0095` 正文引的
  `memory-builtin.md:249` / `:259` **全部失配**（该文件整体**下移 3 行**）——
  而在此之前**没有任何机器或文字能回答「这条断言是建立在这个材料的哪个版本上」**。
  写法：`<材料>@<sha>`（或 `@<tag>`，如冻结快照用 tag）+ 行号，例
  `openclaw@052d26ee 的 docs/concepts/memory-builtin.md:252`。
  **为什么必须写**：材料是**外部世界**，它会动；只写行号 = 一条**无法复核、也无法判定过期**的断言
  （检查⑥ 抓不到这类 —— 它只判越界，见「材料」一节硬边界 2）。

### 每份文档「owned by」什么（免得同一件事写五遍）

| 文档 | 它**拥有**的层 | 它**不该**承担 |
|---|---|---|
| `README.md` | **当前状态** + 面向使用者的表（如「默认开关」表） | 决策理由；维护者百科 |
| `docs/maintainers.md` | **维护者面**（契约台账、模块 Owns、能力长节、可选 CLI、投影预设） | 使用者入口；默认开关表（仍在 README，门③） |
| `adr/*.md` | **决策与理由** + 判据 + 缺陷表（权威） | 状态流水账 |
| `BACKLOG.md` | **状态与指向** | 复制 ADR 的表（应写「见 `adr/00xx` §n」） |
| `CHANGELOG.md` | **每个版本的历史归档**（含当时的数字） | 当前状态 |
| `../.docs/fix/<日期>/` | **证据**（链路矩阵 / 正反对照 / 怎么重放） | 决策与状态 |

同一条事实出现在多层时，**每层只回答它自己那个问题**，而不是互相复制同一段文字。

> **报告产物（`docs/*.html` / `docs/*.candidate.json` 一类）**：`adr/0093` §1 登记的证据 —— **留仓，但不进发布面**
> （`package.json` 的 `files` 只发 `docs/*.md`，v1.21.8 起）。两条实测出来的坑：
> ① **npm 的 `files` 白名单无视 `.gitignore`** —— 写 `"docs"` 会把被忽略的 `docs/*.visual-check.*`（截图/JSON）
> 也打进包（实测 0.96 MB）；② **引用门根本扫不到它们** —— `citation-audit.lib.ts` 的 `CITE` 正则只认
> `md|ts|mts|json|ya?ml|py|sh|ps1`，`.html` / `.png` 永远在扫描面之外 ⇒ 这类产物的过期**不会有任何门报警**，
> 只能靠人 / ADR 级决定（v1.21.8 实测：包 1.8 MB → 621.5 kB）。
>
> **归档层只有一份（v1.21.15 起）**：`CHANGELOG.md` 的历史条目与**全部历史 tag** 已按用户决策清除，
> **只保留当前版本**。三条要知道的：① **净收益为 0** —— 该文件**不进 npm 发布包**，删它不改变包体积；
> ② **门 ⑤ 因此失去判别对象**（`past = 0`：它仍绿，但不再回答「过去的版本有没有漏打 tag」）——
> 与本仓记过的「门绿着、判别力没了」同型；③ **旧文本与旧 tag 仍在 git 提交史里**，仓外另存快照于
> `../.docs/fix/2026-09-25/`。⇒ 写新条目时**不要再断言「历史在哪条版本里」** —— 那些版本号已无对应条目。
>
> **由此立的常设口径（v1.21.16 起）**：`CHANGELOG.md` **每版覆盖、不追加**（本文件**永远只有一份**）；
> **tag 每版只留一个**（删旧建新）。⇒ 发版动作相应地变成：改版本三处 → **覆盖** CHANGELOG → 提交 →
> **删旧 tag** → 打新 tag → 推 `main` + 新 tag。⚠ **代价**：git 提交史之外**没有历史版本可查**，
> 且门 ⑤ 永远在「0 个已过去的版本」上通过（**绿而无判别对象**）—— 这是被明确选择的形态。

## 判据收一处 / 修一类而不是修一条

- 同一个判据（比较点扫描、路径分类、默认值回落、降级是否可见）**只能有一份实现**。
  本仓已有两次代价明确的教训：`tools/comparison-points.lib.ts`（两个审计工具各写一份正则）、
  `core/util.ts:numOr`（默认值回落散在三处）。
- **修一类缺陷时要扫同类 `catch`**，不是只修清单上那几条 —— v1.15.65 就因为在
  `writeAbstracts` 旁边漏看了一个同族 `catch` 而返工。

## 改长文档的四条规矩（v1.15.67 立三条、v1.15.72 补第四条，**因为差点丢掉 34 个版本条目**）

事故经过（锚点命中同名标题 ⇒ 删掉 34 个版本条目、`Set-Content` 零报错、靠数不变量才发现）
见 **`adr/0085` §8.7**；此处只留规矩。

| # | 规矩 | 为什么 |
|---|---|---|
| 1 | **先算行区间，只在区间内定位锚点**。**绝不**用 `indexOf` / `Replace` 在全篇找「看起来一样」的标题 | 长文档里同名小节极多（`### 6. 验证` 在本仓出现过几十次），全篇查找等于**赌** |
| 2 | **`Replace` 必须打印匹配数，0 就拒绝写盘** | PowerShell 的 `.Replace()` **没有匹配也不报错**，静默返回原文。本轮已有 **≥3 次**「以为改了、其实没改」（一次导致条目里整段内容没写进去） |
| 3 | **改完先核对一个可数的结构不变量**（标题数 / 表格行数 / 章节数），再提交 | 「命令成功」不等于「改动按意图发生」。本仓每道门都在打印数字，**文档改写也该如此** |
| 4 | **在长文档顶部插入新条目时，别把下一条的标题当 `old_string` 锚点** —— 若这么用了，`new_string` **必须把那个标题原样再写一遍** | `edit` 要求 `old_string` **恰好出现一次**，于是「锚点」自然落在下一条的标题行上；而 `new_string` 是在它**前面**插入，**忘了重新写回就等于删掉了那个标题**。**v1.15.71 与 v1.15.72 各犯一次**（都是给 `CHANGELOG` 加新条目时删掉了上一条的标题），两次都靠**数 `## [v` 标题数**才发现（应为 109/110，实为 108/109）。**更稳的锚点 = 上一段的结尾空行或一句话**，不是下一个标题 |

**优先用 `edit` 工具做精确替换**（它要求 `old_string` **恰好出现一次**，天然满足规矩 1）；
只有批量改写才用脚本，且必须满足规矩 2、3。

## 「能推出来的字段不要手写」+ `audit:docs` 门

**判据**：任何能由其他数据推出来的字段 —— **版本号 / 计数 / 链路清单** ——
**要么别手写，要么配一道门**。事故经过（实测数据、三个字段各自烂了多久）见 **`adr/0085` §8.7/§8.8**，此处只留规矩。

**`npm run audit:docs`**（已进 `verify`）查下面这几条 ——
⚠ **本行故意不写条数**：v1.15.75 加了第 4 条检查，而这里写着「查三条」、工具自己的文件头写着「两条检查」——
**同一件事三个地方三个数**，而**没有任何门在守它**。
⇒ 按本文件的规矩「**最好只给规则与命令、不写数**」：**条数 = 下表行数，以下表为准。**

| # | 检查 | 退出码 |
|---|---|---|
| ① | **三方版本一致**：`package.json.version` / `README` **行首粗体**的「当前版本」行 / `CHANGELOG` 首个 `## [vX.Y.Z]` | 0 / 1 / 2 |
| ② | **`verify` 与两块文档**：每一步都被 `README` 的「改代码后先过闸门」代码块与 `AGENTS.md` 链路行点名（正向），**且**那两块点名的每个 `npm run` 都在 `verify` 里（**反向**） | 0 / 1 / 2 |
| ③ | **声明计数 = 实际计数**：`README` 默认开关表声明的「现 N 行」必须等于实际数据行数 | 0 / 1 / 2 |
| ④ | **README 当前版本行不得复制 `CHANGELOG` 条目**：两者之间不得有 **≥40 字**的连续逐字重复（v1.15.75 补；同一处缺陷**第 4 次**复发才立的门）。**只查当前版本那一行** —— 历史行是归档叙述，用门要求重写历史行 = 用门改写归档 | 0 / 1 / 2 |
| ⑤ | **已经过去的版本必须都打过 tag**：`CHANGELOG` 里 ≥ 仪式起点（`v1.15.77`）**且版本号 < 当前版本**的每一条，都必须存在同名 tag；缺一个就红（v1.15.82 补）。**当前版本豁免**，读不到 `.git` 报结构缺失（**不是「通过」**） | 0 / 1 / 2 |
| ⑥ | **`文件:行号` 引用不得越界**：当前态文档（根目录 `*.md`（除 `CHANGELOG`）· `docs/*.md` · **未冻结** ADR）里的引用，凡目标**能唯一解析**的，其行号区间必须落在该文件**现在的行数**之内；越界即红（v1.18.4 补；`docs/*.md` 于 v1.21.0 纳入，因 README 拆出 `docs/maintainers.md`）。**归档层（`CHANGELOG`）与冻结 ADR 豁免**；同名多个 / 找不到 ⇒ 计入「未判定」，**绝不挑一个来判** | 0 / 1 / 2 |
| ⑦ | **三处「验证基线」必须等于 `package.json` 的 `engines.dsh`**：`README` 的 `\| 验证基线 \|` 行（含内联码）、`README` 的 `\| 声明 \|` 行、`CONTEXT` 的「验证基线」术语行（v1.20.2 补 —— **由来**：`0706f4c` 抬了 `engines.dsh` 却**只改代码**，三处文档留在旧值而 `verify` 全绿）。读不到 `engines.dsh` 或 `README` 缺基线表 ⇒ 结构缺失（**不是「通过」**）。**刻意窄**：不判正文里对历史基线的追述（「为什么从 A 抬到 B」那段本就该含旧版本号），也不判 `presets/README` 的「(current as of X)」这类**上游状态**叙述 | 0 / 1 / 2 |

- **② 的反向一半是必须的**：只有正向时，「门被从 `verify` 里摘掉」反而让检查更宽松、照样全绿，
  而文档里那一行变成**死指针**。
- **③ 的由来**：v1.15.67 只给**链路清单**配了门，**计数**只立了规则没落地 ⇒ 一天内就发现两处在腐烂。
  它只覆盖**这一处**计数（结构稳定、可机械化）；其余计数（如 `BACKLOG` 的表头）用「**判据 + 命令**」代替写死。
- **⑤ 的由来与能力边界（v1.15.82 补，用这道门之前必须读这两段）**：
  tag 这半边连续两次是**用户提醒驱动**的（v1.15.77/78 属补打、v1.15.80 与 v1.15.81 都由用户先说「tag」）⇒
  按本文件那句「**要么写进清单、要么配一道门**」配了门（与前两条门同源：都诞生于「一处手工纪律反复失效」）。
  **边界一（当前版本豁免，结构决定的、不是偷懒）**：tag 要指向**该版本的发布提交**，
  而那笔提交只有在版本号改完之后才建得出来，而本门跑在 `verify` 里 = **提交之前** ——
  若要求当前版本也必须有 tag，**每次发版都会在提交前误红**。⇒ 本门实际回答的是
  「**已经过去的版本有没有漏打 tag**」，**代价是迟一版发现**（漏打会在下一次发版时被抓住），
  换来的是不会再出现 v1.15.4…v1.15.76 那样**攒到 73 个版本**才发现。
  **边界二（它刻意不做的两件事）**：**不查远端**有没有这个 tag（门里不联网）、
  **不查** tag 指向哪个提交（那要读对象库）⇒ 发版时那句 `git rev-parse --short vX.Y.Z` 仍是**人工核对**的一步。
  **边界三（缺件不静默）**：读不到工作副本的 `.git` ⇒ 报**结构缺失(2)**，**不是「通过」**（ADR-0049）——
  它用**读 ref 文件**（松散 ref + `packed-refs`）实现，刻意不 spawn `git`。
- **⑥ 的由来与能力边界（v1.18.4 补）**：由**实测**而来 —— `adr/0081`（**未冻结** ⇒ 属「必须修」层）
  引 `reflection/patterns/success-rate.ts` 的 **`:1-29`**，而该文件实际只有 **27** 行（已改成**符号引用**）。
  ⚠ 举例时**故意把路径与行号拆开写**（`…success-rate.ts` 的 `:1-29`）：贴成一条完整引用会被 ⑥ 读成**活引用**而报越界 ——
  **精确原形留在归档层的 `CHANGELOG`**（那儿不在门的作用范围内）。
  判据**只有一条、故意窄**：**只判「越界」**。它**不判**「行号存在、但那一行已经不是原来那句话」
  （那要读语义），也**不判外部材料的内容位移**（那要先有 pin）。
  ⚠ 两个更宽的判据都被**测量否决**：把「引用后的引文必须出现在被引行上」当判据 ⇒ 实测 **139 处「不符」、
  几乎全是误报**；收紧成只认 `「…」` 后 ⇒ **误报 0、覆盖也 0**（带引文的引用全在冻结 ADR 与外部材料上）。
  **经过与数据写在 `tools/citation-audit.lib.ts` 的文件头 —— 想加判据之前先读它。**
  与 ⑤ 同源：都诞生于「一处手工纪律反复失效」（行号腐烂是本仓第 N 次），都由本文件的
  「要么写进清单、要么配一道门」推出来。
- ⚠ **这些检查都只是必要条件**：答「有没有 / 一致不一致」，答不了「顺序与描述对不对」⇒ **不得据它宣称文档正确**。
- **发版时三处一起改**：`package.json` 的 `version` · `README` 的「当前版本」行 · `CHANGELOG` 新条目。
- **末条（v1.15.69 的血教训）：在「修计数腐烂」的同一段里，我又手写了一个错的计数。**
  `BACKLOG` 表头那处修计数时写了「✅ 3 个 ⇒ 现存 10 条」，而真值是 **✅ 4 个（漏了刚结案的 `T8`）⇒ 现存 9 条**，
  13−3=10 的算术跟着错，**而它自己列的名字只有 9 个**（数字与列表互相矛盾）。**`audit:docs` 抓不到它**——
  检查③只管 `README` 的表格行数。⇒ **两条**：
  **① 「判据 + 命令」比「写死的数」好，但最好的是「只给规则与命令、不写数」**（写数就得负责它下次还对）；
  **② 写了数就当场用命令核一遍并写下口径**（本例：按**标记**算 9 条，按**实质**算可能 8 条——`T3` 未带 ✅ 但正文称已分诊）。
- 本仓工作副本是 **CRLF**：写解析代码前**先归一化行尾**（`\r` 会让 `$` 锚定的正则失效 ——
  v1.15.68 的检查③就因此报过「声明 18 / 实际 19」的**假警报**，差一点把对的文档改错）。
- **推导出的数分两类，待遇不同（v1.15.71 立，因为我把两类弄反了）**：

  | 类型 | 例子 | 有人要改它时怎么判 |
  |---|---|---|
  | **受门保护的数** | `mode` 总数 **62**（`test/recall-envelope.test.ts:104` 断言） | **先假定它对**。要改它必须**同时改门**并说明门为什么错 —— **不许拿一个探针去推翻它** |
  | **没有门的数** | 探针/脚本现算出来的任何计数 | 改它不必连带改别处，但**写进文档必须带口径与命令** |

  **事故（v1.15.70）**：我用一个没有门的探针算出 **61**，据此把 `CONTEXT.md` 里**受门保护的 62** 标成
  「**未复核的旧测量、来源不可考**」，并把 61 写进了 `README`（当前态层）—— **方向正好反了**。
  根因：探针两条判等形态写死成 `===`，漏了 `query/planning.ts:15` 的 `!== "plan"`（详见 `adr/0086` §8.5）。
  ⇒ **「来源不可考」这句话本身要有证据：说它之前先 `grep` 这个数在仓库里是否已被某道门断言。**
  ⇒ 报「实测」时，**探针的实现**与**被测量那一侧的写法**都要看过（我当时只看了前者）。
