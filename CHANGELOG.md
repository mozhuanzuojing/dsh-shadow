# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **本文件自 v1.21.15 起只保留一份（当前版本）**：历史条目与全部历史 tag 已按用户 2026-09-25 决策清除；
> 且**由此立为常设口径** —— 每版**覆盖**本文件、tag **只留一个**（见 `AGENTS.md`）。

## [v1.21.16] `D2` 设计定稿：跨项目根注册（`adr/0108` 草案）

**决策记录面；无产品行为改动**。本条**只立设计、未实现** —— 实现前不得引用为「已落地」。

### 本版做了什么
- 新增 **`adr/0108`**：把 `D2`「跨项目根注册 + 可信度设计」落成可实现的形态 ——
  - **配置面** `tools/trusted-roots.json`（与 `tools/*.json` 同类：进版本控制、**不进发布面**）；
  - **解析顺序**加一档「已注册根，**唯一命中才算**」（多根同名 ⇒ **仍不判**；根不可用 ⇒ **未判定 + 点名根**）；
  - **授权模型**：`addedBy` **只接受 `human`** —— **agent 不得自行加根**；
    否则「被判者自己控制证据面」，等于自己给自己发合格证（与 `ADR-0059`「不猜」同源）；
  - **消费方点名**：只有 `--include-archive` 与门 ⑥ 两处（`ADR-0087`：**不建没有消费方的机制**）；
  - **完成判据 + 标定要求**（正 / 负 / 控制变量）写在该 ADR §5。
- `BACKLOG` 的 `D2` 由「待设计」改为「**已设计、待实现**」。

### 立了一条常设口径（承接上一版的历史清除）
`CHANGELOG.md` **每版覆盖、不追加**（本文件永远只有一份）；**tag 每版只留一个**（删旧建新）。
⇒ 本版即按此执行：`v1.21.15` 的 tag 已删除，只保留 `v1.21.16`。

### 当前状态（这就是「最新的一份」）
dsh-shadow 是一个 **DSH 记忆插件**：把一件事的来龙去脉写成工作区里的 markdown，换会话后可按主题召回，
并回答「它还算不算数」—— 不让你「相信」记忆，而是让你和 agent 能**核查**记忆。

- **读侧**：`shadow_query` · `read_shadow`（含 `mode:` 家族）· `recall_shadow`。
- **写侧**：`.shadow/atoms` 落盘 + 保留期 / 失效 / 证据等级。
- **治理**：ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · 分层方向门 · 脚本语言门。
- **唯一验证入口**：`npm run verify`（步骤清单见 `README.md`；语料根由 `SHADOW_EVAL_ROOT` 指定）。
- **宿主原生能力**：`dsh 原生 browser-use` / `dsh 原生 computer-use`（见 `adr/0089`；本仓**不引入**第三方浏览器依赖）。
- **本次会话（2026-09-25）已闭合**：`T12` 时间炸弹 · `V10` rc.2 消费面（主问）· `D3` 不做 ·
  `D1` 删除 `ChangeSet`/`invalidateFor`（**含代价记录**）· `D12` 归档层去数 · 历史清除（`P0` 快照闭环）·
  `D2` 设计定稿（本版）。
- **未做**：`D2` 的**实现**（须按 `adr/0108` §5 的判据与标定）；`BACKLOG` 的 `P1`（历史清除的悬空引用余波）。
