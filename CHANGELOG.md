# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **本文件自 v1.21.15 起只保留一份**：**每版覆盖、不追加**；tag **每版只留一个**（删旧建新）—— 口径见 `AGENTS.md`。

## [v1.21.17] `D2` 实现：可信根注册（`adr/0108` §5 三条完成、两条列后续）

**工具面；无产品行为改动**（注册表为空时，判定行为与实现前**逐项相同**）。

### 做了什么
- 新增 `tools/trusted-roots.lib.ts`：注册表**读取 + 校验 + 解析**。三条纪律写死在代码里：
  ① `addedBy` **只接受 `human`**（agent 不得自行加根）· ② 坏件**不静默**（逐条报问题、该条不入表）· ③ **唯一命中才算**（多根同名**仍不判**）。
- `tools/citation-audit.lib.ts` 的 **`judge`**（唯一判据实现）加**第四档**解析：
  本仓 → `_src` → 裸文件名兜底 → **已注册根**；新增三种「未判定」理由并**点名根**：多根同名 / **根不可用** / 都找不到。
- 新增 `tools/trusted-roots.json`（**空注册表** —— 形状即文档）。
- **标定 +5 条（共 16 条）**：⑫ 唯一命中可判定 · ⑬ 多根同名仍不判 · ⑭ 根不可用必须点名 · ⑮ scope 外不解析 · ⑯ 授权违规不静默。

### 一个必须先说清的读数（**是上一版清历史的账，不是本次代码**）
归档层引用较「清历史前」大幅下降 —— `CHANGELOG.md` 从 8000+ 行砍到 39 行，那些条目里的引用一并消失：
现读数 **引用 322 · 可判定 191 · 未判定 131 · 越界 0**。
**本次代码在注册表为空时的影响 = 0**；机制的效力由 ⑫–⑯ 五条标定证明。

### ⚠ 要生效必须由**人**填根（我不能替你做）
`adr/0108` §2.3 写死：**只有人能加根**。我实现机制，但**不会**把任何根标成可信。
⇒ 需你在 `tools/trusted-roots.json` 里加条目；在那之前，未判定数**一处都不会变**。

### 未做（如实列出）
- **`adr/0086` 的十字段登记未写入**（§5.5）：该表有固定列结构，须人工确认列对齐。
- **真实外部根的实测未做**：注册表为空且我不能自己加根 ⇒ 「未判定下降」这条完成判据**当前无法演示**。

### 当前状态
dsh-shadow 是 DSH 记忆插件：读侧 `shadow_query` / `read_shadow` / `recall_shadow`；写侧 `.shadow/atoms` + 保留期 / 失效 / 证据等级；
治理 = ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · 分层方向门 · 脚本语言门；唯一验证入口 `npm run verify`（语料根由 `SHADOW_EVAL_ROOT` 指定）；
宿主原生能力经 `adr/0089`。
- **本次会话（2026-09-25）已闭合**：`T12` · `V10`（主问）· `D3` · `D1` · `D12` · 历史清除 · `D2` 机制实现（本版）。
- **待你**：`D2` 填根 · `P1` 悬空引用余波（三个收口）。
