# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **本文件自 v1.21.15 起只保留一份**：**每版覆盖、不追加**；tag **每版只留一个**（删旧建新）—— 口径见 `AGENTS.md`。

## [v1.21.19] `T2` 首批分诊：B 类线索 `93` 条（台账原写 85，已更正）+ 授权口径记入台账

**台账与判定面；无产品行为改动**。

### 本版做了什么
- **`T2` 首批分诊**（按本仓纪律：`不得凭静态分析定罪`）——把实测 `93` 条按「**值从哪来**」分成四类：
  **① 外部数据（宿主载荷 / 工具入参）`43` 条 ⇒ 分支可达、非缺陷**（`mode=*` 占 37 条；证据：`const mode = String(args?.mode || "")`）·
  **② 工具自身常量 / 夹具 `22` 条 ⇒ 误报形态**（全在 `tools/` 下，驱动来自 argv / 夹具）·
  **③ 写侧用常量、工具按字面量匹配 `2` 条 ⇒ 误报（已知窗口盲区）**（`status=archived|superseded`）·
  **④ 待逐条核 `26` 条**（**如实列出，不冒充已判**）。
- **更正了一处过期读数**：`T2` 标题原写「85 条」，实测 **93 条** ⇒ 工具读数随语料漂移，台账写死的计数**会过期**。
  这正是本仓「能推出来的字段不要手写」的同族 —— 标题已改为**带「实测 + 复现命令」**的形态。
- **授权口径记入台账**（本仓既有惯例：`D4`/`T19` 都是「用户『按推荐』授权」）：
  用户 2026-09-25 明确「**全部开始，不用经过我**」⇒ 此后**逐项的 triage/收口由我按推荐决定并落账**，
  **不再逐项等拍板**；⚠ 但**仍有两条我不会自作主张**：① `D2` 的**填根**（`adr/0108` §2.3：只有人能加根）·
  ② 任何**不可逆且无快照**的删除。

### 当前状态
dsh-shadow 是 DSH 记忆插件：读侧 `shadow_query` / `read_shadow` / `recall_shadow`；写侧 `.shadow/atoms` + 保留期 / 失效 / 证据等级；
治理 = ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · 分层方向门 · 脚本语言门；唯一验证入口 `npm run verify`。
- **下一批**：`T2` 的 ④ 那 `26` 条逐条定性；其后 `T6`/`T7`/`T9`/`T11`/`T13` 后半/`T17-C`/`T18`/`T21`。
- **仍等你**：`D2` 填可信根（只有人能加）· `6.3 待定语义`（**明写「不修，需先拍板」**）。
