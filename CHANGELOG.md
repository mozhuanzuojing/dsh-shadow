# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **本文件自 v1.21.15 起只保留一份**：**每版覆盖、不追加**；tag **每版只留一个**（删旧建新）—— 口径见 `AGENTS.md`。

## [v1.21.29] `T13` 后半 结案：**复杂度预算门**（硬上限 800 行 + 热点棘轮只降不升）

**工具面 + ADR + 台账面**。由来：`adr/0078` D3（hl_mem 的 `check_complexity_budget.py` + `complexity_budget.json`，
口径是「本期每个热点上限**只能下调**」）；本仓 `AGENTS.md` 自己早写着「文件小而专注（200–400 行典型，≤800）」，
但**只是散文** —— `T13` 前半只把「分层方向」做成了门。

### 1. 判据：两个**文本代理**，各自声明代理哪种风险（`adr/0109`）

| 代理 | 代理的风险 | 判据 |
|---|---|---|
| **文件行数** | 单文件过长 ⇒ **接手成本** | **硬上限 800 行** |
| **热点行数（棘轮）** | 已很长的大文件**继续膨胀** | 行数 ≥ **300** 的文件进基线，**只能下调** |

**刻意不判**：圈复杂度 / 参数数 / 函数体行数（本仓 `typescript@7.0.2` 是 native(Corsa) 移植，包根只导出 `version`
⇒ 无可用 AST，**写不了就不假装能判**）、导出数（那是接口面不是复杂度，`core/types.ts` 26 个导出会误伤）。

### 2. 棘轮三分类（**与 `audit-wiring` 的棘轮刻意不同**）

- **上涨**（已记录的热点变大）⇒ **违规**；
- **新热点**（首次跨过阈值）⇒ **记账，不算违规**（否则每加一个大文件就红 = 假闸门）；
- **退出热点**（降到阈值下 / 已删）⇒ **提示收紧基线，不算违规**（降下去是好事，不该罚）。

两处「不算违规」**必须打印**（不打印＝静默放过，ADR-0049）。与 `audit-wiring` 同形状却不同语义 ⇒ **判据不复用**：
那边的桶是**缺陷类别**（消失可能是工具坏了），这边的桶是**文件**（消失的正常解释就是改小了）。

### 3. 防假绿：**扫到的文件数**健康门

本门默认结论是「合规」—— 若根写错 / 遍历坏了，扫到 0 个文件也是「没有超限文件」⇒ **假绿**。
故基线记 `scanned_files`，本次扫到**低于其 90%** 即**拒绝出读数**（exit 2）。
（同族：`eval` 的 `min_corpus_files`、`audit-wiring` 的「生产语料为空 ⇒ 拒出 0 线索读数」。）

### 4. 先量后定（`T13` 前半的教训：表定错了门当天就红）

实测（本轮）：产品面 **219** 个 `.ts`、最长 **587** 行（`core/toolset/index.ts`）、**0** 个文件 > 800 ⇒ 硬上限有余量；
热点（≥300）**10** 个。**范围 = 产品面**；`test/`（`recall-attribution.test.ts` **4546** 行）与
`tools/`（`retrieval-eval.ts` **629** 行）**明确不判**、数字留档在 ADR —— 纳进来当天就红＝假闸门。

### 5. 落地物与接线

- `tools/audit-complexity.lib.ts`（纯判据：`hardCapVerdict` / `hotspotVerdict` / `scanHealthVerdict`）。
- `tools/audit-complexity.ts`（CLI，`--ratchet` / `--update-ratchet`；打印口径与范围）。
- `tools/audit-complexity.selftest.ts`（**四组标定**，每类判据都有负对照，含「扫到 0 个必须拒绝」）。
- `tools/audit-complexity.baseline.json`（`scanned_files: 219` + 10 个热点）。
- **进 `verify`**（插在 `audit:docs` 之后）；`README` 的闸门块 + `AGENTS.md` 的链路行**双向点名**
  （`audit:docs` 检查② 实测：**12 步**都被两块点名，且两块没有死指针）。

### 6. 验证

- `SHADOW_EVAL_ROOT=D:\project\net1 npm run verify` → **exit 0**（`run-tests` **70/70**：69 + 新增 1 个工具自检）。
- `npm run audit:docs` → ② 显示 **12 步**双向点名通过；①③④⑤⑥⑦ 全过。
- `npm run audit:ratchet` → **与基线逐桶相等**（新工具**没有**新增 B 类线索，故本轮无棘轮记账）。
- `node tools/audit-complexity.ts .` → `通过 ✅（硬上限 800 行 · 热点 10 个均未上涨 · 无新增热点）`。

### 7. 诚实标注（做不到的不假装）

- **判据是文本代理，不是真复杂度**：一个 500 行但结构清晰的编排文件与一个 500 行的面条代码被判成一样；
- **行数会因注释/格式抖动**（本仓注释密度高）⇒ 棘轮上涨时**先看是不是只加了注释**；
- **拦不住「长出一个 799 行的新文件」**（只有 >800 拦得住）；
- **范围外**（`test/`、`tools/`）不判，其数字只写在 `adr/0109` 里，会腐烂 —— 要读就现算。

### 当前状态
dsh-shadow 是 DSH 记忆插件：读侧 `shadow_query` / `read_shadow` / `recall_shadow`；写侧 `.shadow/atoms` + 保留期 / 失效 / 证据等级；
治理 = ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · **分层方向门 + 复杂度预算门** · 脚本语言门；唯一验证入口 `npm run verify`；
**发版唯一入口 `npm run release`（闸门在第一步）**；评测口径 = **dev 切片（默认）** / **留出切片（报告，显式 `--holdout-only`）**。
- **下一批**：`T17-C`（派生索引验证矩阵）→ `T18` 另半边（`decision/` 原语接线）→ `T21`（审计流被读侧消费）。
- **仍等你**：`D2` 填可信根（只有人能加）· `6.3 待定语义`（明写「不修，需先拍板」）· 一份**冻结语料**（报告级基线）。
