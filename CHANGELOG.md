# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **本文件自 v1.21.15 起只保留一份**：**每版覆盖、不追加**；tag **每版只留一个**（删旧建新）—— 口径见 `AGENTS.md`。

## [v1.21.31] `T18` / `T21` 结案 + 两条残余**拆成独立条目**（`T26` / `T27`）

**台账面；无代码改动。** 本版只做一件事：把两条「挂着但判据已满足」的条目**如实结案**，
并把它们各自的**残余**拆成**卡点明确的独立条目** —— **这是记账，不是把残余做完了**（见下）。

### 1. `T18` 结案：它的两条判据**都满足**

- 原判据：① 「若要把 `decision/` 原语也接线，**另切片**」；② `npm run verify` 全绿。
- ② 已满足（`v1.20.6` 起全绿，含 `test/decision-primitive.test.ts` 的标定）；
  ① 的措辞**本身就是「另切片」** ⇒ 本条**不含** `choose()` / engines 的接线。
- ⇒ 结案；那个「另切片」拆成 **`T26`**：三个候选切片（读侧渲染建议 / 写侧记录候选 / 不接）**附各自代价**，
  约束是 `adr/0096` §7（不新增工具 / mode / config 键）。

### 2. `T21` 结案：它的三条判据**全部 ✅**（`v1.19.1` 落地）

- ① 只含动作的 fixture 上材料仍可从读路径取到 ✅；② 审计流**仍不进** `_meta.json` / 索引 ✅；③ 读不到时**留痕** ✅
  （端到端断言在 `test/granularity-e2e.test.ts` ④⑤）。
- 台账自己写明再往前一步（让审计材料参与**证据派生**）是「**另一件事**」⇒ 拆成 **`T27`**，
  卡点写明：审计记录与记忆之间**现在只有 `agent` + 日期两个弱键**，「属于哪条线索」判不出来；
  三个候选方向（用路径当键 / 时间窗+agent / 明确不接）都列了，**先选型再写代码**。

### 3. 为什么不直接做 `T26` / `T27`

- `T26` 的三个切片里，② 改**写侧语义**（要过 `adr/0086` 契约登记册）、① 的引擎**当前只报 `null`**（收益待评估）、
  ③ 等于「按 `ADR-0087` 评估删除」—— **三条都不是随手能定的**，属**设计决策**。
- `T27` 的卡点是**键选型**：键选错 = 把「同一批审计记录」错配成一条线索，而证据层要的是**可复核**的键。
  ⇒ 这两个都是「先拍板、再写码」的形状；本轮把**问题写成可决策的形态**（候选 + 代价 + 完成判据），把代码留给拍板之后。

### 4. 验证

- `SHADOW_EVAL_ROOT=D:\project\net1 npm run verify` → **exit 0**（`run-tests` **70/70**；本版无代码改动）。
- `npm run audit:docs` → ①三方版本一致 / ②12 步双向点名 / ③④⑤⑥⑦ 全过。
- 本版**产品代码零改动** ⇒ 不需要 `sweep:timebomb` / `bench:derived-index`（未改任何默认值与索引逻辑）。

### 5. 诚实标注

- **`T26` / `T27` 的工作一点没做** —— 它们只被**定义清楚**了；本轮没有实现任何 `decision/` 接线，也没有动审计流。
- `T18` / `T21` 的结案依据是**它们自己的判据**（不是「我认为够了」）：每条都把判据原文与证据落点写在条目里。

### 当前状态
dsh-shadow 是 DSH 记忆插件：读侧 `shadow_query` / `read_shadow` / `recall_shadow`；写侧 `.shadow/atoms` + 保留期 / 失效 / 证据等级；
治理 = ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · 分层方向门 · 复杂度预算门 · 脚本语言门；唯一验证入口 `npm run verify`；
**发版唯一入口 `npm run release`（闸门在第一步）**；评测口径 = dev 切片（默认）/ 留出切片（报告，`--holdout-only`）。
- **下一批**：`T26`（`decision/` 切片选型）· `T27`（审计→证据的键选型）—— 两条都是**先拍板再写码**。
- **仍等你**：`D2` 填可信根 · `6.3 待定语义` · 一份**冻结语料**（报告级基线）· **跨平台环境**（`sqlite` 设默认的前提）。
