# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **本文件自 v1.21.15 起只保留一份**：**每版覆盖、不追加**；tag **每版只留一个**（删旧建新）—— 口径见 `AGENTS.md`。

## [v1.21.23] `T25` 结案：simulation 的 `explored`/`compared` 是**预留登记**，不是断线

**代码注释 + 台账面；无行为改动**。

### 结论（**并更正了我起这条时的猜测**）
- 我原以为它与台账 §7.3「`simulation/` 本轮未修」吻合 ⇒ 疑似未接线泳道。
- **实测推翻**：`reality-boundary.ts:4` 的禁止集是 **`predicted/confirmed/expected`**，**不含** `explored`/`compared`；
  类型与 `outcomeIsHypothetical` 的白名单**都允许**它们。
- **真相**：`engine/simulator.ts:25` **只生产 `"hypothetical"`**（注释：「只 hypothetical，禁 predicted/confirmed/expected」）⇒
  这是 **「声明的状态面 > 生产的状态面」= 预留登记**，**不是缺陷**；与 §7.3 的吻合**只是字面**，不是因果。
- **处置**：沿用本仓对「未接线的预留」的既有判法（**保留并注明**）—— 在 `types/outcome.ts` 就地注明
  「当前引擎只产 `hypothetical`；若将来真要生产这两值，须**同时**补产生路径与断言」。
- **未做的备选（如实记录）**：**没把类型收窄**到 `"hypothetical"` —— 那会**改一个契约面**（类型白名单），
  按本仓纪律应先走 `adr/0086` 的登记册流程，且属**语义决策**，不随分诊顺手做。

### 本会话关于「B 类判据」的第三条留痕
`audit-wiring` 的 B 类**分不清**「未实现的状态」与「断线」：它只答「有没有单行字面量写入者」。
⇒ `T25` 这类**预留登记**会**永久**出现在它的输出里（除非有人写了生产者）。这是**判据的固有形态**，不是它的 bug ——
但**读它的人必须知道**（本条与 `T2` 结项就是这条读法的示例）。

### 当前状态
dsh-shadow 是 DSH 记忆插件：读侧 `shadow_query` / `read_shadow` / `recall_shadow`；写侧 `.shadow/atoms` + 保留期 / 失效 / 证据等级；
治理 = ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · 分层方向门 · 脚本语言门；唯一验证入口 `npm run verify`。
- **下一批**：`T6`（兜底根 fallback）→ `T9`（sidecar 读路径收益）→ `T11`（留出集/冒烟门）→ `T13` 后半（复杂度预算门，**须先立 ADR**）→ `T17-C` → `T18` 另半边 → `T21`。
- **仍等你**：`D2` 填可信根（只有人能加）· `6.3 待定语义`（明写「不修，需先拍板」）。
