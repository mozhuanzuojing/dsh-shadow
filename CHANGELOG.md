# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **本文件自 v1.21.15 起只保留一份**：**每版覆盖、不追加**；tag **每版只留一个**（删旧建新）—— 口径见 `AGENTS.md`。

## [v1.21.18] 台账与文档面四项收口（含修一处「台账撒谎」）

**文档与台账面；无产品行为改动**。

### 修了一处我自己造的「台账撒谎」
`BACKLOG` 的 `D12` 标题此前仍写「⚠ **执行口径待定**」，而它 **`v1.21.13` 就已按 (iii) 全量执行完毕** ——
执行时我改了 CHANGELOG / ADR / `tools/`，**唯独没把台账标题回写**。这正是本仓最忌的形态（写过的话没回写），已修。

### 两项待分诊拍板落地
- `T23` ✅ **补指针**（不是重复事实）：`references.md` §2 那一行现在指向本文件的 `## 宿主原生能力`，
  并明写「**别把这一行读成「本仓缺浏览器能力」**」—— 收住它**单独读**时的误导面。
- `T24` ✅ **维持现状**：`_research/` 实测 `git ls-files` = 0（未跟踪）、且不在 `files` 里 ⇒
  **不脏 git、不进包**，**语义上已是「仓外」**，只是物理上在工作区；已在该目录内加一行**本地说明**（同样未跟踪）。

### `V10` 的连带项收口
`presets/README` 里几处 `0.1.7-alpha.2` 的行号读数：**标签保留**（那是「在哪一代量的」这一事实），
并补一段说明 —— 它们在 `rc.2` 上**不重跑也成立**：载体包 `dsh-experimental-tool-agent-team`
在 `alpha.2` / `rc.1` / `rc.2` 三代**逐字节未变**（逐文件 MD5 相同），按行号的读数不可能因换代而变。

### 当前状态
dsh-shadow 是 DSH 记忆插件：读侧 `shadow_query` / `read_shadow` / `recall_shadow`；写侧 `.shadow/atoms` + 保留期 / 失效 / 证据等级；
治理 = ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · 分层方向门 · 脚本语言门；唯一验证入口 `npm run verify`；
宿主原生能力经 `adr/0089`；跨项目可信根机制经 `adr/0108`（注册表待人工填根）。
- **待你拍板**：`D2` 填可信根 · `P1` 悬空引用三收口 · `6.3 待定语义`。
- **下一批（不需要你拍板）**：`T2` —— 85 条 B 类线索逐条分诊。
