# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **自 v1.21.15 起本文件只保留一份（当前版本）** —— 历史条目与全部历史 tag 已按用户 2026-09-25 决策清除。

## [v1.21.15] 重写为单份：清除全部历史 tag 与历史版本条目

**仓库面（历史清除）；无产品行为改动**。

### 关于本文件的现状（先说清楚，免得后来者以为丢件）
- 本文件**曾经保留全部版本条目**（每条含决策 / 边界 / 验证记录）。按用户 2026-09-25 决策，
  **历史条目已全部删除，只保留这一份**；**全部历史 tag 已从本地与远端删除**，此后只保留当前版本的 tag。
- **净收益 = 0（执行前就测到，并如实告知后才执行）**：本文件 **不进 npm 发布包** ⇒ 删它**不改变包体积**；
  tag 是 `.git` 内的 ref，也不进包。**可发布面一寸未省。**
- **净损失（如实记录，不缩小）**：全仓有大量 `vX.Y.Z` 形式的引用（分布在本仓多个 md）；
  除本文件自身外，那些「见 vX.Y.Z」**大多不是 `file:line` 形态** ⇒ **引用门 ⑥ 看不见** ⇒ **静默悬空**。
  已在 `BACKLOG.md` 立项跟踪（条目 `P1`）。
- **门的形态变化（与 v1.21.14 记过的同型）**：`audit:docs` ⑤ 判「**已经过去的**版本有没有漏打 tag」。
  历史清空后 **`past = 0`** ⇒ 该门**仍然绿，但已无判别对象**。这是**被明确选择的代价**，不是执行事故。
- **可回退**：旧文本与旧 tag 都仍在 **git 提交史**里（tag 名 → 对应提交可由 `git log` 找回）；
  仓外另存了一份清除前快照（`../.docs/fix/2026-09-25/`，按本仓「取证产物出仓」约定）。

### 当前状态（这就是「最新的一份」）
dsh-shadow 是一个 **DSH 记忆插件**：把一件事的来龙去脉写成工作区里的 markdown，换会话后可按主题召回，
并回答「它还算不算数」—— 不让你「相信」记忆，而是让你和 agent 能**核查**记忆。

- **读侧**：`shadow_query` · `read_shadow`（含 `mode:` 家族）· `recall_shadow`。
- **写侧**：`.shadow/atoms` 落盘 + 保留期 / 失效 / 证据等级。
- **治理**：ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · 分层方向门 · 脚本语言门。
- **唯一验证入口**：`npm run verify`（步骤清单见 `README.md`；语料根由 `SHADOW_EVAL_ROOT` 指定）。
- **宿主原生能力**：`dsh 原生 browser-use` / `dsh 原生 computer-use`（见 `adr/0089`；本仓**不引入**第三方浏览器依赖）。
- **未动的两处**：`adr/`（决策记录）与 `BACKLOG.md`（待办台账）—— 本次**只清 CHANGELOG 与 tag**。

### 本次会话（2026-09-25）的决策留痕（细节只在台账与 ADR）
- `T12` 时间炸弹：扫清。 `V10` rc.2 基线：消费面判定为纯增量。 `D3` 细粒度取代：不做。
- `D1`：删除 `ChangeSet` / `invalidateFor`（**含代价记录**：检测器失去真仓库校准锚点）。
- `D12`：归档层去数（**含不可复核声明**与逐条清单）。
- 本文件：历史清除（本条）。
- **未做**：`D2`「跨项目根注册」—— 已决策「做」，须**先立 ADR**（新增配置面 + 可信根授权模型，涉 inv 178）。
