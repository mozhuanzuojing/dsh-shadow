# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **本文件自 v1.21.15 起只保留一份**：**每版覆盖、不追加**；tag **每版只留一个**（删旧建新）—— 口径见 `AGENTS.md`。

## [v1.21.24] `T6` 三选一落定：选 `②`（保留兜底根 + **显式可见信号**）+ 修一处过期注记

**台账与决策面；无行为改动**（实现留给下一轮，见下）。

### 决策：`②`，不是 `①` 也不是 `③`
`T6` 的问题是「**兜底根这条路径本身是否该存在**」（`adr/0074` 的已知空白：落到 `~/.dsh-observer/shadow` 时
**没有 session 可问** ⇒ 写入仍走部署 fallback ⇒ 被围栏拒绝）。三选一里定 `②`：
- **贴合本仓第一原则**：`ADR-0049`「**缺件不静默**」—— 现状是「写入发生了、却没人知道它没经会话授权」，正是静默降级；
- **不改口径、无数据风险**：`①` 要改 `scope.ts:13`「保证**可写**，而非不写」的口径，并先确认兜底根下有无历史数据依赖；
- **有前例**：信号形态可沿用 `lastFlushError`，属**加法**而非改契约。

### 实施要求（下一轮，机械可执行）
1. `resolveShadowScope` 落到 `DEFAULT_SHADOW_ROOT` 且问不到 session 时，**记下可见信号**（形态对齐 `lastFlushError`）；
2. **附一条复现该场景的测试**（**无 session + 无显式 root**）—— 完成判据对 `①②` 的硬要求；
3. ⚠ **不得**做成静默通过，否则等于把 `②` 做回了 `③`。

### 过程诚实记录
本版第一次落盘时脚本**语法错**（`CONCL` 里的反引号把模板串截断了）⇒ **什么都没写**；
改用**单引号数组拼接**后重跑成功。⚠ 这也是本会话第三次「**工具自身出错**」的实例（前两次：归档层引用度量的判据、B 类写侧正则）。

### 顺手更正
本条原写「**优先级低于 B3**」，而 **B3 早已闭环**（`v1.15.40`，宿主 2026-09-12 重启）⇒ 该句已无意义，已改。

### 当前状态
dsh-shadow 是 DSH 记忆插件：读侧 `shadow_query` / `read_shadow` / `recall_shadow`；写侧 `.shadow/atoms` + 保留期 / 失效 / 证据等级；
治理 = ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · 分层方向门 · 脚本语言门；唯一验证入口 `npm run verify`。
- **下一批**：`T6` 的**实现 + 测试** → `T9` → `T11` → `T13` 后半（须先立 ADR）→ `T17-C` → `T18` → `T21`。
- **仍等你**：`D2` 填可信根（只有人能加）· `6.3 待定语义`（明写「不修，需先拍板」）。
