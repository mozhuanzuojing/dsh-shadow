# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **本文件自 v1.21.15 起只保留一份**：**每版覆盖、不追加**；tag **每版只留一个**（删旧建新）—— 口径见 `AGENTS.md`。

## [v1.21.25] `T6` 实施设计入台账（含精确锚点）+ 一处**有意不做的说明**

**台账面；无代码改动**。

### 本版做了什么
把 `T6`（`②`：保留兜底根 + **显式可见信号**）的**实施设计连同精确锚点**写进台账，使下一轮**机械可执行**。

### 侦察得到的关键事实（决定了实现形态）
`resolveWorkspace` 的调用方**只有三处**：`core/writer/capture.ts:73`（写）· `core/writer/materialize.ts:292`（写）·
`query/query.ts:61`（读）。而 **`lastFlushError` 的设置点就在 `materialize.ts:299`** ⇒
**信号应落在同一个地方**（对齐既有形态），而不是另造一条路 —— 否则就是「同一件事两处写」。

### ⚠ 本版**有意不做**的一件事（说明理由，不缩小承诺）
`T6` 的实现是 **4 文件 + 1 条新测试**的连贯改动（`core.ts` 加字段与初值 · `materialize.ts` 置信号 ·
`index.ts` 读侧出口 · 新测试驱动「无 session + 无显式 root」的 flush）。本轮上下文预算已接近见底，
**起一个半成品会把仓库留在脏状态**，比停下来更糟 ⇒ 本轮**只落设计**，下一轮一次做完并跑门。
设计（含每处的精确锚点、可参考的既有用例位置、验收命令）已全文写进台账 `T6` 条目。

### 当前状态
dsh-shadow 是 DSH 记忆插件：读侧 `shadow_query` / `read_shadow` / `recall_shadow`；写侧 `.shadow/atoms` + 保留期 / 失效 / 证据等级；
治理 = ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · 分层方向门 · 脚本语言门；唯一验证入口 `npm run verify`。
- **下一批**：`T6` 的实现 + 测试 → `T9` → `T11` → `T13` 后半（须先立 ADR）→ `T17-C` → `T18` → `T21`。
- **仍等你**：`D2` 填可信根（只有人能加）· `6.3 待定语义`（明写「不修，需先拍板」）。
