# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **本文件自 v1.21.15 起只保留一份**：**每版覆盖、不追加**；tag **每版只留一个**（删旧建新）—— 口径见 `AGENTS.md`。

## [v1.21.27] `T9` 结案：sidecar 的**真机代价标定** + 两条决定（不接检索 / 不回填）

**工具面 + 台账面 + 文档面；产品代码未改**（本轮是给 `adr/0075` 的「未验证」三项补读数与决定）。

### 1. 新增读数工具 `tools/sidecar-cost.ts`（`npm run sidecar:cost -- <工作区根>`）

- **驱动产品面**：`createShadowCollector(...).ensureIndex(ws)` ⇒ 真实 `writeAbstracts`
  （`core/writer/materialize.ts`），**不自己再算一遍派生**（另算一遍，量到的就不是产品行为）。
- **绝不写用户语料**：读真实语料、**写全部重定向**到临时沙箱（overlay：读先看沙箱、再落回语料）；
  **两个口径各用独立沙箱**（否则上一轮的 `_meta.json` 会污染下一轮）。
- **自带两条机器自检**：① 有没有写落在沙箱之外；② 语料文件数有没有变。任一不成立 ⇒ exit 1（不许把「测坏了」当读数）。

### 2. 真机读数（`D:\project\net1`，读数时刻 `2026-09-25T05:51:11Z`）

| 项 | 读数 |
|---|---|
| 语料 | `atoms/` **460** 条记忆文件 · **2** 个日期桶（`2026-09-24` … `2026-09-25`）；`.shadow` 共 1498 个文件 |
| 一次索引重建的写 | **26** 次（sidecar **2** · Episode 收口 22 · `indexes/_index.md` 1 · `_meta.json` 1） |
| sidecar 份数 | = **活跃集的 distinct 日期桶数**（本机 2）—— 与「写入次数有界（O(#dates)）」的推理一致 |
| 单份 sidecar | 1227–3146 B（合计 4373 B）；写耗时 median **0.46–0.67 ms/份**（沙箱 fs） |
| 重建总耗时 | **65–69 ms**（读源 + 派生 + 全部写）；`forget` 开/关同读数（本机语料太新，14 天阈值未命中） |
| `_index.md` 引用 | 引用 **2** 行 = 写出的 2 份（`showInIndex` 默认 3，本机未触顶） |

### 3. 两条决定（判据 ① 与 ③）

- **① 读路径收益 → 决定「不接检索」**：sidecar 只被 `_index.md` 的目录摘要段引用、**不参与 `matchShadowNodes` 打分**；
  要接进排序就得先有评测口径 **+ 留出集**（= `T11` ① 尚未落地）⇒ **记依赖，不擅自接**。
  **在那之前不得表述为已验证。**
- **③ 存量回填 → 决定「不回填」**：代价小（2 次写 / 4.4 KB / ~1.4 ms）但**收益同样小**，且 `_index.md` 只引最近
  `showInIndex`（默认 3）个桶 ⇒ 旧桶 sidecar **写了没人读**（纯无效写）；更重要的是**不引入第二条写路径**
  （本仓「判据收一处」的反面）。→ 旧桶在各自（若发生的）索引重建时自然生成。

### 4. 两条**新边界**（本轮实测发现，此前无读数）

1. **`covered` 是「活跃集」口径，不是目录文件数**：本机两桶自报 `covered` 19 + 4 = **23**，而 `atoms/` 有 **460** 个文件
   （其余被 Episode 收口 / 遗忘移出活跃集）。与 `_index.md` 口径一致，但**别把「记忆 4 条」读成「那天只有 4 条」**。
2. **「写了 D 份 / 被引用 ≤ `showInIndex` 份」**：`writeAbstracts` 为**每个**日期桶写 sidecar，而索引只引最近 N 个 ⇒
   **D > 3 时多写的没人读**。本机 D = 2 未触发；只登记，不改默认。
3. **L0 顶格即截断**：本机两条 L0 都**恰好 256 字**（`L0_MAX`）。「首段式摘要」是 `adr/0075` 的设计，
   但「顶格 = 被截断」此前没有读数指出过；相关性判断仍属读侧、**未经评测**。

### 5. 文档同步（同一件事只写一处）

- `adr/0075`：**追加补记**（正文不改写）—— 上表读数 + 两条决定 + 两条新边界。
- `BACKLOG.md`：`T9` 标题改 ✅ 结案，正文追加「结案」小节。
- `test/abstract-sidecar.test.ts`：末尾「未在本文件验证」四条改为**指向本轮读数与决定**（原文是「未测」）。
- `AGENTS.md`：「构建与验证」段加一行 `npm run sidecar:cost`（含「刻意不进 `verify`」的边界与重跑时机）。

### 6. 验证

- `SHADOW_EVAL_ROOT=D:\project\net1 npm run verify` → **exit 0**（`run-tests` 69/69）。
- 读数可重放：`node tools/sidecar-cost.ts D:\project\net1` → 自检「写越界 0 次 · 语料文件数未变（1498 → 1498）」、exit 0。
- **typecheck:tools / audit:scripts** 对新增 `.ts` 工具通过（`.ts` 口径）。

### 7. 诚实标注（做不到的不假装）

- **召回收益没测**：本轮是「明确决定不接」（判据允许的**那一支**），不是「测得无收益」；依赖 `T11` ①。
- **L0 质量只有读数、没有评测**：给的是「顶格即截断」这个可复算事实，不含相关性判断。
- 本轮**产品代码零改动** —— 若要改 `showInIndex` / 只写前 N 个桶等成本优化，属另一切片（需先定判据）。

### 当前状态
dsh-shadow 是 DSH 记忆插件：读侧 `shadow_query` / `read_shadow` / `recall_shadow`；写侧 `.shadow/atoms` + 保留期 / 失效 / 证据等级；
治理 = ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · 分层方向门 · 脚本语言门；唯一验证入口 `npm run verify`；
**发版唯一入口 `npm run release`（闸门在第一步）**。
- **下一批**：`T11` ①（先定留出集）→ `T13` 后半（复杂度预算，须先立 ADR）→ `T17-C` → `T18` 另半边 → `T21`。
- **仍等你**：`D2` 填可信根（只有人能加）· `6.3 待定语义`（明写「不修，需先拍板」）· `T11` ① 的语料决策。
