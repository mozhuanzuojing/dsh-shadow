# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **本文件自 v1.21.15 起只保留一份**：**每版覆盖、不追加**；tag **每版只留一个**（删旧建新）—— 口径见 `AGENTS.md`。

## [v1.21.20] `T7` 改判：删除 `relationForProposal`（零消费方 + 恒返回常量的「假接口」）

**代码面 + 决策改判；无产品行为改动**（该函数生产与测试引用皆 0，删它不动任何调用点）。

### 为什么删（三选一里选「删」）
- **原判**（`adr/0062` 正文「保留并注明」+ `BACKLOG` 的 A 类分诊）：**保留** —— 原文已注「留接口」（给未落地的 reflection 泳道）。
- **本轮改判依据**：① **零消费方**（全仓仅定义自身一处）—— `ADR-0087`：**不允许建没有消费方的机制**；
  ② 它**恒返回常量**且参数从不使用 ⇒ 留着就是一个**假接口**：将来真接线时会**静默给出错答案**（本仓最忌的形态）；
  ③ 「让它真的用入参」需要一个**尚未实现**的 reflection 语义 ⇒ 那是**臆造**，不做。
- ⇒ **删除**。若 reflection 泳道将来落地，**重新立条目 / ADR 再实现**，而不是复活一个恒返回常量的占位。

### 改判记录与同步更正
- `adr/0062` 追加补记（原判决与理由一并留档 + 本次改判依据）。
- `BACKLOG` 的 `T7` 置 ✅；**A 类分诊表里那行「保留并注明」也一并更正** ——
  **写过的话必须回写**（本会话已出过一次同类：`D12` 标题写着「待定」而实际已执行）。

### 当前状态
dsh-shadow 是 DSH 记忆插件：读侧 `shadow_query` / `read_shadow` / `recall_shadow`；写侧 `.shadow/atoms` + 保留期 / 失效 / 证据等级；
治理 = ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · 分层方向门 · 脚本语言门；唯一验证入口 `npm run verify`。
- **下一批**：`T2` ④ 的 26 条逐条定性 → `T6` / `T9` / `T11` / `T13` 后半 / `T17-C` / `T18` / `T21`。
- **仍等你**：`D2` 填可信根（只有人能加）· `6.3 待定语义`（明写「不修，需先拍板」）。
