# Changelog

> dsh-shadow 变更历史（Keep a Changelog）。语义化版本。
> **本文件自 v1.21.15 起只保留一份**：**每版覆盖、不追加**；tag **每版只留一个**（删旧建新）—— 口径见 `AGENTS.md`。

## [v1.21.21] `T2` ④ 首批取证：`26` 条收紧为三组 + **修掉我自己取证脚本的一处假判**

**台账与判定面；无产品行为改动**。

### 修了一处「工具自身的假判」（本条最值钱的一段）
第一版取证用 `[:=]\s*"lit"` 判「有没有写侧」，**把 `=== "lit"` 里的 `=` 当成赋值** ⇒
它打印的「示例」本身就是**读**（`if (lc === "revoked")`）。改用**负向后顾**排除 `=!<>` 后重跑，读数大幅改变
（`rejected` 写侧 5 → **2**、`revoked` 2 → **0**）⇒ **结论跟着变**。
这正是本仓「**工具必须先标定再用**」的又一实例：**判据错了，读数再多也是假的**。

### `T2` ④ 的三组结论
- **B1 有真写侧 ⇒ 跨文件写入 = 工具的窗口盲区 ⇒ 误报（8 个字面量 / 约 13 条）**：
  `rejected` · `decision` · `stale` · `validated` · `undecidable` · `ok` · `failed` · `query`。
- **B2 无简单写侧但值是外部来源 ⇒ 分支可达**：`directory`（宿主 `listDir` 条目）· `clear`（宿主载荷的 `change.operation`）。
- **B3 需读生产表达式再判（不冒充已判）**：`expired` · `revoked` · `metadata` · `session` · `L2` · `mem` · `exists`
  （其中 `session` 已有在案判定：`core/lineage/validator.ts:25` 注释写明该支**永不可达** —— `ADR-0063` 实测）。
- **盲区如实标注**：只扫 8 个目录的 157 个 `.ts`，且只认 `= "lit"` / `: "lit"` 两种形态 ⇒
  **三元 / `return` / 数组里的生产者会漏判** ⇒ **「无简单写侧」≠「无生产者」**。

### 当前状态
dsh-shadow 是 DSH 记忆插件：读侧 `shadow_query` / `read_shadow` / `recall_shadow`；写侧 `.shadow/atoms` + 保留期 / 失效 / 证据等级；
治理 = ADR 登记册 · 棘轮 · 引用门 · 文档一致性门 · 分层方向门 · 脚本语言门；唯一验证入口 `npm run verify`。
- **下一批**：`T2` ④ 的 **B3 那 7 条**逐条读表达式 → `T6` / `T9` / `T11` / `T13` 后半 / `T17-C` / `T18` / `T21`。
- **仍等你**：`D2` 填可信根（只有人能加）· `6.3 待定语义`（明写「不修，需先拍板」）。
