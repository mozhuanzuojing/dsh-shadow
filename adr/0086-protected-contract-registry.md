# ADR-0086: Protected Contract Registry —— 什么算受保护契约、以及最小的弃用流程（T15 切片 1）

- 状态：**已接受；T15 判据 ①②③ 完成 + 十字段（每条契约 10 字段）已填（切片 2）**；切片 3（D2 漂移细分 / D3 再框定）未做（2026-09-13 / v1.15.72；v1.15.70 的计数与因果错误见 §8.5，切片 2 见 §8.6）
- 关联：`BACKLOG.md` **T15**（用户 2026-09-12 指定为 `T8` 之后的**下一阶段主产物**）·
  `adr/0083` §14.1（**路线漂移**：连续 8 轮审查→修而 T15 未动）· **ADR-0050**（本仓唯一的硬切先例，
  无弃用窗口）· **ADR-0003**（派生件不是 source）· **ADR-0049**（缺件不静默 → 「未知枚举不得落回默认」同族）·
  **ADR-0052**（`_meta.json` 的 schema 面）· D8 的判定（本仓不为自己的组件打 `stable/beta/experimental`）
- 定位：**契约**。本 ADR 是 T15 的**判据与理由**：先定「什么算契约」与「怎么退场」，
  再逐条填十字段 —— **十字段（切片 2）已于 v1.15.72 填完，登记册本体在 `README.md`（§8.6）**；
  切片 3（D2/D3）未做。

## 1. Context：为什么需要它，以及为什么它一直没被做

本仓的**公开面**是「**工具名 + `mode` 串 + 配置键 + 落盘格式**」，而**没有任何兼容性政策**。
**代价已经发生过一次**：ADR-0050（v1.13.0）把旧 `mode` 名**直接废止**，并写明「非『mode 串永远不变』」——
**那次没有任何弃用窗口**，靠的只是 `CHANGELOG` 的可读性。

同时，`adr/0083` §14.1 记下了更重的一条：用户 2026-09-12 定的泳道是 `T8 → T15 → D1/D2/D3 → A段 → …`，
而我在 `T8` 之后连续**五轮**元审查，**T15 一步未动**。**这份 ADR 存在的第一个理由就是终止那件事**：
把「下一阶段的主产物」从一句台账条目变成**已签入的产物**。

## 2. 决定一：契约 ≠ API 清单

采用用户给定的定义（`BACKLOG.md` T15）：一条契约 = **Surface + Semantics + Stability + Allowed Drift + Verification**，
**十条字段缺一不得入册**（`id` / `surface` / `owner` / `semantic meaning` / `stability level` /
`allowed changes` / `forbidden changes` / `evidence` / `verification` / `ratchet`）。

**因此本 ADR 不谎称登记册已完成**：切片 1 交付的是**面清单**（受保护的**面**有哪些）
+ **稳定性分档口径** + **弃用流程**；**逐条的十字段仍未填**（见 §6 未做）。
按用户自己的规则，缺字段的条目**不得入册** —— 故 README 里那一节叫「受保护契约面」，
而**不叫**「已完成登记册」。

## 3. 决定二：稳定性用**内部三档**，且**不自造等级**

D8 已判定：本仓**从不给自己打 `stable`/`beta`/`experimental`**（凭空造等级就是「让文档比事实强」）。
故采用 T15 规格里的**内部语义**三档，判据落在**「违反时的后果」**上，而不是落在「成熟度」上：

| 档 | 判据（一句话） | 违反时的后果 |
|---|---|---|
| **`hard`** | 使用者**已经依赖**它，改了会**让已记录的东西读不出来 / 让调用失败** | 破坏既有记忆或既有调用 ⇒ **必须走弃用流程** |
| **`soft`** | 改了会**改变可观察行为**，但**不破坏数据**（派生件可整份重建 / 提示文本可改） | 需在 `CHANGELOG` 写清；**不需要**弃用窗口 |
| **`experimental`** | **无承诺**；本仓自己都还没有确定的用法 | 可随时改，但**必须在就地可见地标注**（ADR-0049 同族：不许静默） |

**为什么判据是「后果」而不是「成熟度」**：成熟度需要人给组件打分（本仓拒绝做，D8），
而后果是**从代码可核对的**（`core/paths.ts` 里有读路径吗？有生产消费者吗？）。

## 4. 决定三：最小的弃用流程（用户 T15 判据 ②）

**五步，缺一不可**——

1. **旧名至少保留一个版本**（在此期间两种写法都要能用）；
2. **调用旧名返回可见提示**，且提示**必须点名替代品**；**没有替代品就明说「无替代」**，不得留空
   （本仓已有形态：`query/query.ts` 的 `retiredApiMessage`，返回「已废止：X → 请用 Y」）；
3. `CHANGELOG` 写**迁移说明**（怎么改、影响什么）；
4. **移除的时机**：`CHANGELOG` 里要有**跨度 ≥ 版本**的弃用记录（本仓只有一个使用者，
   硬套 major/minor 三段式是形式大于实质 ⇒ 用「弃用记录的存在与跨度」当代替判据）；
5. **未知枚举不得落回默认**：未知 `mode` / 未知 provider / 未知版本一律**显式失败或显式 `unavailable`**，
   **不许静默进默认召回**（`retiredApiMessage` 已有这条；ADR-0049「缺件不静默」同族）。

## 5. 决定四：登记册放 `README.md`，证据指向代码

用户 T15 判据 ③ 明写「把清单放进 `README.md`（而非另开一份易漂移的文档）」⇒ 遵守。
本 ADR 只放**决策与判据**（稳定性分档、弃用流程、为什么这样分），
`README.md` 放**清单本身**（面向使用者），二者分工与本仓 `AGENTS.md` 的「每份文档 owned by 什么」一致。
**每一条都必须带 `文件`（符号名优先）作为 `evidence`** —— 行号会腐烂（v1.15.67 的教训）。

## 6. 切片 1 交付了什么 / 没交付什么（**不许含糊**）

| 用户 T15 判据 | 状态 |
|---|---|
| ① 列出受保护契约面清单（逐项标 stable 或 experimental） | ✅ **面清单已穷尽（v1.15.70，计数 v1.15.71 更正）**：面族**清单以 `README.md` 的受保护契约面为准**（本 ADR 只放判据 —— 这正是 §5 的分工），全部已列并分档；mode 面 **62**（**受门保护**：`test/recall-envelope.test.ts:104`）。⚠ **此处原先写「七个面族」并逐个列举 —— 那是会腐烂的派生计数**：v1.16.0 实测已是 **9** 个 `*-v1` id，且 `README.md` 的同族分组当时还漏了 `tool-output-v1`。⇒ **本行不再写条数、也不再复制清单** |
| ② 给出一条最小弃用流程 + 说明「无替代品」怎么写 | **完成**（§4） |
| ③ 把清单放进 `README.md` | **完成**（该节） |
| 十字段逐条登记（10 字段 × 每条契约） | ✅ **完成（v1.15.72，切片 2）**：**每条契约 10 字段**全填入 `README.md`（表 A 结构语义 / 表 B 治理，`id` 对齐）；`verification` 一列**如实写出哪些面根本没人守**（见 §8.6） |
| **模块归属表**（`Module` / `Owns` / `Reads` / `Writes` / `Must not own`，**用户要求**） | ✅ **完成（v1.15.73）**：**生成而非手写**（`t15-module-ownership.ts` 直接复用 `audit-layers.lib.ts` 的判据）；`Owns`/`Writes` 不可机械推，**只对 ADR 定过的层给结论、其余标未核**（见 §8.7）。⚠ **v1.15.72 之前它一次都没被提过** —— 那本身就是一处遗漏 |
| D1 的再框定（`ChangeSet` 无生产消费者 / 无验证价值 / 无外部契约 ⇒ 删） | ✅ **已判（v1.15.74，切片 3）**：**保留** —— 前两条成立，但**第三条「没有验证价值」不成立**：它是 `tools/audit-wiring.selftest.ts:132-140` 的**真仓库已知答案**（标定锚点）⇒ 删它会把一条检测器变弱（见 §8.9 第 ）。⚠ 这是三者中**唯一不能机械判**的一条 |
| D2 的漂移细分（名称/结构/语义/行为） | ✅ **已判（v1.15.74，切片 3）**：四类各自对应登记册的 **`allowed`/`forbidden`/`stability`** 列 —— **不需要新造表**（见 §8.9 第 ） |
| D3 的再框定（旧大对象 vs 新细粒度对象） | ✅ **已判（v1.15.74，切片 3）**：**登记册里的契约全是细粒度面，无一条是内部大对象** ⇒ 对内部大对象（`ChangeSet`/`ShadowNode`/`ProjectionStore` 等）的重构是**允许的 breaking internal refactor**，不是兼容性问题（见 §8.9 第 ） |

**mode 串枚举的完成命令**（v1.15.70 做过一遍、**v1.15.71 因算错返工** —— 方法与边界留在探针里，避免下一个人重新发明）：

```powershell
# 口径 = `query/*.ts` 里出现过的 mode 字面量，**三种形态都要抓**（少抓一种就少 ）：
#   ① `MODES = new Set([...])`   ② `modes: [...]`   ③ `mode [!=]== "..."`（**注意 [!=]==，不是 ===**）
# 门（受保护的那份实现，别再手写正则）：test/recall-envelope.test.ts:86-96，断言恰为 62
# 对账探针（说明 62 与 61 差在哪）：node .docs/fix/2026-09-12/t15-mode-count-reconcile.ts
# 已废止映射（属兼容面，必须登记）
Select-String query\query.ts -Pattern 'RETIRED_MODES' -Context 0,6
```

**62 是受门保护的数，不再有「来源不可考」**：`test/recall-envelope.test.ts:104` 断言 `modes.size === 62`，
同文件 `:103` 断言 `CONTEXT.md` 的 mode 参考表覆盖全部 62 —— **新增 mode 不写文档就红**。
v1.15.70 在此处写的「`CONTEXT.md` 的 62 暂时无法复核」是**错的**（见 §8.5）。

## 7. 未做 / 诚实边界

- **逐条十字段未填** ⇒ 按用户规则「缺一不得入册」，**本 ADR 不宣称登记册已完成**。
- **mode 全量枚举未完成**（12 显式 + 模块的内联分派未读）。
- **`ratchet` 列基本空缺**：哪些契约有棘轮覆盖**未逐条核对**（`tools/audit-ratchet.baseline.json` 的桶与契约的对应关系未建）。
- **落盘格式的 `allowed drift` 只给了粗档**（source vs 派生件），**未逐文件给**（memory 文件名格式、`_meta.json` 字段、sidecar 段落格式各自允许什么）。
- **没有生产消费者**：**这份登记册**当前无人消费（没有代码读它）。
  ⚠ **别把这句读成「登记册里的面没有消费者」** —— v1.15.72 正是这么读错的：
  `config-keys-v1` 的键**是被消费的**（`index.ts` 的 `apply(ctx, rawConfig)` / live 走 `ctx.config`），
  「没有消费者」说的**只是这份文档本身**（见 §8.7 第 ）。
  它是**给将来的人与 agent 读的政策** —— 这一点必须在还有记忆时说清楚，
  否则它会像本仓其他「写好了但从不执行」的东西一样（T1/T4 清理过的那一类）。

## 8. 第六轮元审查（v1.15.70）：把切片 1 补完 + **第 「测量错」在发布前被拦住**

### 1. 补完的三个缺口（都在上一轮我自己的交付里）

| # | 缺口 | 实测 |
|---|---|---|
| 1 | **漏了「工具 schema（参数名）」这一整个面族** —— 而它是用户在 `BACKLOG` T15 里**明列的  `surface` 类型之一** | `read_shadow` **127** · `recall_shadow` **2** · `shadow_query` **3**（判据：按**工具边界**切 `parameters.properties`） |
| 2 | `mode` 面写「显式 12，内联**未枚举完**」 | **62**（v1.15.70 报 61、v1.15.71 更正为 62 —— 见 §8.5） |
| 3 | 兼容面只列了 `RETIRED_MODES`  | **另有 参数级废止**：`args.verify`→`verifyEvidence:true` · `args.recall`→`mode:"recovery"` |

**参数计数我第一版也算错**：用「`name:` 后 窗口」抓，把 `recall_shadow` 算成 **5** 个（**串到了下一个工具**）。
⇒ **窗口式提取必然串边界；边界要从结构里取（到下一个 `name: "`），不要从行数猜。**

### 2. 第 「测量错、不是被测量错」—— **这次在发布前拦住了**

探针把每个模块的 `MODES` 集合与 `if (mode === "…")` 链做差集，**第一版报 「会被接受后静默落空」**。
看起来像一批真缺陷。但本仓的分派是**「前面的 mode 各写一个 `if`，最后一个走兜底」**
（`query/adaptation.ts` 的 `adapt-validation` 即如此）—— **探针不认这个写法**。

**没有报出去的原因**：按 v1.15.69 刚立的规矩，我**先读了一个文件**（`query/adaptation.ts`）才下结论。
⇒ 判据修正为：`MODES \ ifs` **允许恰好 **（兜底）、>1 才可疑；`ifs \ MODES` 才是死代码；
**没有 `MODES` 集合的文件不适用该比较**（`reads.ts` 修正前被误报「`episode` 是死代码」）。修正后 **0 异常**。

| 次 | 报了什么 | 根因 | 是否发布出去 |
|---|---|---|---|
| 1 | 「键减多了」 | 探针把 typeof 判据写窄 | 报告里自我更正 |
| 2 | 「多算 过期行号」 | 把 `src/index.ts` 当本仓 `index.ts` | 当场发现 |
| 3 | 「`typecheck:tests` 未被点名」假绿 | 判据是「文档里出现过」 | 当场发现 |
| 4 | 「声明 18 / 实际 」 | CRLF：`$` 不匹配 `\r` 之前 | 差点改错对的文档 |
| **5** | **「 mode 没有分派」** | **探针不认「兜底」写法** | **✅ 发布前拦住** |
| **6** | **「`mode` 面实测 61、`CONTEXT.md` 的 62 来源不可考」** | **探针把判等形态写死成 `===`，漏了 `query/planning.ts:15` 的 `!== "plan"`** | ❌ **发布出去了**（写进 `README` 当前态层与 `CONTEXT.md`，并被当成结论） |

第 之所以被拦住，靠的是「**先读一个文件再下结论**」；第 说明**那条规矩不够** ——
我当时确实读了文件，但读的是**探针的实现**，没读**被测量那一侧的形态**（`[!=]==` 还有哪些写法）。
⇒ 补一条：**报「实测」之前，先 `grep` 这个数在仓库里是否已被某道门断言**（见 §8.5）。

### 3. 因果：`CONTEXT.md` 的「 mode」**是对的**，我上一轮把它判错了（v1.15.71 更正）

v1.15.70 我在这里写：实测 **61**，`CONTEXT.md` 的 62「**来源不可考**」，并请「将来有人能考证出 62 的来历」。
**来历一直在仓库里**：`test/recall-envelope.test.ts:104` **断言 `modes.size === 62`**，而 `npm run verify` 是绿的。
⇒ **62 是受门保护的数，61 是我算错的**，而我把两者反过来了：把受门保护的数标成「旧测量」、
把没有门保护的探针数写进 `README`（当前态层、面向使用者）。

**根因（可机械复现，不需要推理）**：我的探针两条判等形态都写死成 `===`，而本仓有一处写成 `!==`：

    query/planning.ts:15  if (String(args?.mode || "") !== "plan") return undefined;

⇒ 漏掉 `plan` ⇒ 61。判据修成 `[!=]==` 后即为 **62**，与门一致。
对账探针：`.docs/fix/2026-09-12/t15-mode-count-reconcile.ts`（A=62 / B=61 / B′=62，**B′ == A**）。

**这条纪律要写进 `AGENTS.md`**：**宣布一个数「来源不可考」之前，先 `grep` 这个数是否已被某道门断言** ——
否则会把「被守着的数」降级成「没人认领的数」，方向正好相反。

### 4. 切片 1 现在的完成度

| 面族 | 实测 | 稳定档 |
|---|---|---|
| 工具名 | 3 | `hard` |
| **工具 schema（参数名）** | **132**（127+2+3） | `hard` |
| `mode` 串 | **62** | `hard` |
| 已废止映射（含**参数级**） | 4 + 2 | `hard` |
| 配置键（`ShadowConfig` 顶层） | 19 | 加键 `soft` / 改语义 `hard` |
| 落盘格式 | 记忆 `hard` / 派生件 `soft` | — |
| 提示段 | 措辞 `soft` / 前缀与标记 `hard` | — |

**切片 2 已做（v1.15.72）**：十字段填完（见 §8.6）；**切片 3（D2 漂移细分 · D3 再框定）仍未做**。

## 8.5 第七轮元审查（v1.15.71）：把「62」的因果正过来 —— **受门保护的数不该被没有门的探针推翻**

**做了什么**：`CONTEXT.md` 的 62 恢复为现行事实（并注明它是**被门守着的**）·
`README` 与 §8.1 的「61」改为 **62** · 本节 §8.2 补第 测量错（**这次发布出去了**）·
新增可重放对账探针 `t15-mode-count-reconcile.ts`。

**两类数，待遇不同（本节的可复用结论）**：

| 类型 | 例子 | 有人改它时该怎么判 |
|---|---|---|
| **受门保护的数** | `mode` 总数 62（`test/recall-envelope.test.ts:104`） | **先假定它对**。要改它就得**同时改门**，且说明为什么门错了 —— 不能拿一个探针去推翻 |
| **没有门的数** | 探针现算出来的任何计数 | 改它无需连带改动，但**写进文档必须带口径与命令**（AGENTS.md「写数就当场核」） |

**为什么这条重要**：v1.15.70 我把这两类的待遇**弄反了** —— 用一个没有门的探针数（61）
去推翻一个受门保护的数（62），还顺手把后者标成「来源不可考」。
**「来源不可考」这句话本身就该有证据**：说它之前得先 `grep` 过仓库。

**未做（诚实边界）**：本轮**只**纠正了这一处因果与计数，**没有**顺带重算其它派生计数
（`BACKLOG` 的 D/V/G 条数、`AGENTS.md` 的检查条数等一律不碰）——
按 v1.15.70 的教训，**要么现数并写下口径，要么不写数**，不做「批量刷新」。

## 8.6 切片 2（v1.15.72）：十字段填完，以及「**八族里有两族其实没人守**」

> **逐条的 `verification` 不在这里重列**：那是**登记册本体**，在 `README.md` 的表 B 一列里
> （本节只给**结论**）。原因是本仓的纪律「**同一个判据只能有一份实现**」——
> 第一版这里抄了一张 的 `verification` 表，与 `README` 的表 B **逐行重复**（v1.15.73 删）。

**结论（契约按设防强度分三档）**：**6 强 / 1 部分 / 1 无**。⚠ **这个结论 v1.15.83 起不再成立**（下面两族补了「名字面」的枚举门，见 **§8.13**）。
- **强**：`tool-name-v1` · `read-mode-v1` · `retired-mapping-v1` · `memory-file-v1` · `derived-file-v1` · `prompt-segment-v1`
- **部分**：`tool-schema-v1` —— 只守 `mode` 的描述，**参数名无人枚举** ⇒ 删参数不会红（⚠ **已过时**：v1.15.83 补了枚举门，见 **§8.13**）
- **无**：`config-keys-v1` —— 顶层 19 键**没有任何枚举门** ⇒ **删键 / 改名没有测试会红**（⚠ **已过时**：v1.15.83 补了枚举门，见 **§8.13**）

**这一轮真正学到的东西不是「填了 格子」，而是 `verification` 一列把「谁其实没人守」变成了可读的事实。**

**`ratchet` 一列 全写「无桶覆盖」，这是对的、不是欠账。**
本仓的棘轮桶按**缺陷类**分（`wiring` 的 `a1/a2a/a2b/a3/b_keys`、`drift` 的 `drift_keys/drift_sites`），
**不按契约面分**；契约面的守卫手段是 `verification`（具体断言），不是 `ratchet`。
⇒ **没有为凑这一列而新造桶**（本仓已清理过一批「写好了但从不执行」的东西）。
**唯一例外**：`read-mode-v1` 的「`CONTEXT.md` 必须覆盖全部  mode」**本身就是一条棘轮**。

**为什么没有顺手给 `config-keys-v1` 加门 —— 理由只有一个：本轮的范围。**（**v1.15.83 已补**，见 §8.13）
⚠ **v1.15.72 在这里写的理由是错的**（见 §8.7 第 ）：原文称「删键是否真的危险**还没有生产证据**（**没有生产消费者**，见 §7）」——
**那是把两件事混了**：§7 说的「没有生产消费者」指**这份登记册**没人读，而**配置键恰恰是被消费的**：
`index.ts` 的 `apply(ctx, rawConfig: ShadowConfig = {})` 读 `rawConfig`，live 走 `ctx.config`（Cordis 插件行的 `config`，
经 `cordis.patch.yml` 注入）⇒ **用户在配置里写下的键名就是外部契约**，删掉它会让那份配置**静默失效**。
⇒ 正确表述：**不加门是因为本轮要「填完登记册」而不是「扩工具」**（加门就要维护它），
而**危险本身是有证据的、只是还没被测试固定下来** —— 这两句差别很大。

**口径与复算**：四个数（工具名 3 · 参数 132 · mode 62 · 配置键 19）由一个命令一次算清并**全量打印小样本**：
`node ../.docs/fix/2026-09-12/t15-counts-verify.ts`（按**花括号深度**切第一层键，已避开 v1.15.70 的「窗口串边界」缺陷）。


## 8.7 第八轮（v1.15.73）：审 v1.15.72 本身 —— **缺陷 + 从未开工的遗漏**

| # | 类 | 缺陷 | 处置 |
|---|---|---|---|
| 1 | **因果** | §8.6 称不给 `config-keys-v1` 加门是因为「删键**还没有生产证据**（**没有生产消费者**）」 | **改**：配置键**是被消费的**（`index.ts` 的 `apply(ctx, rawConfig)`，live 走 `ctx.config`）⇒ 真实理由是**本轮范围**（填登记册 ≠ 扩工具）。两句差别很大：前者等于说「危险不存在」，后者说「危险有、但还没被测试固定」 |
| 2 | **重复** | §8.6 抄了一张 的 `verification` 表，与 `README` 表 B **逐行重复** | **删**：与「同一个判据只能有一份实现」同族（`tools/comparison-points.lib.ts` 的教训）⇒ 本节只留**结论**，逐条去 `README` |
| 3 | **重复** | `README` 的 **v1.15.72 版本行整段复制** `CHANGELOG` 同名条目 | **删**：与 v1.15.66 §5 整表复制 `adr/0085` §8.6 同病 —— **而我在 v1.15.65 就为它立过规矩**（增量只写一层，另一层留数字与指向） |
| 4 | **遗漏** | **模块归属表**（`BACKLOG` T15 明写「**用户要求**」）**从未做过、也从未在状态里提过** | **补**（见下）；并把它的完成度写进 `BACKLOG` T15 状态，不再当成已完成 |
| 5 | **遗漏** | **`D1` 的再框定**从「`T15` 之后」的清单里**丢了** —— 我只写过 `D2`/`D3` | **补**：`BACKLOG` T15 与 §6 都涉及 D1/D2/D3 三条，之前漏了 D1 |

### 模块归属表：**生成**，不手写（这条是本节最可复用的结论）

`BACKLOG` 要求的形状是 `| Module | Owns | Reads | Writes | Must not own |`（**27** 个一级模块 = 目录 + `index.ts`）。
**手写  ≈ 格子必然腐烂**，而 `AGENTS.md` 的规矩正是「**能推出来的字段不要手写**」。
⚠ **v1.15.73 在这里写的是「28」** —— 而**同一个探针当时就打印着 **（见 §8.9 第 ）。

关键在于：这张表的**用途**是「**暴露『谁开始越权』**」，而本仓的「越权」**已经有一份可执行的判据** ——
`tools/audit-layers.lib.ts` 的 `DIRECTION_RULES`（禁向）· `FORBIDDEN_TARGETS_EVERYWHERE`（）· `PURE_MODULES`（文件）。
⇒ 新增探针 `../.docs/fix/2026-09-12/t15-module-ownership.ts`：**直接 import 那份判据**（判据收一处，**不写第二份禁向表**），
再用**真实 import 图**算出每个模块的实测出边/入边，把「它实际在读谁」与「它不许读谁」并排。

**四列的可推性（必须分开说，否则会造假）**：

| 列 | 可机械推出？ | 做法 |
|---|---|---|
| `Reads` | **可以** | 真实 import 出边（正则抽取，与 `audit-wiring.lib.ts` 同族；**不是 AST**，已知边界写在探针里） |
| `Must not own` | **可以** | 来自 `DIRECTION_RULES` / `FORBIDDEN_TARGETS_EVERYWHERE` —— 那本来就**有门在守** |
| `Owns` | **不可以**（语义） | 只对 ADR 已定过的层给结论，其余**标「未核」，不编一句** |
| `Writes` | **不可以**（副作用） | **已登记在别处** —— `derived-file-v1` / `memory-file-v1` 两条契约（判据收一处，不在这里再列一份派生件清单） |

**探针实测出来的两条既有结论（与 `audit-layers` 文件头一致，此处得到独立复现）**：
- **`core/` 不是「纯函数层」，是「脊柱」**：文件里**只有 **（`paths`/`types`/`util`/`polarity`）是 0 import，
  且 `core` 实际 import `evidence`/`persistence`/`security`、还 import `node:fs`。
- 存在 `{core, evidence, persistence}` **层间环** —— 这也是 `audit-layers` **明确不判**它的原因（写成禁令就是**假闸门**）。

### 第 「测量错、不是被测量错」——**新探针自己在输出里露馅**

`t15-module-ownership.ts` 第一版把 `PURE_MODULES`（**文件级**白名单）**聚合成层级**，于是 `core` 那一行同时印出：

    | `core` | … import evidence · persistence · security … | 纯模块 **是**（0 import） | import `node:fs` |

**一行之内自相矛盾**：既说它 0 import，又列出它的 import。改口径为「层内白名单文件数 / 该层文件数」后为 ****。
⇒ **教训**：聚合口径（文件 → 层）**自己就是一个判据**，错法和「正则写窄」一样常见；
**看一行具体输出就会发现** —— 这次仍然不需要发布出去（靠**读自己刚生成的那张表**拦住）。

## 8.8 节序事故（v1.15.73）：我把 `§8.5` 追加到了 `§8.6`/`§8.7` **之后**

**症状**：本节的文件头顺序曾是 `§8 → §8.6 → §8.7 → §8.5`。`§8.5`（第七轮）排在 `§8.6`（第八轮切片）**之后**。

**这是本仓记过的同一个病，第三次**：第一轮记的是 `### 6.11` 被追加到 §七之后；
v1.15.69 记的是 `adr/0085` 的 `§8.7/§8.8` 被追加到 `§9` 之后 —— 那次的**修法**就是「把 `§9` 移到最后」。

**根因**：我每次都在**文件末尾追加**新小节（那是最省事的写法），而**小节号是语义顺序、追加位置是物理顺序**，
两者一旦不一致就产生这种错。⇒ **规则**：**新增 `§x.y` 时必须插到它的数值位置，不许一律 append**；
**改完数一遍 `## ` 标题的顺序**（不是只数个数 —— 个数对、顺序错正是本病的特征：本次标题数 9→11，个数一直「对」）。

**同轮另一处编号错误**：`§8.6` 原标题写「**第八轮**（v1.15.72）」——
而 **v1.15.72 是「`T15` 切片 2」这一交付，不是元审查的一轮**（第八轮元审查是 v1.15.73，即本节）。
⇒ 已改为「**切片 2（v1.15.72）**」。**「轮次」与「切片」是两种不同的计数，混用会让「第几轮审了谁」无从复核。**

## 8.9 第九轮（v1.15.74）：切片 3 —— `D1`/`D2`/`D3` 的判定，以及一次**被我自己的工具当场抓到的计数错**

### 1. 计数错：我写「**28** 个一级模块」，真值 **27** —— 而**同一个探针当时正打印着 **

`README` / `BACKLOG` / 本节 / `CHANGELOG` 共 ****写了「一级模块」（以及由它算出的 `28×5≈140`、`≈112`）。
真值 = **目录 + `index.ts` = 27**（`t15-module-ownership.ts` 输出 **27** 行，`BACKLOG` 列的名单也是 27 ——
我数出「28」是因为正则把**下一条 bullet 里的 `drift`** 也算进了模块名单）。

**这是本轮最刺眼的一处**：我上一轮刚写下「**这张表是生成的，不是手写的**」，
然后**手写了一个由该生成器输出的数**，而且**没看它打印什么**。
⇒ 与 v1.15.69 的 `BACKLOG` 计数错、v1.15.70 的「工具 schema 面整族漏掉」是**同一个动作**：
**我引用了自己工具的输出，却没读它。** 修法：探针现在**自己打印「一级模块 」**，文档写「**以探针打印为准**」。

### 2. 形状不符：探针输出 6 列，而要求的是 5 列

`BACKLOG` 要求的形状是 `| Module | Owns | Reads | Writes | Must not own |`，
而 v1.15.73 的探针输出的是 `Module / Reads / 入边 / Must not own / 纯模块 / 副作用信号` ——
**既没有 `Owns` 也没有 `Writes` 列**，读者拿不到要求的形状。
⇒ 已改为**正好那 5 列**（`Owns` 未核的层写「未核」、`Writes` 指向两条契约），
把「入边 / 纯模块占比」挪到**第二张辅助表**，不挤进要求的形状里。

### 3. `D1` 的判定：**保留** —— 理由是「**现时的验证价值**」，不是「将来可能有用」

`BACKLOG` D1 的判据：「若 `ChangeSet` **没有生产消费者 / 没有验证价值 / 没有外部契约** ⇒ **删**」。

**逐条实测（递归搜，不是只看顶层 `*.ts`）**：

| 条件 | 实测 | 结论 |
|---|---|---|
| 没有生产消费者 | 生产侧只有 `core/change-set.ts` 自身 + `core/projection-store.ts` 的 **`import type`** 与 `invalidateFor?(set: ChangeSet)`（**该方法本身也未接线**）；**生产侧 实例化**（⚠ **不是「全仓 」** —— `test/change-set.test.ts` 有 、`test/projection-store.test.ts` 有 ，V1.15.75 更正口径） | **成立** |
| 没有外部契约 | 不在登记册的契约里，不在工具 schema / 配置键 / 落盘格式里 | **成立** |
| **没有验证价值** | ❌ **不成立** —— 它是 **`tools/audit-wiring.selftest.ts:132-140` 的「真仓库已知答案」**：该断言要求 `countCallSites(prod, "ChangeSet").sites === 0`，用来**标定 A 类检测器**（「导出但生产无调用点」）。`tools/fixtures/wiring-fixture.ts:37` 也把它列为现成案例 | **不成立** |

⇒ **判定：保留。** 关键区别：它**不是**「将来可能有用」（那正是 D1 要拒绝的），
而是**现在就被另一道门的标定用着** —— 删掉它会**打掉一条检测器的已知答案**，把那条棘轮变弱。

**这同时纠正了一个被我夸大的口径**：我曾说「`T15` 之后 D1/D2/D3 会**从拍脑袋变成按契约机械判定**」。
**`D1` 不是。** 三个条件里前两条可机械判（且都成立），**唯一决定性的第三条「有没有验证价值」是主观的** ——
而它恰好**推翻**了前两条导向的结论。**诚实表述**：`T15` 让 D1 的**证据收集**变机械（该查谁、查到什么），
**没有**让**判定**变机械。这条要留在册，免得下一个人以为可以照条件表自动删。

### 4. `D2` 的漂移细分：**可以**由登记册机械判

`D2` 要求把 `drift` 分成 **名称 / 结构 / 语义 / 行为** 四类（否则会把「无害重命名」与「真兼容性破坏」混在一起）。
登记册的 `allowed changes` / `forbidden changes` 两列**已经隐含了这个分类** ⇒ 四类的判据就是「**它落在哪一列**」：

| 漂移类 | 一句话 | 由登记册哪一列判定 | 例 |
|---|---|---|---|
| **名称漂移** | 只改名，行为不变 | 登记册里各条契约的 `forbidden` **都含名称** ⇒ 一律要走弃用流程 | `mode` 改名 / 工具改名 |
| **结构漂移** | 增删字段或参数 | `allowed` 允许**加可选**参数；`forbidden` 收**删/改名** | 给 `read_shadow` 加可选参数 |
| **语义漂移** | 名字不变、含义变 | `stability = hard` 的**语义面** ⇒ 禁止（`adr/0084`：**显式 0 ≠ 未传**） | 改已生效配置键的默认语义 |
| **行为漂移** | 可观察输出变、但数据结构不变 | `stability = soft` 的面 ⇒ 允许，**写 `CHANGELOG` 即可** | `RECALL_PREFIX` 措辞 / 派生件格式 |

⇒ **判据落点**：`D2` 不需要新造一张表，只需要**指明四种漂移各自对应登记册的哪一列**（上表）。

### 5. `D3` 的再框定：**机械可判**，答案是「内部大对象不是 protected」

`D3` 的原文：「若 T15 明确『**旧的大对象不是 protected、新的细粒度对象才是**』，则 D3 不再是兼容性问题，
而是**允许的 breaking internal refactor**」。

**检查登记册里的契约，全是「细粒度面」**：工具名 · 参数名 · `mode` 串 · 废止映射 · 配置键 · 记忆文件名 · 派生件格式 · 提示段。
**没有任何一条是内部大对象** —— `ChangeSet` / `ShadowNode` / `ProjectionStore` / `ReadQuery` **都不在册**。

⇒ **D3 判定：T15 已明确「内部大对象不在受保护面内」** ⇒ 对它们的破坏性重构**是允许的**，
不属于兼容性问题。**这条是机械可判的**：只需问「登记册里有没有内部对象」，答案是没有。

### 6. 切片 3 的完成度（`D1`/`D2`/`D3` 三条一起）

| 项 | 判定 | 机械可判？ |
|---|---|---|
| `D1` | **保留**（现时的验证价值 = 别处门的标定锚点） | ❌ 第三条件主观（见第 ） |
| `D2` | **四类漂移各自对应登记册的一列** | ✅ |
| `D3` | **内部大对象不在册 ⇒ 允许的 breaking internal refactor** | ✅ |

**未做**：没有为 `D2` 建立「漂移类」的**可执行检测**（那需要读 `audit-drift.ts` 的判据并分类），
本轮只给出**分类的判据落点**；`D1` 的**保留**也**没有**写成门的断言（它现在是「另一道门的夹具」，隐式成立）。

## 8.10 第十轮（v1.15.75）：「A 段 」—— **泳道指着它，而它从未被写下过**

### 1. 遗漏：一个「指针没有目标」的交付物

泳道（`adr/0083` §14.1）是 `T8 → T15 → D1/D2/D3 → **A段** → T2(B段) → …`。
`T15` 与 `D1/D2/D3` 都已判完 ⇒ **下一项就是 `A段`**。而 `BACKLOG` 里两处提到它，**都只引用、不给内容**：

    BACKLOG:792   > 建起来之后，**D1/D2/D3 与 A 段 会从「拍脑袋决策」变成「按契约机械判定」**
    BACKLOG:1259  D1/D2/D3 + A 段  ⚠ **不要先拍** —— 它们本质都是「哪些东西算受保护契约」，故排在 T15 之后

**它的定义藏在 之前，而且藏在一个标着 ✅ 已结案的小节里** ——
v1.15.43「A 段残余 逐条分诊」把 分成 **真断线 1 · 配对包装一半 4 · 仅测试/公开面正当 7 · 无法判定 6**，
**「无法判定（6）」就是泳道说的「A 段 」**：它们的共同点是「**全部缺「产品决策」而非代码证据**」，
而那个产品决策**正是「哪些东西算受保护契约」** ⇒ 这就是它们被排在 `T15` 之后的**唯一原因**。

⇒ **一个已结案的小节里藏着一个未开工的下一阶段交付物。** 读者按泳道去找，会先看到「✅ 已结案」，然后以为做完了。
**这类遗漏不报错、也不腐烂** —— 它只是**永远不被做**。

### 2. 「A 段」这个名字在本仓指过三样东西（**干扰**）

| 出处 | 指什么 | 状态 |
|---|---|---|
| `adr/0062` / `BACKLOG:232` | `audit-wiring` 的 **A 类**线索（导出但生产无调用点），**** | ✅ 已落格（v1.15.33） |
| `BACKLOG:53` | 其中「需人工逐条查」的**残余 ** | ✅ 已分诊（v1.15.43） |
| `adr/0083` §14.1 泳道 | 上表那 里的 **「无法判定（6）」** | **未做 → 本轮首次枚举** |

⇒ 同名不同指 ⇒ **说「A 段已结案」和说「A 段是下一项」可以同时为真**，这正是没人发现它没做的原因。
**规则**：引用一个「段/桶」时，**要么给符号名，要么给到那一行的链接**，不要只写「A 段」。

### 3. 用登记册判这  ⇒ **撞出登记册自己的一个缺口**

与判据落点：

| # | 符号 | 缺的产品决策 | 用登记册判 |
|---|---|---|---|
| 1 | `hasNoUpgradeApi` | 删 vs 补源码级棘轮 | **不在册** ⇒ 无外部契约 + 无验证价值 ⇒ 不涉及兼容性 |
| 2 | `renderIntent` | 是否改调它（`observer/core.ts:31` 内联渲染） | **不在册** ⇒ 内部一致性问题，**内部重构允许** |
| 3 | `renderIdentityModel` | 是否进某条读路径 | ✅ **已判（v1.15.77，见 §8.12）**：属 `tool-output-v1` 的 **`soft`** 半边 ⇒ **允许接线，须写 `CHANGELOG`**，且不得让原有内容静默消失 |
| 4 | `progressiveDisclosure` | ADR-0048 成本折叠是否进默认读路径（与 #5 同一处决定） | ✅ **已判（§8.12）**：**允许接线，须写 `CHANGELOG``；但它会折叠内容 ⇒ **必须由信封披露被折叠的部分**（该契约的 `hard` 半边） |
| 5 | `refineTree` | 同上 | 同上（`core/knowledge/cost.ts:7` 原文即写「生产里**无调用点**」） |
| 6 | `relationForProposal` | 「T7 三选一」 | **被 T7 挡着** ⇒ 归 T7，本轮不判 |

**缺口**：#3/#4/#5 问的都是同一件事 ——「**把某个内部渲染/折叠接进默认读路径，算不算改受保护面？**」
而登记册覆盖的是 工具名 · 参数名 · `mode` 串 · 废止映射 · 配置键 · 记忆文件名 · 派生件格式 · 提示段 ——
**没有一条覆盖「工具的返回内容」**。`mode` 只决定「**读哪一类东西**」，**不约束「读出来长什么样」**，
而使用者实际依赖的恰恰是后者。
⇒ **登记册缺一条契约**（暂称 `tool-output-v1`）。**本轮只登记缺口、不入册** ——
入册要 字段，且要先定它的 `stability`，那是**一个新决定**，不属于「审查」。

### 4. 重复：同一处文档缺陷**第 ** ⇒ 这次**加门**（`audit:docs` 检查 ④）

v1.15.66 §5 → v1.15.72 → v1.15.74 → 本次：**四次都是「README 版本行整段复制 `CHANGELOG` 条目」**，
而我每次**都当个例修掉**。`AGENTS.md` 的归属表早写了「每层只回答它自己那个问题」，**但它只是散文**。
⇒ 按本仓先例（检查 ②③ 都诞生于「同一处手工纪律反复失效」）：**反复失效的手工纪律要变成门。**

**检查 ④ 的判据**：`README` 版本历史表里**当前版本那一行**，与 `CHANGELOG` 同名条目之间
**不得有 ≥40 字的连续逐字重复**。阈值 40 = 放行版本号/标题这类必然重合的短串。
**只查当前版本那一行** —— 历史行是**归档叙述**，拿新门要求重写历史行等于**用门改写归档**。
**它当场抓到了自己**：加完跑第一遍，报的正是 v1.15.74 那一行（881 字）。标定 3 组（⑮负/⑯正/⑰结构缺失）。

⚠ **加这道门时踩到的坑（留档）**：新检查会**短路别的组** ——
fixture 里 README 的版本历史表**没有当前版本那一行**时，④ 以结构缺失(2)退出，
把「期望 」的既有断言全变成 2。⇒ **加检查时必须同时补齐 fixture 的前置**，
否则「新门挂了」和「旧断言坏了」看起来一模一样。

### 4.5 **工具互相干扰**：我的新检查让另一道门误报了 

加完检查 ④，`npm run audit:ratchet` 立刻红：

    ✗ 线索**变多**：a1 24 → 28（+4）      ← `tools/docs-consistency.ts` 的 导出被判「生产无调用点」

**我没有改任何生产源码**（只加了 导出），而且那 既有导出在 HEAD 上贡献 **0** 条。
用 `git stash` 对比确认：HEAD = ，我的版本 = 。**根因**：

`audit-wiring.lib.ts` 的 `stripComments` / `maskStrings` 是**单趟状态机**，
其**写在注释里的已知边界**正是「**不处理正则字面量里的引号**」。
而我为读 `package.json` 写了 `match(/"version"\s*:\s*"([^"]+)"/)` —— **正则里的 `"` 让状态机错位**，
于是它把**本文件其后所有内容**当成字符串抹掉，**连 CLI 里那 调用点一起吞了** ⇒ 导出全成了「零调用点」。

⇒ **修法**：改用 `JSON.parse`（既避开该边界，也比正则更对）。
⇒ **教训（新的一类）**：既有的两类教训是「探针写窄」「聚合口径写错」，
**这一类的形态是「一个工具的输出，取决于另一个工具的解析器容不容得下我写的字面量」**。
**共用这一族正则/状态机判据的工具侧文件是 **（审计 CLI：`audit-wiring` / `audit-drift` / `audit-layers`，
加共享判据 `tools/comparison-points.lib.ts`；另有 测试文件也用）。
⚠ **v1.15.75 这里写的是「」—— 手写未核，v1.15.76 更正为 5**（口径：`Select-String -Path tools\*.ts -Pattern 'stripComments|maskStrings|comparison-points|bucketOf'`）。
⇒ **在新代码里写「含引号的正则字面量」「含 `/*` 的字符串」这类 token 时，要意识到有工具在按字面量解析你**。
**这次是棘轮抓住的**（`a1` 增多即非零退出）—— 这正是棘轮存在的意义，也是它第一次抓到「工具间」的缺陷而非「代码里」的缺陷。

### 5. 因果：v1.15.74 写「**全仓** 实例化 `ChangeSet`」—— **错**

正确口径是「**生产侧** 」：`new ChangeSet` 全仓共 **5** 处，**全在 `test/` 下**。
`D1` 的**结论不变**，但那句话把**测试**也算进「全仓」，而这条判据的全部意义就在于「**生产侧**有没有消费者」。


## 8.11 第十一轮（v1.15.76）：把 A 段「待核」收敛成「**缺口**」，并查出契约计数烂在 ****

### 1. A 段 #3/#4/#5 **不是三个待核，是同一个阻塞**

§8.10 把 #3 `renderIdentityModel` · #4 `progressiveDisclosure` · #5 `refineTree` 记为「**待核**」。
本轮**读了代码**，三条的答案收敛到**同一件事**：

| # | 实测（代码自带说明） | 为什么是同一件事 |
|---|---|---|
| 3 | `identity/timeline.ts:68` 写明：`mode:"identity-advance"` 会**写** `IdentityModel`，而 `renderIdentityModel` 在**生产无调用点**（`audit-wiring` A1 桶，测试引用 **0**） | **接线它 = 改 `mode:"identity-advance"` 的返回内容** |
| 4 | `core/knowledge/cost.ts:7` 原文：「本文件的 `progressiveDisclosure` / `refineTree` 在生产里**无调用点** —— 生产读路径…（默认路径可能刻意不做成本折叠）」 | **接线它 = 改默认读路径的返回内容** |
| 5 | 同上（同一个文件、**同一处决定**） | 同上 |

⇒ 三条问的都是「**改工具返回内容，算不算改受保护面？**」，而登记册里**没有一条契约覆盖「工具的返回内容」**
（`mode` 只决定「**读哪一类**」，**不约束「读出来长什么样」**）。
⇒ **它们是「缺契约」，不是「缺三个产品决策」。** 补齐 `tool-output-v1` 后，三条**一起**可判。
**✅ v1.15.77 已补齐并入册（见 §8.12）**，三条随之得到判定。

**这是本轮把「悬案」变成「缺口」的地方** —— 悬案无法行动，缺口可以。

### 2. 但**当时不能入册 `tool-output-v1`** —— 先看清它要落进什么里

入册一条新契约，就要把文档里所有「**契约 / 八族**」改成 9。**⚠ 本节首版写的「」是错的 ——
而错法比数字本身更值得记**：

| 项 | 实测（v1.15.77 复核） |
|---|---|
| 我写的 | 「散在 ****」（还在括号里分好了  / 11） |
| 同一条命令实际返回 | ** / 匹配**（`README`  · `BACKLOG`  · `adr/0086` ） |
| 为什么会错 | 我引用的是一次**更早、更窄**的运行结果；而**写下「有 N 处写了 8」这段文字，本身又添了几处** |

⇒ **自指**：`数「有几处写了 N」` 会**把自己数进去**，所以那段话里的数**在写下的瞬间就已经不对**。
这不是「数错了」，是**这类数根本不该这么写**。

**而这正是本仓反复出事的形态**：一个能数出来的数，**手写在文档里，且没有任何门守它**。
⇒ **入册的正确顺序是「先把计数收一处（或去掉），再加新契约」**，而不是往一个会腐烂的计数里再加一个。
**v1.15.76 只做了前置的一半**（核实并写下口径），**v1.15.77 完成了它**：**把契约条数从文档里全部删除**
（不写数 ⇒ 没有可腐烂的东西），**然后**才入册 `tool-output-v1`（见 §8.12）。

### 3. 同型的另一处：`audit:docs` 的检查条数**三处三个数**

v1.15.75 加了检查 ④，于是：

| 位置 | 写的 | 真值 |
|---|---|---|
| `AGENTS.md` | 「查**三条**」 | **4** |
| `tools/docs-consistency.ts` 文件头 | 「本文件就是那道门（**两条检查**）」／「0 = **两条**都过」 | **4** |

⇒ **同一件事三个地方三个数**，而且**没有任何门在守它**（检查 ② 守的是 `verify` 步骤，不是本工具的检查数）。
**修法（本轮已做）**：**不写数** —— `AGENTS.md` 改为「查下面这几条，**条数 = 下表行数**」并补第 ④ 行；
工具文件头改为不写条数。这是本仓规矩「**最好只给规则与命令、不写数**」的又一次应用。

### 4. 又一处手写未核的数：我把「这一族工具有 」写进了 CHANGELOG 与 §8.10

实测（命令：`Select-String -Path tools\*.ts -Pattern 'stripComments|maskStrings|comparison-points|bucketOf'`）：
**工具侧文件是 **（审计 CLI：`audit-wiring` / `audit-drift` / `audit-layers`，加共享判据 `tools/comparison-points.lib.ts`，
另有 测试文件也用）。**「7」是我随手写的、没数过** ⇒ 已更正为 5 并写下口径。

⇒ **这是同一个动作的第 **（v1.15.69 BACKLOG 计数 / v1.15.70 工具 schema 面 / v1.15.74 一级模块 28→27 / 本次 7→5）：
**我在「断言一个数」时，比在「写一段论证」时松懈得多。** 论证会被自己复查，数不会 —— 因为它看起来像事实。

## 8.12 第十二轮（v1.15.77）：**先删数、再入册** —— `tool-output-v1` 成为登记册的第 

### 1. 「」是错的，而**错法**比数字更值得记：**自指**

v1.15.76 我写「这些写法散在 ****」，并在括号里分好了 `README` 3 · `BACKLOG` 2 · `adr/0086` 11。
**同一条命令实际返回  / 匹配**（`README` 5 · `BACKLOG` 3 · `adr/0086` 13）。

| 项 | 值 |
|---|---|
| 我写的 | **16** |
| 实测（v1.15.77 复核） | ** / 匹配** |
| 为什么会错 | 我引用的是**一次更早、更窄**的运行结果；**而写下「有 N 处写了 8」这段文字本身又添了几处** |

⇒ **自指**：`数「有几处写了 N」` 会**把自己数进去** ⇒ 那个数**在写下的瞬间就已经不对**。
**这不是「数错了」，是这类数根本不该这么写。** 这也是本项目里**第一个「写下来就必然错」的数**。

### 2. 于是按顺序做：**先把条数删掉，再入册**

| 步 | 做了什么 |
|---|---|
| ① | **把契约条数从当前态文档里全部删除**（精确替换，**带守卫**：每处要求**恰好 匹配**，否则拒绝写盘；末尾复核残留） |
| ② | `README` 的登记册节加一条**规则**（不写数）：「**条数＝下表行数**」 |
| ③ | **然后**才把 `tool-output-v1` 加进表 A / 表 B —— **不再有任何数字需要跟着改** |

**守卫当场发挥了作用**：第一次运行时 **全部被拒绝**（`.NET` 的当前目录不是 PowerShell 的 `cd`，
路径解析成了仓库外）⇒ **零写盘**。若用裸 `.Replace()`，那 会「成功」而归零改动 —— 正是 v1.15.67 的形态。

**未删的「契约」是有意保留的**：`CHANGELOG` 与 `README` 的**版本历史行**、`adr/0086` 的 **§8.6 记录** ——
它们是**带日期的历史叙述**，当时确实是 。**改写它们才是造假。**

### 3. 第 ：`tool-output-v1`（本 ADR §2 的十条字段俱在，见 `README` 表 A/B）

| 字段 | 内容 |
|---|---|
| `id` / `surface` | `tool-output-v1` · **工具返回内容**（`read_shadow` / `recall_shadow` / `shadow_query` 吐出的 Markdown 骨架与**召回信封**字段） |
| `owner` | `query`（读侧组织；渲染片段来自 `retrieval/render.ts`） |
| `semantic meaning` | **`mode` 只决定「读哪一类」，这条决定「读出来长什么样」** —— 使用者实际依赖的是后者 |
| `stability` | **`soft`**（骨架与措辞可改，须写 `CHANGELOG`）/ **`hard`**（**「不静默丢内容」**：截断必须自报） |
| `allowed` / `forbidden` | 允许：改措辞 · 加信封字段 · 加新段落。禁止：**截断不报**（信封消失）· 把「坏件」与「空件」混同 · **接线任何优化时把丢掉的内容静默吞掉** |
| `evidence` | `query/reads.ts` 的信封构造 + `core/util.ts` 的 `RECALL_PREFIX` |
| `verification` | **强**：`test/recall-envelope.test.ts`（**逐字**断言信封四要素 `> 未返回的命中：` / `limit=N 上限 M 条` / `> 下一步：` / `> 未返回示例：`）+ `test/recall-attribution.test.ts:440`（前缀） |
| `ratchet` | 无桶覆盖（与其余各条同理：棘轮桶按**缺陷类**分，不按契约面分） |

### 4. A 段 #3/#4/#5 —— **现在可以判了**（这就是补齐这条契约的全部意义）

| # | 判定 | 依据（**机械**：查 `tool-output-v1` 的 `allowed` / `forbidden`） |
|---|---|---|
| 3 | `renderIdentityModel` **允许接线** | 它改的是 `mode:"identity-advance"` 的**返回内容** ⇒ 属 `tool-output-v1` 的 **`soft`** 半边 ⇒ **允许，须写 `CHANGELOG`**；且**不得让原有内容静默消失** |
| 4 | `progressiveDisclosure` 进默认读路径 **允许接线** | 同上（改默认返回内容）⇒ **`soft`：允许，须写 `CHANGELOG`**；**但它会折叠内容 ⇒ 必须由信封披露被折叠的部分**，否则违反该契约的 **`hard`** 半边（「不静默丢内容」） |
| 5 | `refineTree` | 与 #4 **同一处决定**，同一判定 |

⇒ **`T15` 的第一条「真·机械判定」在这里发生了**：三条从「**缺产品决策**」变成「**查契约的两半边**」，
而且**判据不是我的意见，是登记册里那两行**。
⚠ **判定 ≠ 已实施**：本轮**只给判定**，没有接线这三处（接线要写 `CHANGELOG` 与摊开折叠披露，属独立任务）。

## 8.13 补记（v1.15.83）：给「名字面」补门 —— `tool-schema-v1` / `config-keys-v1` 不再是「没人守」

**起因不是新发现**：§8.6 的结论就是「8 族里有 **2 族其实没人守**」（`tool-schema-v1` 只守 `mode` 描述、
`config-keys-v1` 连枚举都没有）⇒ **删参数 / 删键不会有任何东西变红**。当时不加门的理由已在 §8.7 第 改正为「**本轮范围**」。

**这一轮做了什么**（判据收一处：判据一份、门一份）：

| 层 | 落点 | 语义（与登记册的 `allowed` / `forbidden` 对齐） |
|---|---|---|
| 判据 | `tools/contract-surface.lib.ts` | `diffSurface`（差集：`missing` = 改名/删除 ⇒ **禁**；`added` = 新增 ⇒ **允许**）· `extractConfigKeys`（按**大括号深度**抽 `ShadowConfig` 顶层键，**跳过嵌套键**）· `extractParamNames` |
| 门 | `tools/contract-surface.selftest.ts`（随 `verify` 的 `test:all` 跑） | 两族各冻一份**清单**：缺名即红并**点名缺哪一个**；新增只报告。带**两条标定**：差集判据（相同 / 缺名 / 多名）+ 抽取判据（构造语料里 `inner` 与后续接口的键**不得**入选） |
| 重生成清单 | `SHADOW_PRINT_SURFACE=1 node tools/contract-surface.selftest.ts` | 只在「**确属有意新增**」时跑；改完清单要在 `CHANGELOG` 说清 |

**为什么冻「清单」而不是「计数」**：计数只答「变了没有」——**点名缺谁**它答不出来，
而这两族的 `forbidden` 恰恰是**改名 / 删除**这类必须点名的变化（本仓对「数字与列表互相矛盾」有前科：§8.9 第 ）。

**这道门刻意不做的事（覆盖面就这么大，`README` 的完成度表已按此写）**：
- **只守名字**：参数的类型 / 枚举值 / 默认语义**仍无人守**；`config-keys-v1` 的**子键**（`recall.enabled` 之类）**不守**，
  已生效键的默认语义归 `adr/0084`。
- **新增不红**：那是登记册的 `allowed`，不是漏判；但新增会**打印出来**，人看得见。
- **不为凑列造桶**：`README` 表 B 的 `ratchet` 列仍写「无桶覆盖」（棘轮桶按**缺陷类**分，不按契约面分）。

**复算（可重放）**：

```powershell
node tools/contract-surface.selftest.ts                          # 绿 ⇒ 冻结清单无一缺失；同时打印各工具参数计数与顶层键计数
SHADOW_PRINT_SURFACE=1 node tools/contract-surface.selftest.ts   # 重新生成清单（粘贴回 selftest）
```

**同轮（v1.15.83）另外三处**：① `README` 的 mode 计数（此前写「全部  mode」，与**受门保护的 62** 冲突 ⇒ **删数**，指向门）；
② `tools/retrieval-eval.ts` 的语料根由「只推一层」改为**候选三级**（此前 `<工作区>/vendor/dsh-shadow` 布局下 `npm run verify` 第 5 步 **exit 2** ⇒ **后 3 步不跑**）；
③ `.gitattributes` 钉 `dist/** text eol=lf`（此前每次 build 后 **** dist 假脏）。三处细节见 `CHANGELOG` 的 v1.目。
