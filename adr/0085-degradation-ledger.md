# ADR-0085: 能力降级台账 —— ADR-0049 的「可见信号」收口到一处

- 状态：**已接受并实现**（2026-09-12 / v1.15.65）
- 关联：**ADR-0049**（缺件不静默：`unavailable` 状态 / flush warn / debug trace **三者至少一个**，
  **`console.log` 不算**）· `BACKLOG.md` **T8 的 A 部分**（7 条静默降级）· `adr/0084`（T8 的 B 部分）·
  `adr/0083` §14.1（路线漂移）· `adr/0003`（派生件不是 source）
- 定位：**纪律**（不是能力）。产出一个机制 + 一条判定「什么算可见信号」的操作口径。

## 1. Context：ADR-0049 早就写下了判据，但**没有承接它的东西**

ADR-0049 的判据很明确（`adr/0049:45` 的自检清单）：任何可选增强「**有可见信号（状态 / warn / debug 之一）**」。
T8（v1.15.34 立账）顺着这条判据查出 **7 处降级没有任何一条信号**，且其中一个形态特别刺眼：

> `core/writer-llm.ts` 的 `streamText` 唯一的痕迹是 `if (opts.label) console.log(...)`。
> 而 `recallSelect` / `knowledgeNavigate` 传的是 `label: ""` ⇒ **连 log 都没有**。
> 更根本的是：`!llm`、`!route`、`finish.reason.kind === "error"|"aborted"` 三条路径
> **从不进 catch** ⇒ **即使有 label 也不会出声**。

⇒ 问题不是「不知道要留痕」，而是**留痕没有承接物**：写侧没有地方可写、读侧没有地方会渲染。
每一处降级各自 `console.log` 的结果，是**七种互不相同的、且大多不可见的**信号。

## 2. 决定一：信号的**唯一承接物**是 `WriterCore.degrade` + `getFlushWarn()`

```
写侧（core/writer-core.ts）      noteDegrade(core, capability, reason, effect)   ← 唯一写入口
                                  ↓
统一渲染（core/writer.ts）        getFlushWarn()  把台账渲染成横幅（每个能力一行）
                                  ↓
所有读路径（query/*.ts）          60+ 处 `+ flushWarn` ⇒ **一处渲染，全部 mode 同时获得信号**
```

**为什么不各能力自己打印**：`getFlushWarn()` 的结果已经被**每一个**读 handler 带在返回值里。
在它里面加一行，所有 mode 同时获得信号，不需要逐个 handler 改。
「判据收一处」在这里的具体含义就是：**「能力降级怎么让读者看见」只有一个答案**。

**为什么读侧也能写**：`queryLog` 与 `recallLedger` 是**读路径**上的降级，它们手里没有 `WriterCore`。
故经 `ShadowQueryDeps.noteDegrade` 注入同一个入口 —— 写侧与读侧写的是**同一个台账**、渲染成**同一个横幅**。

## 3. 决定二：`effect` 是**必填**，且「关掉」不算降级

两条都是为了不让信号退化成噪音：

1. **必须说清后果**（`DegradeNote.effect`）。ADR-0049 要的是「**可见**」，而可见的前提是说清**丢了什么**。
   一句「llmRecall failed」对读者没有用，因为他还得自己推断这会导致什么。
   ⇒ 每条留痕都要回答「你读到的这个东西少了什么 / 可能错在哪」。
2. **用户显式 `enabled: false` 不留痕**。ADR-0049 的判据是「**关掉或**缺件后行为退到某处却无信号」——
   但「关掉」是**用户自己的选择**，把它渲染成告警等于把读者的决定当成故障。
   ⇒ 留痕只在**该工作却没工作**时发生（缺件 / 调用失败 / 后端拒绝 / 坏件）。
   实现上由调用点的条件保证（如 `if (!obsOk && fs && ws && cfg.queryLog?.enabled !== false)`）。

## 4. 决定三：同类**覆盖**，且按能力名排序

- **覆盖**（`Map` 的键是能力名）：一直坏着的能力不该每回合追加一条 ——
  「一直坏着」与「刚刚坏」对读者是同一件事，刷屏只会让信号变成噪音（ADR-0049 的反面）。
- **排序**：`Map` 的插入序会随「哪个先降级」变化 ⇒ 同一状态两次读会得到不同字符串，
  这会让**逐字节比对**的门禁变脆。故渲染前按能力名排序。

## 5. 决定四：`projectionStore` 的静默判为**正当**（T8 第 6 条）+ 一条**类判据**

依据是 ADR-0049 **表里的行级豁免**（`adr/0049:38`）：「Projection Store | 缓存文件 |
读失败 / 坏行 → `null` → 重建（**缓存不是真相**，`rm -rf` 无影响）｜可见信号：**否**」。

### 5.1 类判据（写进 `core/projection-store.ts`，本仓只此一处）

> 一条降级**不算缺陷**（不必加可见信号）**当且仅当读者拿到的内容逐字节不变** ——
> 即被降级的那一步只是一个**冗余副本 / 加速结构**，它的缺失不改变答案。

这条判据的价值是**它替后面的人判**，而不是每条都重新拍一次脑袋。用它判出来的同类项：

| 项 | 判定 | 为什么 |
|---|---|---|
| `projectionStore` 缓存读失败 | **正当静默** | 重建结果与命中缓存逐字节等价（ADR-0003：源才是真相） |
| `query/observatory.ts` 的 `writeShadowReport` 写失败 | **正当静默** | 报告正文由 `query/reads.ts` 的 shadow-report 读查询**原样返回给读者**（`return scrubFinal(RECALL_PREFIX + text + flushWarn)`），落盘只是副本 |
| `core/writer-materialize.ts` sidecar 写失败 | **必须有信号** | `continue` 会让该日期目录的 L0 **不再写进 `_index.md`** ⇒ 索引少一行 = **内容变了** |

**T8 其余条目同理**：少摘要 / 窄召回 / 冷却失效 / 观测数据丢失 / Episodes 段消失 —— 都会改变读者看到的内容。

### 5.2 纠正我在本节前一版的**过度声明**（因果）

前一版我写：「同一份 ADR 的自检清单（`adr/0049:45`）要求**每一个**可选增强都有可见信号，
表里却给了豁免、清单里没写例外 ⇒ **ADR 内部张力**」。

**这个说法不准确，已改正**：那份清单的小标题是「**新增**可选增强时的门禁」（`adr/0049:42`）——
它约束的是**将来新增**的增强；而表格是**冻结时的现状台账**（`:28`「本 ADR 冻结时逐条对源码核对」）。
两者的**适用范围不同**，因此**不构成矛盾**。本裁定只需援引表格的行级豁免，
**不需要**、也不应该把一份冻结的 ADR 说成自相矛盾 —— 那会让读者去找一个不存在的裂缝。
（`core/projection-store.ts` 里同一段注释已同步改正。）

**该裁定的失效条件**：若这份缓存将来**参与答案**（存了不可重建的中间判断），
它就不再是「冗余副本」这一类，本裁定立即失效。

## 6. 落地清单（7 条 → 各自的信号形态）

| # | 能力 | 形态 | 备注 |
|---|---|---|---|
| 1 | `llmRecall` | 台账 + 横幅 | 修掉了「**连 log 都没有**」 |
| 2 | `summary` | 台账 + 横幅 | 「文件里没有摘要」与「尚未生成」此前**不可区分** |
| 3 | `recallExpansion`（语义 B 档） | 台账 + 横幅 | `README` 自称「**静默**退回 A 档」—— 承认写在文档里，读者在输出里看不到 |
| 4 | `queryLog`（**默认开启**） | `recordQueryObservation` 返回 `boolean` → 调用点留痕 | T8 里优先级最高（默认开启项的静默） |
| 5 | `recallLedger` | `readLedger` 区分 `corrupt`/`unreadable` → 调用点留痕 | 修掉「留了 `corrupt` 标记却**没有消费者**」= 等价于没留 |
| 6 | `projectionStore` | **不加信号**（裁定，见 §5） | 缓存不是真相 |
| 7 | `episodes` / 解析 | 台账 + 横幅 | ① 解析失败 ⇒ 该记忆从 Episodes/Decision 里**整个消失**；② 派生失败渲染成「暂无连续任务片段」 |
| **8** | **`abstracts` sidecar**（**T8-A 漏项，复查时补**） | 台账 + 横幅 | 写失败 ⇒ 该日期目录的 L0 **不再写进 `_index.md`**（索引**少一行** = 内容变了）⇒ **不属于**正当静默那一类 |

> **第 8 条是怎么漏的（留档）**：`writeAbstracts` 的 `catch` 就在我改 T8-A 的**同一个文件、同一个函数附近**，
> 我只给它保留了原来的 `console.log` 就过去了。`README` 自己早就标着它「**部分可见**：
> 索引里看不到它，但无显式 warn」—— 也就是说**文档写了、我读了、还是漏了**。
> 这不是「线索不够」，而是「修一个 `catch` 时没有顺手看邻近同类 `catch`」。
> ⇒ **修一类缺陷时要扫同类，不是只修清单上那几条**；清单是起点，不是范围。

另：`knowledgeEngine.llmNavigate` 已在 `query/reads.ts` 的 knowledge 读查询里**就地披露**「（…LLM 导航未启用/失败 → 确定性检索）」，
本 ADR **不**给它加第二条披露（同一件事只写一处）。它现在多给的是**原因**（`no-route` / `llm-error` …），
与那句「退到了哪里」是两个层级，不重复。

## 7. 我在写测试时犯的两个错（留档，因为它们正是本条的论据）

写 `test/t8-silent-degradation.test.ts` 的**端到端**断言时，我**两次**猜错读路径：

1. 用 `read_shadow({topic})` 去测 `queryLog` —— 实际 `recordQueryObservation` 挂在 `mode:"query"` 上；
2. 用 `mode:"recovery"` 去测 `recallLedger` —— 实际 `readLedger` 在**不传 mode** 的默认召回上，
   而 `recovery` 走的是另一个渲染器（它只是**复用了**前一轮留下的横幅，因为 `flushWarn` 是统一的）。

**两次都是「正对照」把我拦下来的**（第一版的正对照是「健康 fs 下 query-log 必须真的落盘」，
它当场变红，证明代码路径没走到、那句「没有横幅」是**假绿**）。

⇒ 两条教训：
- **端到端断言必须先证明路径真的被走到**，否则「断言通过」只证明「我没测到」。
- **我不该猜读路径**。为此新增取证探针 `.docs/fix/2026-09-12/t8a-read-path-map.ts`，
  把四条路径的**实际输出开头 + 落盘键**打出来，用事实代替猜测。
  这与 `adr/0084` §4 的教训同型（那次是探针自己把判据写窄了）：
  **凡「我以为是那样」的地方，先做一次能证伪的观察。**

## 8. 结果（可对账）

| 项 | 修前 | 修后 |
|---|---|---|
| 有可见信号的降级 | 0 / 7（`console.log` 不算） | **7 / 8**（含复查时补的 `abstracts`），另 1 条**裁定为正当静默**（ADR-0049:38 行级豁免） |
| 承接物 | 各处自己 `console.log` | **1 个台账 + 1 个渲染点**（`WriterCore.degrade` / `getFlushWarn`） |
| 健康路径输出 | — | **逐字节不变**（②组的负对照锁住） |
| `npm run verify` | 54/54 | **55/55** |

## 8.5 元审查 self-fix（同轮，v1.15.65）

用户在 T8 收口后又下了一次同一条元审查指令，本轮据此**回头审我自己的 v1.15.64/65 产出**，
查出四类问题并修正：

| 类 | 具体 | 处置 |
|---|---|---|
| **遗漏** | `abstracts` sidecar 写失败（§6 第 8 条）—— 站在同一个 `catch` 旁边却没给它加信号 | 已补信号 + 测试（⑤c 组） |
| **遗漏** | `README` 的默认开关表**仍把已修项标成 ⚠️静默**，且 `episodes` 仍写「**关不掉**」、注① 仍描述缺陷为现状 | 全表 + 注① + 三处「静默退回」措辞一并更新（v1.15.64/65 的结果写进去） |
| **因果（过度声明）** | 我说 ADR-0049 有「**内部张力**」（清单要求每个增强有信号 vs 表里给豁免）—— **不成立**，清单只管**新增**增强、表格是**冻结快照** | 已改正（§5.2），并写进 `adr/0049` 补记第 4 条 |
| **因果（范围夸大）** | `adr/0084` 写「本仓 **25 处** `Number(x) \|\| dflt`」—— 实为 `core/` 下的**修前快照** | 已改为带范围与时刻的表述 |
| **缺失环节** | `adr/0049` 的现状盘点表把 3 行「可见信号：否」冻结在那里，读者会当现状 | 已加 **补记**（增量表 + 「三选一实务上只用 flush warn」+ 类判据） |
| **缺失环节** | **一条「曾经存在过、但是一次性的」检查**：`CHANGELOG` 的 v1.15.22 条目里写着「三方版本一致 ✅ 均 `1.15.22`」—— 那是**人工核验过一次**，不是门。后果：`README` 的「当前版本」行**停在 `v1.15.40`，落后 26 个版本而无人报错** | 补成门：**`tools/version-consistency.ts`**（`npm run audit:version`，**已进 `verify`**；退出码 `0` 一致 / `1` 不一致 / `2` 结构缺失）+ `version-consistency.selftest.ts`（**spawn 真 CLI** 标定，5 组含**归档层隔离**）。一般化为「**能推出来的字段要么别手写、要么配一道门**」 |
| **干扰** | 多处 `文件:行号` 引用在我改代码后**已过期**（如 `core/writer-core.ts` 的 `showInIndex` 取默认那行 / `core/writer-materialize.ts` 的 `episodeShow` 闸门那行） | 见 §8.6：**先按层分，再动手**（归档层 102 处一处不改；当前态 14 处改写成不依赖行号的形态；权威表 10 处手工改符号引用） |

**新规则（写进 `dsh-shadow/AGENTS.md`）**：**改完代码必须回头核对引用它的文档行号** ——
行号是最易腐烂的引用形态，而仓库的三条纪律（每条断言带 `文件:行号`）依赖它。

### 8.6 过期行号：**先分层，再动手**（量化）

清点时发现，全仓 `.md` 里指向本轮改过的 14 个源文件的 `文件:行号` 呈**两层分布**，
**处置相反**。下面的数字**全部实测**（脚本 `.docs/fix/2026-09-12/live-doc-ref-repoint.ts`
的分层计数 + 一次全仓 `Select-String`），不是手算：

| 层 | 处数 | 处置 | 为什么 |
|---|---|---|---|
| **归档文档层**：`CHANGELOG.md` 34 · 已冻结 ADR 正文 28 · `adr/0083` 审查快照 6 | **68** | **一处不改** | 行号是「**当时 / 发现时**」的 —— 改写等于**伪造历史**，也抹掉「当时是在哪一行看到的」这条证据 |
| **当前态文档内的归档区间**：`README` 的版本历史表 · `BACKLOG` §〇 已结案 / §六§七 审查线索 | **34** | **一处不改** | 同上。**关键**：它们长在「当前态文档」里 ⇒ **只按文档分层不够，必须精确到行区间** |
| **当前态 + 受本次改动影响** | **14** | 改写成**不依赖行号**的形态（保留文件名） | 行号必须指向**当前**代码，否则会把读者送到别处、还让他以为看对了 |
| **当前态 + 未受影响**（`N < 该文件首个变更行`） | **10** | 不动 | 引用仍然正确，改了反而丢信息 |
| **权威表格**（`adr/0084` 的缺陷表 + `adr/0085` 自己的引用） | **10** | **手工**改成**符号引用** | 这两张表是 ADR 的权威内容，值得写准，不适合套机械脚本 |
| `AGENTS.md` | 2 | **故意保留** | 它们是「过期行号」的**举例**，改成符号就失去示例意义 |

判据是**客观可复算**的（不是逐条肉眼）：一个 `f:N` 可疑 ⟺ `N >= f` 自 `v1.15.63` 起
**首个变更行的旧行号**（从那里起文件内容整体位移）。脚本**默认干跑**、`--apply` 才落笔，
并打印每一处改动 —— 这次的 14 处就是先看清了计划再落笔的。

**第一版脚本错了**：它把 `README` 版本历史表里的 6 处也「修」了 —— **干跑**把它拦下来，
才补上「按**行区间**分层」这一步。

**一次自我更正（统计口径）**：我第一版的全仓统计里把 `references.md` 的
DSH 本体的 `src/index.ts`（第 96 行）也算成了本仓 `index.ts` —— **同一个文件名后缀**，正则不分仓库。
它实际引的是 **DSH 本体**的文件，与本仓无关 ⇒ 多算 2 处。已写进脚本的说明里当**已知误匹配模式**。

**优先形态**（写进 `AGENTS.md`）：**引符号名**（函数 / 常量 / 配置键），行号只在必要时加。
一句「`core/writer-core.ts` 的 `episodeShow`」在下次重构后**依然正确**，而 `:77` 不会。

## 9. 未做 / 诚实边界

- **`episodeParse` 与 `episodes` 两个生产者只有接线、没有端到端触发**：
  本仓夹具里没有能让 `parseMemory` / `deriveEpisodes` 抛异常的输入 ⇒
  这两条锁的是**机制与接线**（③组同型），不是它们各自的触发路径。**这是缺口，不是已验证。**
- **`recallLedger` 的「写失败」留痕未端到端触发**：`writeLedger` 只在
  `cooldownTurns > 0 && servedDetail.length` 时调用，而 `servedDetail` 需要「tier ≠ L0 且渲染被截断」
  的长记忆 ⇒ 夹具成本较高。本轮锁到「读不到 / 坏件」两种，写失败那条只有接线。
- **`knowledgeNavigate` 的留痕没有专门断言**（与已断言的三个生产者共用同一机制）。
- **一次观察未修**：台账**坏件**时本回合若走到写台账那一步，会把坏件**覆盖**掉
  （内容是 `{}` 派生的 `{turn:1}`）。原文件已无法解析 ⇒ 没有「可用数据」丢失，
  但**手工抢救的机会**同时消失。已在横幅的「后果」里写明；未改成「写到旁路文件」——
  那是另一个决定（需要定「坏件保留多久、谁清理」），不在 T8 范围内。
- `summary` 的降级留痕是**能力级**而非**逐条记忆级**：横幅只说「摘要能力坏了」，
  不说「这 37 条记忆没有摘要」。后者需要逐条记账，成本与噪音都更高。
- **本轮我自己踩的坑（留档）**：改完 `writeAbstracts` / `observatory` / `projection-store` 后，
  我跑了 `npx tsc --noEmit`（**只类型检查、不产出**）就去跑 `test/*.ts` —— 而那些测试
  **import 的是 `dist/`**（构建产物）⇒ 断言读到的是**旧代码**，「补了信号却没上横幅」的假象由此而来。
  是探针（`.docs/fix/2026-09-12/t8a-abstracts-note-probe.ts`：打印「writeText 被拒次数」与横幅有无）
  把它定位出来的 —— **改完源码必须 `npm run build` 再跑 `dist/` 消费者**；
  这也再次说明「先做一次能证伪的观察」比继续猜便宜。
